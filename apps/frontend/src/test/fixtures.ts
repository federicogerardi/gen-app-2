/**
 * Shared test fixtures.
 * Import constants from here to keep test configuration in a single place.
 */

/** Base URL used by the backend API in test environments */
export const TEST_API_BASE_URL = 'http://localhost:3000';
