---
status: active
version: 3.1
last-reviewed: 2026-05-27
next-review-date: 2026-08-03
owner: Domain Architecture
---

# Domain Naming Decision Log

## Decision Rules
- Every new canonical term must be logged before broad reuse.
- Decisions must include rationale and propagation scope.
- Conflicting synonyms must be resolved here first.
- Decision IDs must be unique and immutable; revisions must update the existing row or reference supersession, never duplicate an ID.

---

## Approved Naming Decisions

| ID | Date | Canonical Term | Decision | Rationale | Scope |
| --- | --- | --- | --- | --- | --- |
| DDD-001 | 2026-05-03 | Artifact | `Artifact` is the canonical term for a persisted generation output. Not `Output`, `Result`, `Generation`, or `Document`. | `artifact` is used consistently in DB schema (`artifacts` table), TypeScript types (`ArtifactType`, `ArtifactStatus`), and frontend contracts. Renaming would require broad migration. | all contexts |
| DDD-002 | 2026-05-03 | GenerationRequest | `GenerationRequest` is the canonical term for the command that initiates generation. Not `GenerationInput`, `GenerationPayload`, or `CreateGenerationDto`. | Consistent with `requestId` as the primary correlation identifier throughout the system. | Generation, Frontend |
| DDD-003 | 2026-05-03 | WorkflowStep | `WorkflowStep` is the canonical backend term for a step in a multi-step generation flow. Not `Task`, `Stage`, or `Phase`. | Backend uses `WorkflowStepDescriptor` and `WorkflowStepState` consistently. | Generation |
| DDD-004 | 2026-05-03 | ToolStep | `ToolStep` is the canonical frontend term for a named step in a specific tool's flow (e.g., `optin`, `vsl`, `landing`). Coexists with `WorkflowStep` as the frontend concrete counterpart. | Frontend tool machines use `ToolStep` as a union type of actual step names; backend `WorkflowStep` is the abstract descriptor. Kept distinct to avoid confusion. See DDD-C-001. | Frontend |
| DDD-005 | 2026-05-03 | ClaimUsage | `ClaimUsage` is the canonical command name for the atomic quota reservation operation. Not `DecrementQuota`, `ConsumeQuota`, or `CheckAndReserve`. | `claimUsage` is used in `UsageAdapter` interface and `usageMachine` actor consistently. | Usage/Quota |
| DDD-006 | 2026-05-03 | ReadinessSnapshot | `ReadinessSnapshot` (not `ReadinessState` or `CanStartFlags`) is the canonical term for the computed start-eligibility object on the tool page. | Used in `tool-page.machine.ts` and documented in `tool-generation-flow-source-of-truth-spec.md`. Deprecated aliases `ToolPageReadinessSnapshot`, `ToolPageReadinessReasonCode` (see DDD-014) have been removed. | Frontend |
| DDD-007 | 2026-05-03 | ExtractionContext | `ExtractionContext` is the canonical term for the structured payload extracted from a briefing file and used as generation input. Not `BriefingContext`, `ExtractedData`, or `ParsedBriefing`. | Defined in `apps/frontend/src/features/generation/machines/frontend-stream.machine.ts:21`; deprecated aliases `ToolExtractionContext` and `BriefingContext` remain as backward-compat only (see DDD-012). | Frontend, Generation |
| DDD-008 | 2026-05-03 | AuthSessionPrincipal | `AuthSessionPrincipal` is the canonical shared read model passed across Auth → other contexts. Not `CurrentUser`, `LoggedInUser`, or `SessionUser`. | Defined explicitly as `AuthSessionPrincipal` in `apps/backend/src/lib/types/auth.ts:46`. | Auth |
| DDD-009 | 2026-05-03 | BackendStreamEvent | `BackendStreamEvent` is the canonical term for SSE events emitted during a generation. Types: `start`, `chunk`, `terminal`. | Defined in `packages/contracts/src/index.ts` (shared contract). Consistent with SSE protocol naming. | Generation, Frontend |
| DDD-010 | 2026-05-03 | HydrationResult | `HydrationResult` is the canonical term for the state snapshot produced when loading prior artifact history into a ToolPage. Not `ResumeState`, `CheckpointData`, or `SessionSnapshot`. | `HydrationResult` is the explicit type in `tool-page.machine.ts` and aligns with `StepHydration` service naming. | Frontend |
| DDD-011 | 2026-05-03 (revised 2026-05-04) | ToolKey (FE form registry → ToolFormKey) | ~~`ToolKey` is the registry implementation type (`keyof typeof toolFormRegistry`) in `tool-form-architecture.ts`.~~ **Revised (DDD-029)**: The FE form registry implementation type is renamed `ToolFormKey`. `ToolKey` is no longer a standalone FE domain term — it is promoted to cross-context canonical identifier for Tool identity. Docs must use `SupportedTool` or `ToolKey` (cross-context) when referring to domain concepts; `ToolFormKey` is acceptable only in code-level spec sections describing the form registry. | `ToolKey` as both a FE-specific registry type and a Generation step-orchestration identifier created a naming collision (DDD-011 vs DDD-025). Resolution: `ToolKey` becomes the single cross-context canonical term; the FE form registry implementation type takes the distinct name `ToolFormKey`. Superseded by DDD-029. | Frontend (superseded by DDD-029) |
| DDD-018 | 2026-05-03 | RegistryBackedWorkflowType / RegistryBackedArtifactType / RegistryBackedToolKey | **Declassified as internal implementation type** — not for domain documentation or canonical glossary entries. These three open-union types in `apps/backend/src/lib/types/xstate.ts` (lines 5–7) extend canonical value objects (`ToolWorkflow`, `ArtifactType`) with `string & {}` to accommodate registry-driven entries not present in the canonical enumerations at compile time. They must not appear in domain documentation or glossary entries as primary terms. The canonical terms (`ToolWorkflow`, `ArtifactType`, `ToolKey`) remain the authoritative names for domain communication. | These types surface in `GenerationSystemContext` and `generation-system.machine.ts` where the registry may inject values unknown at compile time. Declassification confirmed 2026-05-03: permanent infrastructure-layer widening types, not candidates for promotion to first-class domain concepts. | Generation |
| DDD-012 | 2026-05-03 | ExtractionContext | `ExtractionContext` is the canonical term for the structured briefing extraction payload. `ToolExtractionContext` (former name in `frontend-stream.machine.ts`) and `BriefingContext` (former definition in `tool-form-architecture.ts`) were aliases for the same concept; both removed as live definitions and replaced with backward-compat aliases pointing to `ExtractionContext`. | Three names for the same Value Object caused confusion at import sites. `ExtractionContext` aligns with DDD-007 and the canonical glossary entry. | Frontend |
| DDD-013 | 2026-05-03 | ToolStepStatus / `'done'` | `ToolStepStatus` with value `'done'` is the canonical representation of a completed step. The value `'completed'` was a drift in `tool-page.machine.ts`, `tool-ux-state.ts`, and UI components relative to the canonical definition in `tool-flow.machine.ts`. All occurrences updated. `CanonicalToolUiState = 'completed'` and `ArtifactStatus = 'completed'` are unrelated concepts and were not modified. | Canonical source is `tool-flow.machine.ts:5`. All derived types and runtime comparisons must use `'done'`. | Frontend |
| DDD-014 | 2026-05-03 | ReadinessSnapshot / ReadinessReasonCode | `ReadinessSnapshot` and `ReadinessReasonCode` are the canonical terms for the start-eligibility snapshot and its reason codes (see DDD-006). The `ToolPage` prefix was redundant within the `tool-page.machine.ts` namespace. Backward-compat aliases `ToolPageReadinessSnapshot = ReadinessSnapshot` and `ToolPageReadinessReasonCode = ReadinessReasonCode` were introduced for 1 deprecation cycle and **removed in the 2026-05-03 quality audit** — these aliases no longer exist in the codebase. | Prefix removal reduces verbosity without losing precision. | Frontend |
| DDD-015 | 2026-05-03 | RegistryVersion / RegistrySnapshotRef | `RegistryVersion` and `RegistrySnapshotRef` are the canonical terms for the tool registry branding types in `apps/backend/src/lib/types/xstate.ts`. The former `ToolRegistry` prefix was redundant in the `types/` namespace. Backward-compat aliases `ToolRegistryVersion = RegistryVersion` and `ToolRegistrySnapshotRef = RegistrySnapshotRef` maintained in `xstate.ts`. | Consistent with DDD-001 principle of removing context-redundant prefixes. | Generation |
| DDD-016 | 2026-05-03 | LlmUsageMetrics | `LlmUsageMetrics` is the canonical term for the token and cost metrics of a completed generation. The former `StreamUsageMetrics` interface in `xstate.ts` was a structural duplicate; it is now a backward-compat alias `export type StreamUsageMetrics = LlmUsageMetrics`. The definition authority is `apps/backend/src/lib/types/xstate.ts`; `generation.adapters.ts` imports and re-exports it. Backward-compat alias `StreamUsageMetrics` **removal-target: 2026-Q3**; shim: `apps/backend/src/lib/types/xstate.ts:97`. | Single authoritative definition eliminates structural drift. Source field in glossary updated. | Generation |
| DDD-017 | 2026-05-03 | ArtifactStatus | `ArtifactStatus` (from `apps/backend/src/lib/types/artifact.ts`) is the canonical term for the lifecycle state of an artifact in persistence. The former `PersistedArtifactStatus = 'generating' \| 'completed' \| 'failed'` in `generation.adapters.ts` was a structural duplicate with identical values. It is now `export type PersistedArtifactStatus = ArtifactStatus` (backward-compat alias). All consumer files that import `PersistedArtifactStatus` continue to compile without modification. Backward-compat alias `PersistedArtifactStatus` **removal-target: 2026-Q3**; shim files: `apps/backend/src/lib/adapters/generation.adapters.ts`, `apps/backend/src/lib/adapters/index.ts`, `apps/backend/src/lib/adapters/postgres-redis.stub.ts`. | Aligns with DDD-001 (canonical `ArtifactStatus` as source of truth). | Generation |
| DDD-020 | 2026-05-03 | ArtifactRelaunch | `ArtifactRelaunch` is the canonical concept for starting a new generation cycle from an existing `Artifact`. The relaunch entry must be defined once at domain level (not split into primary/secondary concepts). `HydrationResult` resolution is deterministic by source `ArtifactType`: direct context recovery for `extraction`, linked extraction recovery for `content` using `briefingId` and/or `extractionArtifactId`. In relaunch-init state (`prefilled-regenerate`), the effective primary action is `regenerate-current-step`; `start-generation` is constrained to `intent='new'` first-time generation. Default runtime intent for artifact-driven relaunch entries is `regenerate`. **Scope note**: `ArtifactRelaunch` is a **governance-only concept** — it is not materialized as a TypeScript type in the codebase. Implementation uses `runtimeIntent: WorkflowRunMode` and `relaunchFromArtifactId` as concrete identifiers; these are valid implementation variants of the same domain concept and must not be promoted to separate domain terms. | Current frontend terms `ToolRelaunchIntent` and copy keys `relaunchPrimary`/`relaunchSecondary` indicate implementation/UI variants for one domain concept. Canonical UL requires one relaunch concept, one deterministic relaunch-init CTA policy, and one default runtime intent for artifact-driven entries. | Frontend, Generation |
| DDD-021 | 2026-05-03 | ExtractionContext | `ExtractionContext` completeness is deterministic and mandatory at ToolStep dispatch time. The GenerationRequest for step 1 must include a non-empty `briefingText` and a structured `extractionPayload` resolved through ordered fallbacks: parsed extraction artifact content (raw JSON, fenced JSON, or payload envelope), then `sourceRequest.input.extractionPayload` when content is non-JSON. | Session evidence showed real smoke requests with large `briefingText` but empty `extractionPayload`, causing semantic context loss. Resolution is now enforced in frontend runtime and verified by tests: `apps/frontend/src/features/tools/runtime/tools-client.ts:113-329`, `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx:47-589`, `apps/frontend/src/features/tools/runtime/tools-client.test.ts:134-187`. | Frontend |
| DDD-022 | 2026-05-03 | RouteType | `RouteType` (`'generic' \| 'tool' \| 'extraction' \| null`) is an **internal implementation type** local to `apps/backend/src/lib/machines/generation-system.machine.ts`. It is not a bounded-context domain term and must not appear in domain documentation or glossary entries. The canonical routing concepts remain `ToolWorkflow` (Generation context) and `SupportedTool` (Frontend context). | `RouteType` controls internal dispatch branching within `GenerationSystem`; its values map to `ToolWorkflow` values for routing logic. Registered 2026-05-03 to prevent future confusion with canonical terms and to block promotion attempts. | Generation |
| DDD-023 | 2026-05-04 | BackendStreamEvent / GenerationRequest (contract authority) | `apps/frontend/src/features/generation/contracts/backend-stream.ts` is the **single authoritative FE source** for `BackendStreamEvent`, `GenerationRequest`, `ArtifactType`, and `OutputFormat`. No other FE file may re-define these types. A compile-time parity guard (`backend-stream.parity.guard.ts`) enforces structural identity with the pinned BE shapes in `apps/backend/src/lib/runtime/stream-contract.ts` and `apps/backend/src/lib/runtime/request-contract.ts`. Both BE source files carry doc-comments declaring their authority. | Eliminates silent drift across the FE↔BE contract boundary. Parity guard fails the typecheck if FE types drift from the pinned BE shapes. | Generation, Frontend |
| DDD-024 | 2026-05-04 | Legacy shim / deprecated export removal | Deprecated types and functions removed from `apps/frontend/src/features/tools/runtime/tool-ux-state.ts` in Sprint 4: `ToolUiDerivationInput`, `ToolUiDerivationOutput`, `deriveCanonicalToolUiState` (old-style overload). File is now a pure re-export shim for `CanonicalToolUiState`, `PrimaryActionPolicy`, `SecondaryActionFlags`, `derivePrimaryActionLabel`. Removed from `apps/frontend/src/features/tools/runtime/useToolForm.ts`: `useStepSelection` (unused, no runtime consumers), `useToolUiState` (test-only, mocked in tests but not imported at runtime). Deprecated optional event payload fields `hasExtractionContext?` and `hasPrimaryTargetStep?` removed from `PROGRESS_SYNCED` event type in `tool-page.machine.ts` — machine derives these internally via `deriveHasExtractionContext` and `deriveHasPrimaryTargetStep`. Admin dead code removed in Sprint 3: `listAdminModels`, `listAdminActivity`, associated types, `admin.models`/`admin.activity` from `ApiPaths` and `BackendCapabilities`. | Surface-area reduction aligned with PAT-002 (reachability matrix) and PAT-003 (thin ToolPage orchestration). All removals verified: typecheck clean, test baseline 208/211 (same 3 pre-existing fails). | Frontend |
| DDD-025 | 2026-05-04 | ToolKey (Generation context) | `ToolKey` is the canonical term in the Generation bounded context for the **step-orchestration identifier** carried in a `GenerationRequest` and stored in `GenerationSystemContext.toolKey`. It identifies which `toolWorkflowStepOrder` entry to use when resolving `WorkflowStep` dependency artifact IDs. Valid values are the step-capable subset of tools: `funnel-pages`, `nextland` (kebab-case). It is distinct from `ToolWorkflow` (artifact routing + type determination, snake_case values: `funnel_pages`, `nextland`, `meta_ads`, `extraction`) — both fields coexist in `GenerationSystemContext` and serve orthogonal concerns. The implementation type `SupportedToolWorkflow` in `apps/backend/src/lib/runtime/tool-workflow-registry.ts` is the closed-set enforcement for `ToolKey` values; it is a valid implementation detail, not a domain term. ACL translation at the Generation ↔ Frontend boundary: `ToolKey` (Generation) ← `SupportedTool` (Frontend). See also DDD-011 (`ToolKey` as Frontend registry type), DDD-C-001 (resolved: `ToolWorkflow` vs `SupportedTool`), DDD-C-005 (resolved-documented: naming-convention divergence). | `GenerationSystemContext` in `apps/backend/src/lib/types/xstate.ts` carries two fields that reference "the same tool" from orthogonal angles: `toolKey` (step orchestration) and `workflowType` (artifact routing). Neither was documented as a distinct domain concept, causing ambiguity at code-review and onboarding: reviewers conflate the two or treat `toolKey` as a redundant alias for `workflowType`. Additionally, `toolKey` values use kebab-case (`funnel-pages`) while `ToolWorkflow` values use snake_case (`funnel_pages`), a silent naming-convention divergence for the same logical tool. Registering `ToolKey` as canonical in Generation context closes the documentation gap, confirms the two fields are intentionally distinct, and grounds the divergence in DDD-C-005. | Generation |
| DDD-026 | 2026-05-04 | Tool | `Tool` is the canonical cross-context **Concept** for a system instrument that encapsulates a complete user-facing capability: structured input intake, an ordered `WorkflowStep` chain, progressive context enrichment across steps, and final `Artifact` production. Each Tool addresses one specific user problem; steps within a Tool may be reused across Tools. `Tool` is the organizing concept that gives the application its core purpose. It is expressed as `SupportedTool` (Frontend identifier), routed via `ToolWorkflow` (Generation routing), and step-ordered via `ToolKey` (Generation orchestration). The term `Tool` must not be used as a synonym for `SupportedTool`, `ToolWorkflow`, or `ToolKey` — those are context-local representations of the same underlying concept. Currently implemented Tools: `funnel-pages`, `nextland`. | The codebase uses `toolKey`, `SupportedTool`, and `ToolWorkflow` extensively but never defines what a Tool *is* at the domain level. Without a canonical Concept entry, documentation and onboarding conflate the identifier (ToolKey/SupportedTool) with the concept itself, and new contributors cannot understand the organizing principle of the application from the UL alone. | all contexts |
| DDD-027 | 2026-05-04 (revised 2026-05-24) | WorkflowStepType | `WorkflowStepType` is the canonical term for the execution strategy category of a `WorkflowStep`. Values: `extraction` (LLM-driven structured extraction, handled by `ExtractionChain`), `generation` (LLM-driven content production, handled by `StreamTransport` + `PersistenceBatch`), `acquisition` (external API data retrieval through persisted `ApiService` definitions). Status: **provisional** with implemented backend baseline — `WorkflowStepDescriptor` now supports optional `type` metadata in `xstate.ts`; acquisition runtime baseline is active through `acquisitionChainMachine`, context merge assembly, and orchestrate-path tests. Progressive rollout by tool configuration remains in scope before promotion to fully canonical. | The original decision established vocabulary before implementation. Backend rollout has now delivered the baseline execution path and persistence/catalog integration while preserving phased adoption controls. Keeping provisional status documents that governance promotion depends on complete production rollout across configured tools, not only baseline runtime support. | Generation |
| DDD-028 | 2026-05-04 | StepHydration (reclassification) | `StepHydration` is reclassified from "Domain Service" to **"Client-Side Projection"** in the UL. It projects backend-owned `WorkflowStep` state (persisted as `Artifact` history) into FE-local context for resume/regenerate flows. It does not own or compute domain logic: step ordering authority is `toolWorkflowStepOrder` (BE, `apps/backend/src/lib/runtime/tool-workflow-registry.ts`); dependency resolution authority is `resolveStepDependencyIds` (BE) and `/api/tools/orchestrate` (BE endpoint). FE functions `getStepDependencies` and `collectCompletedStepsByTool` are FE-side read projections, not domain logic. **Architecture note**: FE runtime now dispatches orchestration through `orchestrateToolStep` in `apps/frontend/src/features/tools/runtime/useToolPageRunController.ts`; residual cleanup scope remains documentation-only per DDD-C-007. | "Domain Service" implies ownership of domain logic. In the intended BE-first architecture, Frontend owns only interaction and display. Classifying a FE artifact reconstruction function as a Domain Service was a terminology residue from the prior architecture in which FE shared domain logic with BE. Reclassification aligns the UL with the target architecture and makes the code-level drift visible. | Frontend |
| DDD-019 | 2026-05-03 (revised 2026-05-04, updated 2026-05-18) | toolStepOrder | ~~`toolStepOrder` is the single canonical constant mapping each `SupportedTool` to its ordered `ToolStep[]`. Defined once in `apps/frontend/src/features/tools/machines/tool-flow.machine.ts`.~~ **Revised**: canonical step ordering is shared from contracts as `TOOL_STEP_ORDER` in `packages/contracts/src/tool-workflows.ts`. FE (`toolStepOrder`) and BE (`toolWorkflowStepOrder`) are authoritative projections over that shared source. This closes FE-vs-BE source ambiguity while preserving BE-first orchestration ownership for dependency resolution. | Updated to match current code reality where FE and BE both consume contracts-level ordering, removing residual single-layer source ambiguity from prior audit text. | Generation, Frontend |
| DDD-029 | 2026-05-04 | ToolKey (cross-context), ToolFormKey | Two decisions consolidated: (1) `ToolKey` is promoted to the **cross-context canonical identifier** for Tool identity across all bounded contexts. Values: `funnel-pages`, `nextland` (kebab-case). `SupportedTool` (FE) is now the Frontend-layer projection of `ToolKey`; `SupportedToolWorkflow` (BE type, `tool-workflow-registry.ts:10`) is a closed-set enforcement type — implementation detail per DDD-018 pattern, not a domain term. (2) The old FE form registry implementation type previously called `ToolKey` (DDD-011) is renamed **`ToolFormKey`** (`keyof typeof toolFormRegistry`). `ToolFormKey` is an implementation type only; docs must use `SupportedTool` or `ToolKey` for domain concepts. Supersedes DDD-011 (FE scope) and DDD-025 (Generation scope) for cross-context authority. | Three names for the same concept (`SupportedTool`, `SupportedToolWorkflow`, `ToolKey`) coexisted with no single canonical. Additionally, the FE form registry reused `ToolKey` as an implementation type, creating a collision with the Generation-context `ToolKey`. Consolidation resolves both issues: one canonical cross-context term for tool identity, one distinct name for the form registry impl type. | all contexts |
| DDD-030 | 2026-05-04 | meta_ads (deprecated ToolWorkflow value) | `meta_ads` was deprecated as an obsolete `ToolWorkflow` value with no Frontend counterpart. **Superseded by DDD-094** for controlled reactivation under a new canonical workflow identity (`meta_ads_generator`) and explicit migration governance. | Original deprecation removed an inactive dead value from canonical sets. Supersession keeps historical traceability while enabling deterministic re-onboarding through a new canonical identity. | Generation |
| DDD-032 | 2026-05-04 | GenerationRequestAssembly | **Provisional** — `GenerationRequestAssembly` is the canonical term for the Application-Layer process that translates accumulated FE session state — `HydrationResult`, `ExtractionContext`, and selected `ToolStep` — into a well-formed `GenerationRequest` command ready for BE dispatch. The process runs entirely in the Frontend runtime but produces a BE-owned domain command; scope is therefore Generation (output ownership). Implemented in `apps/frontend/src/features/tools/runtime/tools-client.ts:113-329` (~216 lines). The process is not a Domain Service (it contains no domain logic) and not a Value Object (it is a transformation, not a data structure); type classification: **Process** (Application Layer, boundary-crossing translation). Status: provisional — term introduced to establish canonical vocabulary before any potential refactor of the assembly logic into a dedicated module. | Three FE concepts (`HydrationResult`, `ExtractionContext`, `ToolStep`) compose into one BE command (`GenerationRequest`) through ~216 lines of code with no domain name. Without a canonical term, the assembly block is invisible in planning, ADRs, and code review. Naming it makes the boundary-crossing intent explicit and grounds the concept in the UL before architectural decisions about ownership are finalized. | Generation (output ownership) |
| DDD-031 | 2026-05-04 | ToolStepOrchestration | **Provisional** — `ToolStepOrchestration` is the canonical term for the backend operation that determines which prior `Artifact` IDs a given `ToolStep` depends on, given a `ToolKey` and the current step completion history. The operation is owned by `resolveStepDependencyIds` (BE, `apps/backend/src/lib/runtime/tool-workflow-registry.ts:27`), exposed via `/api/tools/orchestrate` (BE endpoint), and consumed by FE runtime via `orchestrateToolStep` from `apps/frontend/src/features/tools/runtime/useToolPageRunController.ts`. **Current implementation status**: implemented-runtime provisional; remain provisional until final convergence notes in DDD-C-007 are fully closed. | Vocabulary is now backed by runtime usage and endpoint availability while preserving provisional governance classification until all residual drift notes are retired. | Generation |
| DDD-033 | 2026-05-04 | ArtifactRole | `ArtifactRole` is the canonical Value Object classifying the role of an `Artifact` within a multi-step Tool execution chain. Values: `'step'` (intermediate artifact produced by a non-final `WorkflowStep`; its output feeds dependency resolution for subsequent steps) and `'final'` (artifact produced by the last `WorkflowStep` in the chain; represents the complete Tool output). Persisted in the artifact input JSON under `toolWorkflow.artifactRole` (see `ToolWorkflowPersistenceMetadata`, DDD-034 and DDD-050). Consumed by `StepHydration` for resume/regenerate classification and by Frontend display logic to determine CTA visibility. Status: **canonical** — value set is stable and persisted in DB. | The distinction between step and final artifacts is operationally fundamental: `StepHydration` must know whether a prior artifact is a dependency input or a terminal output. Without a canonical name the distinction is invisible in UL, ADRs, and code review. Gap surfaced by Generation context coverage audit 2026-05-04 (G-003). | Generation |
| DDD-034 | 2026-05-04 | ToolWorkflowPersistenceMetadata | `ToolWorkflowPersistenceMetadata` is the canonical Value Object embedded under the `toolWorkflow` key in an `Artifact`'s persisted input JSON when `routeType = 'tool'`. Base orchestration shape: `{ toolKey: ToolKey, workflowType: ToolWorkflow, runMode: WorkflowRunMode, artifactRole: ArtifactRole, stepKey: string, dependsOnSteps: string[], dependencyArtifactIds: string[], dependencyArtifactIdsByStep: Record<string, string> }`. Written at artifact creation time by `buildToolWorkflowPersistenceMetadata` in `generation-system.machine.ts`; read by FE `StepHydration` to reconstruct step context for resume/regenerate flows, and by BE `extractStepFromArtifactInput` (`tool-workflow-registry.ts`) to identify step key from artifact history. This JSON envelope remains the authoritative source for orchestration semantics. **Revision note**: queryable denormalized columns (`session_id`, `step_key`, `artifact_role`, `run_mode`) are governed by DDD-050 and do not replace the JSON authority defined here. Status: **canonical** — base orchestration contract remains stable; queryability revision is additive via DDD-050. | The `toolWorkflow` JSON blob in the artifact input is the foundational persistence contract for the multi-step Tool flow, but it had no UL name. Without a canonical term it cannot be referenced in integration constraints, ADRs, or the BCM translation table. Gap surfaced by Generation context coverage audit 2026-05-04 (G-004). | Generation |
| DDD-035 | 2026-05-04 | WorkflowStepUnlocked | **Canonical** — `WorkflowStepUnlocked` is the domain event emitted by `toolWorkflowMachine` when the dependencies of a `WorkflowStep` are satisfied and the step is ready to execute. Internal event: does not cross the process boundary (not on SSE wire). Triggers the transition from `'idle'` to `'running'` for the step's `WorkflowStepState`. Semantically significant for domain: represents the progression of a multi-step workflow. Emitted by `toolWorkflowMachine` within `GenerationSystem`; consumed by the same actor context. | Multi-step Tool execution relies on dependency-driven step progression. The unlock event is the boundary condition that makes the progression deterministic. Coverage audit G-005 surfaced this as a domain event missing from UL. | Generation |
| DDD-036 | 2026-05-04 | WorkflowStepCompleted | **Canonical** — `WorkflowStepCompleted` is the domain event emitted by `toolWorkflowMachine` when a `WorkflowStep` finishes execution (successfully or with failure) and its `WorkflowStepState` transitions to terminal status (`'done'`, `'error'`, `'skipped'`). Internal event: does not cross the process boundary. Contains the artifact ID produced by the step (if successful) and error metadata (if failed). The completion event serves the dependency graph: downstream steps may only unlock when their dependencies have completed. | Step completion state is fundamental to the multi-step execution contract. Without a canonical event name, internal dependency logic appears ad-hoc. Explicit naming makes the progression semantics visible in domain language. Coverage audit G-005 surfaced this as a domain event missing from UL. | Generation |
| DDD-037 | 2026-05-04 | WorkflowStepBootstrap | **Canonical** — `WorkflowStepBootstrap` is the Value Object that represents the initial state injected into a `ToolWorkflowMachine` when resuming or regenerating from a prior `Artifact`. Shape: `{ stepKey: string, output: string, artifactId: string }`. When `WorkflowRunMode = 'resume'` or `'regenerate'`, the `ToolWorkflowInput` receives a non-null `bootstrap` field containing the prior step state. This enables the machine to skip already-completed steps and continue from the resume point. Persisted in `ToolWorkflowPersistenceMetadata` as part of hydration. See DDD-034, DDD-050, DDD-031, DDD-020 (`ArtifactRelaunch`). | Resume/regenerate flows require a checkpointing mechanism: which step to start from, and what artifacts are already complete. Bootstrap encapsulates this contract explicitly. Without a canonical name, the resume logic is invisible in UL vocabulary. Coverage audit G-009 surfaced this as a gap. | Generation |
| DDD-038 | 2026-05-05 | HydrationResult completeness / ReadinessSnapshot | Artifact-driven relaunch readiness must be gated by **complete** recovered extraction context, not by the mere presence of a non-null `HydrationResult`. For `ArtifactType = extraction`, both local FE hydration and `/api/tools/hydrate` must recover the same semantic minimum: `extractionArtifactId`, `briefingId`, and non-empty normalized briefing text. `ReadinessSnapshot.hasExtractionContext` must remain `false` until that minimum is satisfied. **`extractionPayload` is not part of the readiness gate**: production evidence (2026-05-05) confirmed that historical extraction artifacts do not store a structured payload, yet their `normalizedText` alone is sufficient for `GenerationRequest` assembly. The FE `hasCompleteHydrationResult` predicate therefore requires only `extractionArtifactId + briefingId + normalizedText`; `extractionPayload` is passed through as-is (may be `{}`) and continues to be forwarded in `GenerationRequest.input.extractionPayload` (DDD-021 scope). **Local hydration fallthrough rule**: if the local in-memory extraction artifact has no recoverable text or payload (both empty), the FE must skip local resolution and fall through to `/api/tools/hydrate`. **BE Step 1 fallthrough rule**: if the directly-referenced extraction artifact has no recoverable text or payload (both empty), the BE must fall through to Step 2 (recency-ranked listing) to find the most complete artifact for the project. | Production smoke test on artifact `artifact-rqto8szw` (2026-05-05): local artifact found in 110-item cache but `normalizedTextLength: 0` and `extractionPayloadKeys: 0`. After local fallthrough, BE Step 2 returned `normalizedTextLength: 149679` and `extractionPayloadKeys: 0` — relaunch succeeded with text-only context. Original DDD-038 incorrectly included `extractionPayload` in the readiness gate, conflating the hydration completeness contract with the dispatch completeness contract (DDD-021). The two contracts are now separated: hydration gate = text-only; dispatch payload = text + optional structured payload. | Frontend, Generation |
| DDD-039 | 2026-05-05 (revised 2026-05-09, promoted 2026-05-18) | ToneProfile | **Canonical** — `ToneProfile` is the Value Object for user-selected tone in `WorkflowStepType = 'generation'` requests dispatched from the Tool Workspace Page. Allowed values are constrained to `Professional`, `Casual`, `Formal`, `Technical` through a select control. The extraction step is excluded from `ToneProfile`: extraction execution tone is fixed to `analitico` and is not user-configurable. `ExtractionContext.tone` remains a briefing-derived business field and must not be conflated with generation `ToneProfile` or extraction operational tone. | Promotion evidence confirms governance is implemented end-to-end: constrained Tone selector in Tool Workspace Page (`apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx`), generation dispatch forwarding selected `tone` via form state (`apps/frontend/src/features/tools/runtime/useToolPage.ts`), and fixed extraction tone `analitico` in extraction dispatch (`apps/frontend/src/features/tools/runtime/tools-client.ts`). The previous transitional wording is obsolete. | Frontend, Generation |
| DDD-040 | 2026-05-07 | Youtube LF Script tool identity | Canonical identity for the new Tool is fixed as follows: `ToolKey = youtube-lf-script` (kebab-case), `DisplayName = Youtube LF Script`, `ToolWorkflow = youtube_lf_script` (snake_case). Deprecated aliases: `youtube-long-form`, `youtube_long_form`. | Fixes naming drift across plan, runtime registry, URL routes, and workflow persistence metadata; preserves kebab/snake cross-layer convention (DDD-C-005). | all contexts |
| DDD-041 | 2026-05-07 | ToolStep (youtube-lf-script sequence) | Canonical `ToolStep` sequence for `youtube-lf-script` is prompt-chat-faithful and ordered as: `pre-script-analysis` → `packaging` → `intro-structure` → `body-structure` → `native-cta-embeds` → `outro-structure`. Final step is `outro-structure`. | Eliminates ambiguity between legacy 5-step chain and approved prompt-chat sequence; keeps deterministic orchestration and artifact role assignment. | Frontend, Generation |
| DDD-042 | 2026-05-07 | ExtractionContext (youtube-lf-script schema) | Canonical `ExtractionContext` fields for `youtube-lf-script`: `knowledge_content`, `avatar`, `pain_point`, `purchase_process_type`, `offer`, `proof`, `tone`, `target_duration_minutes`, `proprietary_methodology_disclosure`. Missing fields must be normalized to `null` (no inferred defaults). | Aligns extraction output with the approved prompt-chat input contract and prevents silent fallback semantics. | Frontend, Generation |
| DDD-043 | 2026-05-07 (revised 2026-05-21) | ReadinessSnapshot (youtube-lf-script baseline gate) | Under the map-driven readiness policy, the current baseline readiness key set for `youtube-lf-script` is: `knowledge_content`, `avatar`, `pain_point`, `offer`, `proof`. Other canonical fields may be `null` without blocking start. This baseline is represented in per-tool readiness maps and is extensible through governance without machine logic rewrites. | Preserves deterministic readiness and nullability compatibility (DDD-042) while removing fixed-count coupling from runtime logic. | Frontend |
| DDD-044 | 2026-05-07 | `/api/tools/briefs` toolKey contract | Endpoint contract accepts `toolKey` in both query and body; precedence rule is `body > query`; if missing in both, request is rejected with HTTP `400` and explicit validation error. | Maximizes backward compatibility while making source-of-truth precedence deterministic and testable. | Generation, Frontend |
| DDD-045 | 2026-05-07 | Regenerate stale policy | On regenerate of a non-terminal step, downstream artifacts are preserved, marked `stale`, readable for traceability, and not finalizable until recomputed in sequence. | Preserves audit continuity while preventing invalid finalization over stale dependency chains. | Frontend, Generation |
| DDD-046 | 2026-05-07 | Generation language and model default (youtube-lf-script) | Generation output language for `youtube-lf-script` is always Italian. Model selection remains user-controlled in Frontend; default input value is `openrouter/auto`. | Aligns output policy with business requirement while preserving existing frontend model-selection behavior. | Frontend, Generation |
| DDD-047 | 2026-05-07 | WorkflowSessionIdentifier | `WorkflowSessionIdentifier` is the canonical Value Object for cross-request correlation in multi-step Tool workflows. It is generated by Frontend when ToolPage initializes and propagated in each `GenerationRequest.sessionId`. Scope uniqueness: per `(userId, projectId, toolKey, session runtime window)`. | Replaces heuristic grouping by `(projectId + toolKey + time proximity)` with deterministic session identity. | Frontend, Generation |
| DDD-048 | 2026-05-07 | GenerationSession | `GenerationSession` is the canonical Aggregate Root (Generation context) that groups all `Artifact` entities produced in one multi-step Tool execution. Attributes include: `sessionId`, `toolKey`, `projectId`, `userId`, `runMode`, ordered step-state progression, and artifact membership. | Makes multi-artifact execution queryable and deterministic without changing `Artifact` entity identity (composition, not inheritance). | Generation |
| DDD-049 | 2026-05-07 | SessionArtifactGroup | `SessionArtifactGroup` is the canonical Frontend/UI read model Value Object for session display. It projects session state as ordered step artifacts with `stepKey`, `artifactRole`, status, and content, suitable for tabs/step navigation UX. | Separates backend orchestration aggregate (`GenerationSession`) from frontend display projection while preserving deterministic ordering. | Frontend |
| DDD-050 | 2026-05-07 | ToolWorkflowPersistenceMetadata (revision) | Revision of DDD-034 after session aggregation implementation. `ToolWorkflowPersistenceMetadata` keeps JSON as orchestration authority and adds denormalized query cache columns in `artifacts`: `session_id`, `step_key`, `artifact_role`, `run_mode`. Source-of-truth hierarchy: `input_json.toolWorkflow` for orchestration semantics; DB columns for indexed queries/display. This decision refines queryability and persistence-read concerns without superseding the base orchestration contract defined in DDD-034. | Maintains compact orchestration metadata while enabling deterministic low-latency session queries and role-based filtering. | Generation |
| DDD-051 | 2026-05-07 | SessionSummary / Sessions listing contract | `SessionSummary` is the canonical Frontend/UI listing read model for session aggregates. Canonical shape: `{ sessionId, projectId, toolKey, status, artifactCount, updatedAt }`. Canonical listing endpoint contract is `GET /api/tools/sessions` returning `SessionSummary[]`. Projects navigation rule: Project detail contextual history is served as `SessionSummary[]` filtered by `projectId`, not as non-aggregated artifact list. Current implementation status: backend runtime implements listing endpoint `/api/tools/sessions` and session detail endpoints (`/api/tools/sessions/{sessionId}`, `/api/tools/sessions/{sessionId}/step/{stepKey}`) via route-table dispatch + dedicated tools session handlers; FE consumption continues through session clients/queries. Governance rule: aggregate listing as primary access is a UX implementation policy only, not a domain invariant. Evidence: `apps/backend/src/lib/runtime/auth-http/route-table.ts`, `apps/backend/src/lib/runtime/auth-http/tools-session-handlers.ts`, `apps/frontend/src/features/tools/runtime/session-client.ts:37-44`, `apps/frontend/src/app/runtime/queries/useSessionsQuery.ts:52-56`. | Removes layer ambiguity between aggregate listing and aggregate detail contracts, now aligned with implemented backend listing runtime. | Frontend, Generation |
| DDD-052 | 2026-05-07 | Frontend route namespace separation (`artifacts` vs `sessionsummary`) | Canonical Frontend route ownership is explicitly separated: `artifacts` namespace is reserved for non-aggregated Artifact History and single-generation detail (`/artifacts`, `/artifacts/{artifactId}`); `sessionsummary` namespace is reserved for GenerationSession aggregate navigation (`/sessionsummary`, `/sessionsummary/{sessionId}`). Session listing and detail data contracts remain backed by `GET /api/tools/sessions` and `GET /api/tools/sessions/{sessionId}` (+ `/step/{stepKey}`), while artifact history/detail remain backed by `/api/artifacts` and `/api/artifacts/{artifactId}`. Transitional overload behavior where `/artifacts/{id}` attempts session-first resolution is deprecated and must be treated as backward-compat only (one deprecation cycle). Evidence: `apps/frontend/src/app/routing/app-router.tsx:101-118`, `apps/frontend/src/features/artifacts/pages/ArtifactsPage.tsx:1-9`, `apps/frontend/src/features/artifacts/pages/ArtifactDetailPage.tsx:45-113`, `apps/frontend/src/features/artifacts/ui/SessionsListingSection.tsx:149`, `apps/backend/src/lib/runtime/auth-http/route-table.ts`. | Current FE implementation uses SessionSummary fetch and session detail calls while still routing through `/artifacts`, creating lexical and navigation overlap in copy, route semantics, and responsibility boundaries. Declaring deterministic namespace ownership removes ambiguity and provides a stable contract for coordinated FE/BE rollout. | Frontend, Generation |
| DDD-053 | 2026-05-08 | LlmModel | `LlmModel` is the canonical Entity representing an administrator-managed LLM endpoint available for generation selection. Attributes: `id` (surrogate DB key), `key` (`LlmModelId`), `label` (display name), `status` (`LlmModelStatus`), `sortOrder` (optional). Persisted in the `llm_models` table. Admin CRUD operations are role-gated (`AuthUserRole = 'admin'`). `LlmModel` belongs to the **Generation** bounded context (generation configuration concern). Evidence of intent: `apps/frontend/src/features/admin/pages/AdminModelsPage.tsx` exists with hardcoded fallback and `backendEndpointPending` banner. | `GenerationRequest.model: string` is currently a free-form text input with no authoritative catalog, allowing invalid or deprecated model identifiers to be submitted. Introducing `LlmModel` as a managed entity establishes a single authoritative source, enabling consistent validation, model lifecycle management (enable/disable), and a structured UI selector. Supersedes the previously abandoned `adminModels` / `listAdminModels` dead code (DDD-024). | Generation |
| DDD-054 | 2026-05-08 | LlmModelStatus | `LlmModelStatus` is the canonical Value Object for the operational lifecycle state of an `LlmModel`. Values: `enabled` (visible and selectable by users), `disabled` (hidden from user selection; preserved for audit continuity in artifact records). | Mirrors the `ArtifactStatus` pattern (DDD-001 family): an explicit lifecycle state with a defined value set prevents free-form strings and makes enable/disable behavior deterministic. | Generation |
| DDD-055 | 2026-05-08 | LlmModelCatalog | `LlmModelCatalog` is the canonical Domain Service that provides the admin-managed, ordered collection of `LlmModel` entries available for generation. Authoritative read endpoints: `GET /api/models` (all authenticated users, returns `enabled` models only) and `GET /api/admin/models` (admin). Authoritative write endpoints: `POST /api/admin/models`, `PUT /api/admin/models/:id`, `DELETE /api/admin/models/:id` (all require `AuthUserRole = 'admin'`). Frontend consumes the catalog as option source for `LlmModelSelector`. Status: **canonical** — supersedes deprecated `listAdminModels` / `adminModels` dead code removed in DDD-024. | `AdminModelsPage.tsx` exists with hardcoded fallback and `backendEndpointPending` error, confirming the catalog was intended but never implemented. Naming the Domain Service now provides vocabulary for coordinated BE/FE rollout and closes the regression that occurred when the admin models dead code was removed in Sprint 3. | Generation, Frontend/UI |
| DDD-056 | 2026-05-08 | LlmModelId | `LlmModelId` is the canonical cross-context Value Object for the LLM model identifier. It corresponds to the `key` field of an `LlmModel` entity (e.g. `openrouter/auto`, `gpt-4.1-mini`). In `GenerationRequest.model`, `LlmModelId` replaces the raw `string` type (promotion from untyped field to constrained Value Object). Frontend must submit only `LlmModelId` values drawn from the catalog's `enabled` entries. The default fallback `openrouter/auto` (DDD-046) remains a valid `LlmModelId`. | Promotes `GenerationRequest.model` from an unconstrained `string` to a typed Value Object, enabling compile-time and runtime validation. Named separately from `LlmModel.key` to preserve the distinction between the managed entity (admin-owned configuration record) and the identifier used in generation commands and artifact records. | Generation, Frontend/UI |
| DDD-057 | 2026-05-08 | LlmModelSelector | `LlmModelSelector` is the canonical Frontend/UI term for the `<select>` control that presents the `enabled` entries of `LlmModelCatalog` as selectable options and emits the chosen `LlmModelId` to `ToolPage` form state. Replaces the previous free-text `<input>` for `model` in `ToolFormComponents.tsx` and `ToolPageTemplate.tsx`. Pre-populated by consuming `GET /api/models`. Default selected value: `openrouter/auto` (DDD-046 fallback applies when catalog is empty or endpoint is unavailable). | Establishes the frontend component concept before implementation, preventing naming drift between the form field (`formState.model`), the runtime query (`GET /api/models`), and the display component. Evidence: `ToolFormComponents.tsx:96` (free-text input to be replaced), `AdminModelsPage.tsx` (read-only admin catalog stub). | Frontend/UI |
| DDD-059 | 2026-05-18 (promoted 2026-05-18) | QuotaWindowPeriod | **Canonical** — `QuotaWindowPeriod` is the Value Object for the calendar-anchored time boundary of a User's quota period. It represents when `MonthlyUsed` was last reset to zero and is persisted as `quota_window_started_at`. The reset check runs inside `ClaimUsage` (adapter layer): if the stored period is in a prior calendar month, `MonthlyUsed` is reset and `quota_window_started_at` is updated atomically before decrement. Not user-facing. | Promotion evidence confirms implementation: migration applied for `quota_window_started_at` with default/not-null/index (`packages/infra-db/migrations/20260518_000010_quota_window_started_at.sql`) and runtime usage in `claimUsage` with monthly window reset semantics (`apps/backend/src/lib/adapters/postgres-redis.production.ts`). | Usage/Quota |
| DDD-060 | 2026-05-18 | UsageDecision (extension: resetDate) | Extension of the canonical `UsageDecision` Value Object to include an optional `resetDate?: Date` field. The field is set by `ClaimUsage` only when the `QuotaWindowPeriod` was reset during the current claim — it serves as an audit tracing signal only and does not affect readiness or guard outcomes. Consumers (including `usageMachine`) must treat `resetDate` as optional and non-blocking. Extension is additive: existing `{ granted: boolean, reason?: string }` shape is preserved. | Adding `resetDate` to `UsageDecision` without a governance entry risks future misinterpretation as a gate condition. Explicit registration blocks promotion of the field beyond its audit-only scope. | Usage/Quota |
| DDD-058 | 2026-05-08 | Tool display labels (`Hotlead Funnel`, `Sessions`) | Two Frontend/UI display labels are explicitly separated from their canonical domain identifiers. (1) The `funnel-pages` tool (`ToolKey = 'funnel-pages'`, `ToolWorkflow = 'funnel_pages'`) displays as **"Hotlead Funnel"** in navbar and any UI copy. The display label is a product/editorial name; it does not affect `ToolKey`, `SupportedTool`, route paths, or any backend contract. (2) The `sessionsummary` feature route (`/sessionsummary`) displays as **"Sessions"** in navbar and any UI copy. The display label intentionally surfaces the `GenerationSession` aggregate concept (DDD-048) to users without renaming the route, folder, or any code symbol. "Sessions" must not be treated as a synonym for `AuthSession` (Auth context) or `WorkflowSessionIdentifier` (Generation context) — it is exclusively a UI display label scoped to `appCopy.ui.navigation.sessionSummary`. Source: `apps/frontend/src/app/copy/system.ts`. | Display labels and domain identifiers must remain explicitly decoupled. Using the product name "Hotlead Funnel" in the UI while keeping `funnel-pages` as the technical key prevents pressure to rename canonical identifiers for marketing reasons. Separating "Sessions" (display) from `sessionsummary` (route/folder) maintains backward-compatible URL structure while improving UI clarity. | Frontend/UI |
| DDD-069 | 2026-05-11 | GenerationWorkspace | `GenerationWorkspace` is the canonical term for the React context maintained by `GenerationWorkspaceProvider` that holds per-session, cross-component generation state on a tool page. Key surface: `extractionByProject: Record<string, ExtractionContext>` (keyed by normalized `projectId`), `artifacts: GenerationArtifact[]`, `focusedProjectId`, `isStreamActive`, `streamStatus`, `terminalCompletedStep`, `terminalFailedStep`, and imperatives `getExtractionContext(projectId)`, `upsertExtractionContext(ctx)`, `start(request)`, `cancel()`. `upsertExtractionContext` dispatches `EXTRACTION_UPSERTED` to the underlying `frontendStreamMachine`, which writes to `extractionByProject`. `GenerationWorkspace` is the single shared state bus bridging `BriefingUpload` extraction, generation dispatch, and workspace display within `useToolPage`. Not a domain aggregate: it is a runtime coordination bus (Application Layer, Frontend). | The workspace provider exposes imperative accessors and stateful collections that are referenced in multiple contexts within `useToolPage` (bridge effect, extractionInfo builder, startGenerationStep, PROGRESS_SYNCED). Without a canonical term, documentation and code review cannot refer to this shared bus by name, leading to verbose circumlocutions such as "the value from GenerationWorkspaceProvider's getExtractionContext call". Naming it closes the documentation gap and allows `ExtractionContextBridge` (DDD-070) to be defined precisely. | Frontend/UI |
| DDD-071 | 2026-05-21 | GenerationConsolePage deprecation | `GenerationConsolePage` (route `/tools/console`) is deprecated and removed from the Frontend/UI bounded context. The page was the original free-form generation surface (manual prompt + `workflowType` selector) that predated the guided `Tool Workspace Page` pattern. It did not implement `ReadinessSnapshot`, `ExtractionContext`, or the step-dependency chain mandated by canonical `SupportedTool` flows. The `/tools/console` route now issues a client-side `Navigate` redirect to `/tools` (the canonical `ToolsHubPage`). The associated copy keys `navigation.toolsConsole` and `labels.toolsConsoleDescription` are removed from `appCopy`. Evidence: `apps/frontend/src/app/routing/app-router.tsx`, `apps/frontend/src/features/tools/pages/ToolsHubPage.tsx`. | Route was kept as a secondary link after the navbar refactor (2026-05-21, plan `refactor-frontend-tools-navbar-1.md`) but product decision confirmed that manual free-form generation is not an exposed user flow. Removing it eliminates an undocumented entry point that bypassed the canonical guided flow and reduces surface area for future DDD drift. | Frontend/UI |
| DDD-070 | 2026-05-11 | ExtractionContextBridge | `ExtractionContextBridge` is the canonical term for the React effect in `useToolPage` that syncs a ready `BriefingUpload` actor's extraction context into `GenerationWorkspace` via `upsertExtractionContext`. Lifecycle: fires when `briefingSnapshot.matches('ready')` is true and `briefingId`, `extractionArtifactId`, `normalizedText`, and `parsedFormat` are all non-empty. Guard condition: the effect is idempotent — before calling `upsertExtractionContext` it compares all six fields of the candidate context (`projectId`, `briefingId`, `extractionArtifactId`, `normalizedText`, `parsedFormat`, and JSON-serialized `extractionPayload`) against the current `workspaceExtractionContext`; if they are equal it returns early without dispatching. **Necessity**: `toolPageMachine` sets `ReadinessSnapshot.hasExtractionContext = true` as soon as `briefingActorRef.matches('ready')`; however, without the bridge, `workspaceExtractionContext` in `GenerationWorkspace` remains `null` or stale, so `startGenerationStep` reads `briefingTextLength: 0` and `extractionPayloadKeys: 0` at dispatch time. The bridge ensures the workspace context is populated before effect #7 fires. **Without-bridge failure mode**: `startGenerationStep` returns `false`, machine receives `CANCEL_GENERATION`, generation never starts despite readiness being true. Implementation: `apps/frontend/src/features/tools/runtime/useToolPage.ts` lines ~140–185. | Production regression (2026-05-11) confirmed that calling `upsertExtractionContext` inside the bridge on every render caused an infinite loop (bridge writes workspace → workspace change triggers re-render → bridge runs again). The idempotency guard was introduced to break the cycle. Naming the bridge as a canonical concept makes this guard requirement explicit in the UL and prevents future simplifications that would re-introduce the loop. | Frontend/UI |
| DDD-062 | 2026-05-12 | ToolAvailabilityStatus | `ToolAvailabilityStatus` is the canonical Value Object for Frontend tool exposure. Values: `enabled` (the tool is shown in navigation, dashboard shortcuts, and generated tool routes) and `disabled` (the tool remains implemented but is hidden from frontend exposure). The status is a Frontend/UI concern and does not change the cross-context identity of `ToolKey` or `SupportedTool`. | The frontend now applies a registry-level enable/disable decision to control whether a `SupportedTool` appears in the navigation, dashboard shortcuts, and route table. Evidence: `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts:90-117`, `apps/frontend/src/app/routing/app-router.tsx:1-40`, `apps/frontend/src/features/dashboard/pages/DashboardPage.tsx:14-31`, `apps/frontend/src/app/layouts/MainNavigation.tsx:1-58`. Separating availability from identity prevents future drift where hidden tools are mistaken for unsupported tools. | Frontend/UI |
| DDD-061 | 2026-05-11 | DispatchError | `DispatchError` is the canonical term for the local React state (`string \| null`) in `useToolPage` that holds a human-readable error message when `startGenerationStep` returns `false`. Set in effect #7 immediately before `toolPageSend({ type: 'CANCEL_GENERATION' })` with the message `'Impossibile avviare la generazione. Controlla la connessione e riprova.'`; cleared to `null` by `handlePrimaryAction` before each new dispatch attempt. Surfaces through the `useToolPage` return value and is rendered as a `<p>` adjacent to the primary CTA in `ToolPageTemplate`. **Scope boundary**: `DispatchError` is an Application-Layer UI feedback mechanism, not a domain error. It is distinct from: `ReadinessReasonCode` (pre-dispatch readiness gate), `generationError` (machine context for stream-level errors), and `BriefingUpload.error` (upload/extraction failure). | Effect #7 was originally fire-and-forget (dispatched `STEP_REQUEST_DISPATCHED` synchronously, awaited async, but had no recovery path on failure). The machine would get stuck in `generating` with no user feedback. Introducing `DispatchError` + `CANCEL_GENERATION` recovery closes the UX gap and names the feedback mechanism so it can be referenced deterministically in the tool page spec and in future error-handling extensions. | Frontend/UI |
| DDD-063 | 2026-05-16 | FeedbackChannel / PageStateMessage / GlobalFeedbackMessage | Canonical Frontend/UI feedback channel model is defined as three deterministic channels: `inline-action`, `page-state`, `global` (`FeedbackChannel`). `PageStateMessage` is canonical for query/list lifecycle rendering (`LoadingStateMessage`, `ErrorStateMessage`, `EmptyStateMessage`) in page body, especially Data Table View. `GlobalFeedbackMessage` is introduced as **provisional** for cross-page mutation outcomes and should not replace `DispatchError` or `PageStateMessage`; it complements them. Current implementation evidence shows fragmented local feedback (`feedbackMessage`, `mutationError`) and mixed rendering patterns. | The frontend already has stable local/page-state primitives but no canonical channel taxonomy, causing drift between inline errors, page-state blocks, and mutation confirmations. A deterministic channel model preserves context fidelity: action-scoped failures remain near actions, query/list states remain in-page, and cross-page mutation outcomes can be standardized without collapsing all feedback into one mechanism. Evidence: `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx:255-257`, `apps/frontend/src/features/tools/runtime/useToolPage.ts:663-773`, `apps/frontend/src/app/ui/primitives.tsx:103-113`, `apps/frontend/src/app/ui/ListingTableSection.tsx:51-57`, `apps/frontend/src/features/admin/pages/AdminUsersPage.tsx:98-99`, `apps/frontend/src/features/admin/pages/AdminUsersPage.tsx:332-333`, `apps/frontend/src/features/dashboard/pages/DashboardPage.tsx:119`. | Frontend/UI |
| DDD-064 | 2026-05-16 | extraction_context_insufficient | `extraction_context_insufficient` is the canonical failure reason code for extraction outcomes that are syntactically completed but semantically non-actionable for Tool readiness. Frontend `briefingUploadMachine` must transition `extracting -> idle` when this condition occurs, clear extraction context fields, and require a new upload attempt. Backend maps this reason to stable error envelope `{ code: validation_failed, message: 'Extraction context is insufficient for the selected tool' }`. Frontend runtime maps raw backend/internal reasons (`stream_empty_output`, `extraction_context_insufficient`) to user-readable inline `DispatchError` copy and must not expose raw transport/internal tokens to end users. | Introduced by extraction readiness gate refactor to prevent invalid extraction context from enabling generation start and to align error semantics across BE/FE without leaking machine tokens in UI. Evidence: `apps/frontend/src/features/tools/machines/briefing-upload.machine.ts`, `apps/frontend/src/features/tools/runtime/tools-client.ts`, `apps/frontend/src/features/tools/runtime/useToolPage.ts`, `apps/backend/src/lib/machines/generation-system.machine.ts`, `apps/backend/src/lib/runtime/error-contract.ts`. | Frontend/UI, Generation |
| DDD-065 | 2026-05-16 | ProductChangelog / UserReport / UserReportCategory / IssuePublicationPolicy | Canonical naming set for the implemented admin-to-user communication feature: `ProductChangelog` (admin-published user-facing changelog entry), `UserReport` (user-submitted report record), `UserReportCategory` (fixed values: `issue`, `feature-request`, `other`), `UserReportStatus` (canonical values: `submitted`, `triaged`, `github-published`, `closed`), `GitHubIssueLink` (optional external issue projection), and `IssuePublicationPolicy` (GitHub publication allowed for `UserReportCategory = issue` or `feature-request`). `FeedbackCenterMachine` is the canonical Frontend/UI XState application-service concept for orchestrating the feature flow boundary. | Existing UL had canonical `AuthUserRole` admin/member gate and canonical feedback channel governance, but no canonical vocabulary for changelog publication and user reporting flows. This decision introduced deterministic terminology and is now implementation-confirmed, preventing synonyms (`bug`, `problem`, `ticket`, `request`) from becoming parallel persisted concepts. Evidence anchors: admin role gate in `apps/frontend/src/features/admin/routing/admin-guard.tsx:7-9`; admin HTTP handlers in `apps/backend/src/lib/runtime/auth-http/admin-handlers.ts`; canonical feedback channel matrix in `apps/frontend/src/app/runtime/feedback-channel-map.ts:1-25`; UI governance archetypes in `docs/02-design/specifications/frontend-ui-ubiquitous-language-spec.md`. | Frontend/UI, Auth |
| DDD-066 | 2026-05-16 | ProductChangelogStatus | `ProductChangelogStatus` is the canonical Value Object for the lifecycle state of `ProductChangelog`. Fixed values: `draft`, `published`. Transition to `published` requires publication metadata (`publishedAt`, `publishedBy`) and is admin-gated through `AuthUserRole = 'admin'`. This status model is domain-level and independent from localization/UI copy concerns. | Changelog status appears as persistence-level constraints in the feature spec and is now represented as canonical UL terminology, preventing alternative synonyms such as `announcementStatus` or `releaseState` from spreading. Evidence anchors: `docs/02-design/specifications/admin-changelog-and-user-reporting-spec.md` (Section 7.1.2 status constraint), `apps/frontend/src/features/admin/routing/admin-guard.tsx:7-9`. | Frontend/UI, Auth |
| DDD-067 | 2026-05-17 | AdminDashboard | `AdminDashboard` is the canonical Concept for the unified administrative control plane accessible to `AuthUserRole = 'admin'` users. It is a contained section within the Frontend/UI bounded context that organizes all admin-facing functions (user CRUD, LLM model management, ApiService catalog and binding management, changelog publishing, user report triage, activity log review) under the single entry point route `/admin`. The dashboard is composed of atomic admin pages (`AdminUsersPage`, `AdminModelsPage`, `AdminApiServicesPage`, `AdminChangelogPage`, `AdminUserReportsPage`, `AdminActivityPage`), each following the `Data Table View` pattern. `AdminDashboard` is not a separable bounded context — it is a Frontend/UI feature group. Admin access is gate-checked by the `AdminGuard` component (`apps/frontend/src/features/admin/routing/admin-guard.tsx`). The `AdminDashboard` concept does not own any domain logic; it is a presentation and navigation concern purely. Evidence: `plan/refactor-admin-dashboard-frontend-1.md` (implementation plan, completed 2026-05-17), `apps/frontend/src/app/routing/app-router.tsx:101-118` (admin route config), `apps/frontend/src/features/admin/ui/AdminPageContainer.tsx` (shell pattern), `apps/frontend/src/features/admin/pages/AdminApiServicesPage.tsx`. | The admin section was implemented in Phase 2-3 of the refactor plan but had no canonical UL entry. Without a named concept, documentation and code review could not refer to the admin surface as a coherent whole, forcing circumlocutions such as "the admin pages" or "the admin-only routes". Introducing `AdminDashboard` as a canonical Concept makes the organizational intent explicit and allows future admin feature planning to reference a named scope. | Frontend/UI |
| DDD-068 | 2026-05-17 | DataTableView | `DataTableView` is the canonical UI pattern for Frontend/UI pages that display list-oriented domain entities (e.g., users, LLM models, ApiService catalogs, changelog entries, user reports) in tabular form. Canonical structure: query hook retrieves ordered entity list; toolbar component exposes filters/actions/controls; presentational table component (ListingTableSection) renders rows with atomic action components; mutation hooks and feedback channels handle CRUD outcomes. Pattern alignment rules: all `DataTableView` instances must use shared primitives (`Surface`, `TopBar`, `ListingTableSection` from `uiPrimitives`); row-level actions must use the dedupe-key feedback pattern (`FeedbackChannel = 'inline-action'` for immediate feedback); page-state errors must use `PageStateMessage` (`FeedbackChannel = 'page-state'`); cross-page mutation confirmations route through `GlobalFeedbackMessage` (`FeedbackChannel = 'global'`). Frontend admin pages (`AdminUsersPage`, `AdminModelsPage`, `AdminApiServicesPage`, `AdminChangelogPage`, `AdminUserReportsPage`) all conform to this pattern. Non-admin examples: `SessionsListingSection` in artifact history, future project management table. The pattern is named to encourage consistency and prevent divergent table/list implementations. Evidence: `apps/frontend/src/features/admin/pages/AdminUsersPage.tsx` (orchestrator), `apps/frontend/src/features/admin/pages/AdminApiServicesPage.tsx` (orchestrator), `apps/frontend/src/features/admin/ui/AdminPageContainer.tsx` (shell), `apps/frontend/src/app/ui/ListingTableSection.tsx` (presentational primitive), `docs/02-design/specifications/frontend-ui-ubiquitous-language-spec.md` (page archetype references). | Admin refactor (plan 2026-05-17) systematized five admin pages using an identical structure: query → toolbar → table → mutations → feedback. Without a named pattern, future feature additions would risk diverging from the established rhythm or duplicating the structure without naming the design decision. Establishing `DataTableView` as a canonical pattern name makes the rhythm explicit, quotable in design discussions, and enables guidance for new tables without copying/pasting component scaffolding across the codebase. | Frontend/UI |

| DDD-100 | 2026-05-19 | ToolKeyNormalization — canonical function ownership | `normalizeToolWorkflowKey` exported from `apps/backend/src/lib/runtime/workflow-normalizers.ts` is the **single canonical BE implementation** for normalizing external/user-supplied tool key strings to canonical `ToolKey` format (DDD-029). No handler module may define a local variant. Implementation status after auth-http refactor: (1) local duplicate `normalizeSupportedToolKey` has been removed, (2) tools brief upload normalizes via `normalizeToolWorkflowKey`, and (3) stale dead import has been removed from runtime dispatch orchestration. | The Architecture Weaknesses Review (2026-05-18, Medium severity, Priority 5) flagged normalization duplication. The refactor executed the convergence path and removed duplicate implementations, preventing new divergence during module split and route-table extraction. Evidence: `apps/backend/src/lib/runtime/auth-http/tools-brief-handlers.ts`, `apps/backend/src/lib/runtime/workflow-normalizers.ts`, `apps/backend/src/lib/runtime/auth-http/runtime.ts`. | Generation |
| DDD-072 | 2026-05-19 | FeedbackCenter | `FeedbackCenter` is the canonical Backend/API boundary term for the `auth-http` delivery slice that groups `ProductChangelog` and `UserReport` policy plus public/admin HTTP handlers. It covers category/status normalization, GitHub issue publication eligibility, public report creation and published changelog reads, and admin changelog/report triage endpoints. `FeedbackCenter` is **not** a new bounded context and must not replace the entity terms `ProductChangelog` and `UserReport` in contracts or documentation. `FeedbackCenterMachine` remains the distinct Frontend/UI application-service term. | The codebase already expresses a stable backend grouping through `apps/backend/src/lib/runtime/feedback-center-policy.ts:1-60`, the public HTTP surface in `apps/backend/src/lib/runtime/auth-http/public-handlers.ts:1-153`, and the admin handler surface in `apps/backend/src/lib/runtime/auth-http/admin-feedback-center-handlers.ts`. The UL previously had only the FE term `FeedbackCenterMachine` (DDD-065), leaving backend module naming under-specified. Registering `FeedbackCenter` closes that gap and prevents parallel synonyms such as `reporting`, `changelog`, or `communication-center` from becoming competing BE module names. | Auth |
| DDD-073 | 2026-05-19 | GenerationRequest / RegistryBacked* extensibility boundary | The open-union shapes in `apps/backend/src/lib/types/xstate.ts` (`RegistryBackedArtifactType`, `RegistryBackedWorkflowType`, `RegistryBackedToolKey`) and the broad payload envelope in `packages/contracts/src/index.ts` (`GenerationRequestInput` with index signature) are explicitly classified as an infrastructure-layer compatibility boundary. They are not canonical domain value sets and must not be reused as authoritative domain typing in documentation. Governance guardrails are mandatory: (1) tool/workflow identifiers entering orchestration must pass canonical contract guards (`isToolKey`, `resolveToolWorkflowType`), (2) request assembly must normalize optional/legacy fields through `request-contract.ts`, and (3) compile-time parity guard remains active in contracts. This decision records an intentional trade-off (backward compatibility + registry extensibility) and closes architecture finding tracking for this topic as accepted risk under controls. | The architecture review kept a Medium finding open for type-safety erosion due to open unions and broad request payload shape. Runtime evidence confirms active containment points already exist: canonical key/workflow guards in `packages/contracts/src/tool-workflows.ts`, request normalization and controlled projection in `apps/backend/src/lib/runtime/request-contract.ts`, and the existing DDD-018 declassification baseline for registry-backed open unions. Registering this decision removes ambiguity between accidental drift and intentional boundary design while preserving future hardening options. | Generation, Frontend/UI |
| DDD-074 | 2026-05-20 | packages/domain activation — cross-context domain primitives | `@gen-app-2/domain` (`packages/domain/src/index.ts`) is the **single authoritative source** for the following cross-context canonical Value Objects: `ArtifactType` (DDD-001), `ArtifactStatus` (DDD-017), `OutputFormat` (DDD-022 scope), `WorkflowRunMode` (DDD-037), `ArtifactRole` (DDD-033). Each is exported with its companion const array for runtime type-guard use. Consumers: `packages/contracts` imports and re-exports `ArtifactType`, `OutputFormat`, `WorkflowRunMode` from this package (backward-compat, DDD-023 chain preserved); `apps/backend/src/lib/types/artifact.ts` imports and re-exports all five; `apps/backend/src/lib/types/xstate.ts` imports `WorkflowRunMode` directly. The `packages/domain` package has no runtime dependencies and must remain framework-agnostic. **Activation rule from README still applies**: do not promote types here for convenience alone — only stable, truly cross-context domain assets belong in this package. | `packages/domain` was a placeholder with no source files since project inception. `ArtifactType`, `OutputFormat`, and `WorkflowRunMode` were independently defined as inline type unions in both `packages/contracts/src/index.ts` and `apps/backend/src/lib/types/artifact.ts`; `WorkflowRunMode` was additionally duplicated in `apps/backend/src/lib/types/xstate.ts`. `ArtifactRole` (DDD-033) existed only as an inline `'step' \| 'final'` union in three backend adapter files — no canonical named type. Activating `packages/domain` closes the Medium architecture finding, eliminates three duplication points, and promotes `ArtifactRole` to its first canonical named type. Typecheck validation: all four workspaces (`@gen-app-2/domain`, `@gen-app-2/contracts`, `@gen-app-2/backend`, `gen-app-2-frontend`) pass `tsc --noEmit` with 0 errors. | all contexts |
| DDD-075 | 2026-05-20 | Hydration Briefing Coherence Requirement | `/api/tools/hydrate` endpoint must enforce **briefing semantic coherence** when ranking candidate extraction artifacts for fallback hydration. When `resolvedBriefingId` is provided in the HTTP request, candidate filtering applies **before** `sourceExtractionArtifactId` exact-match ranking and recency ranking. **Filter rule**: Include only candidates where `artifact.input.briefingId === resolvedBriefingId` OR (legacy fallback) `!artifact.input.briefingId && artifact.artifactId === resolvedBriefingId`. When no candidates match after filtering, return HTTP 404 with explicit error code `no_extraction_for_briefing`. When `resolvedBriefingId` is `null`, fall through to existing sourceExtractionArtifactId + recency ranking for backward compatibility. This decision enforces the domain contract (DDD-007, DDD-038) that `ExtractionContext` and `Briefing` are semantically paired concepts and prevents silent semantic incoherence when reusing historical extraction artifacts across sessions with different briefing contexts. **Legacy compatibility note**: Production artifacts without explicit `input.briefingId` use the artifact ID as fallback (line 103, tools-hydrate-handlers.ts). The filter logic must preserve this fallback to remain backward-compatible with historical artifact catalogs. | Confirmed CRITICAL architecture finding (architecture-weaknesses-code-review.md): `resolvedBriefingId` is computed (line 95) but ignored in ranking (lines 170-178). Multi-briefing sessions may relaunch with wrong extraction context (semantic incoherence). Evidence: `apps/backend/src/lib/runtime/auth-http/tools-hydrate-handlers.ts` (lines 95, 100-130, 170-178, 203); FE/BE sync verified correct (hydration.machine.ts:130-150); test infrastructure mature (runtime.auth-http.test.ts:1-80); legacy fallback pattern documented (DDD-038). Pre-implementation audit confirmed fix is feasible with 8-9h effort and identified legacy artifact filtering as hidden edge case (+30m). | Generation, Frontend |
| DDD-076 | 2026-05-21 | RequestTone (`ToneProfile | 'analitico'`) | `RequestTone` is the canonical transport Value Object for `GenerationRequest.input.tone`. It explicitly models two valid request-layer tone families: user-selected `ToneProfile` values for generation steps and fixed operational extraction tone `analitico`. This union is transport-only: it does not replace or broaden `ExtractionContext.tone` (briefing-derived business field). In the same contract hardening step, `LlmModelId` format is fixed to provider/model (`${string}/${string}`) as canonical request boundary shape. | Remediation of architecture finding on weak request contract typing required a deterministic tone boundary that keeps extraction operational behavior explicit while preserving strict generation tone governance. The same remediation tightened model identity shape and required frontend normalization of legacy free-form values before request dispatch. Evidence: `packages/contracts/src/index.ts` (`RequestTone`, `LlmModelId`), `apps/frontend/src/features/tools/runtime/tool-page-runtime-utils.ts` (normalization), `apps/frontend/src/features/tools/runtime/tools-client.ts` (extraction fixed `analitico`). | Generation, Frontend/UI |
| DDD-077 | 2026-05-21 | Angle Generator canonical identity and minimal workflow | Canonicalize the new Tool derived from approved prompt source: `ToolKey = angle-generator` (kebab-case), `DisplayLabel = Angle Generator`, `ToolWorkflow = angle_generator` (snake_case). Canonical minimal `ToolStep` sequence (3-step, user-approved granularity): `context-and-angle-matrix` -> `angle-prioritization` -> `creative-activation`. Step semantics: (1) context mapping + angle matrix generation, (2) top-angle evaluation/prioritization, (3) operational headline/copy activation. This decision closes bootstrap status and authorizes propagation in DDD references. | Source evidence: user-provided "AI Angle Detector & Marketing Strategist" system prompt in current session (2026-05-21), with explicit phase model (`FASE 1`..`FASE 4`) and mandated deliverables; user confirmation via discovery questions set canonicalization to immediate and step granularity to 3 (compacted). DDD-first policy satisfied by registering identity/workflow/steps before implementation. | Generation, Frontend/UI |
| DDD-078 | 2026-05-21 | Angle Generator dual-source extraction input (`BriefingFile` + `AngleDetectorFile`) | Canonicalize the new extraction-input rule for `ToolKey = angle-generator`: extraction must use two user-provided files, `BriefingFile` (existing) and `AngleDetectorFile` (new market-analysis input). The extraction phase executes as one LLM job over merged context assembled from both files; dual independent extraction jobs are not allowed. This decision extends extraction input semantics without changing the command boundary term `GenerationRequest` or replacing `ExtractionContext` as output concept. | User session requirement (2026-05-21) explicitly introduced the double-source extraction rule and identified the second file purpose (market-angle analysis). Existing runtime evidence confirms current baseline is single-file ingestion (`apps/frontend/src/features/tools/machines/briefing-upload.machine.ts:15`, `apps/frontend/src/features/tools/runtime/tools-client.ts:52-59`, `apps/backend/src/lib/runtime/auth-http/tools-brief-handlers.ts:152-157`) and single extraction command envelope (`packages/contracts/src/index.ts:104-116`). Registering this decision first prevents undocumented drift during implementation. | Generation, Frontend/UI |
| DDD-079 | 2026-05-21 | ExtractionFieldKey / ExtractionFieldLabel normalization | Canonicalize extraction field naming across tools with a deterministic two-layer rule. Layer 1 (contract): extraction payload identifiers must be `ExtractionFieldKey` in English snake_case. Layer 2 (presentation): UI copy may render localized `ExtractionFieldLabel` values derived from keys. Mixed guidance lists that combine localized labels and raw keys in the same list are non-canonical and must be normalized through key->label projection before render. Canonical runtime key catalog for this decision: `avatar`, `brand_or_company`, `creative_constraints`, `funnel_goal`, `goal`, `knowledge_content`, `market`, `offer`, `offer_or_service`, `pain_point`, `primary_cta`, `product_or_service`, `proof`, `proprietary_methodology_disclosure`, `purchase_process_type`, `required_sections`, `target_audience`, `target_duration_minutes`, `tone`, `website_goal`. | Runtime convergence evidence now uses one shared contract + FE projection: `packages/contracts/src/extraction-fields.ts` (canonical key catalog + per-tool maps + alias table), `apps/frontend/src/features/tools/runtime/extraction-field-matrix.ts` (label projection map), `apps/frontend/src/features/tools/runtime/tool-page-selectors.ts` (deterministic key->label selector projection), `apps/backend/src/lib/machines/generation/extraction-parsers.ts` (tool-aware normalization in parser output path). | Generation, Frontend/UI |
| DDD-080 | 2026-05-21 | InstructionRequiredExtractionFieldKeysByTool / ReadinessRequiredExtractionFieldKeysByTool | Canonicalize map-driven extraction-field governance with two distinct per-tool maps: `InstructionRequiredExtractionFieldKeysByTool` (UI guidance scope) and `ReadinessRequiredExtractionFieldKeysByTool` (generation gating scope). The two maps must not be semantically coupled. Runtime convergence is unified across all supported tools (`funnel-pages`, `nextland`, `youtube-lf-script`, `angle-generator`, `meta-ads`) through one shared canonical extraction-field matrix contract consumed by both Frontend and Backend normalization/projection paths. Baseline policy in current runtime: instruction map is explicit per tool; readiness map remains explicit and map-driven with `youtube-lf-script` required keys (`knowledge_content`, `avatar`, `pain_point`, `offer`, `proof`), and context-not-empty gating for `meta-ads` and the other supported tools via empty required-key sets (actionable-payload gate preserved). | The implementation plan convergence gate requires scalable readiness evolution (no fixed field-count hardcoding) and explicit anti-drift controls between FE and BE runtime maps. Shared contract ownership in `packages/contracts/src/extraction-fields.ts` prevents FE/BE map divergence while preserving independent semantics for guidance vs readiness. | Generation, Frontend/UI |
| DDD-081 | 2026-05-22 | ToolInputFileRequirementPolicy | Canonicalize the input-file requirement rule for Frontend Tool Workspace setup. Rule set: (1) if a tool defines exactly one input file, that file is always required; (2) if a tool defines two or more input files, only the first file (`BriefingFile`) is always required; (3) every file from index 2 onward must carry explicit per-tool requiredness classification (`required-by-tool-setting` or `optional-by-tool-setting`). This policy governs file-requiredness semantics only and does not replace extraction-field policies (`InstructionRequiredExtractionFieldKeysByTool`, `ReadinessRequiredExtractionFieldKeysByTool`). | Existing runtime evidence shows registry-driven required file lists and dual-source upload gating for `angle-generator`: `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts` (`ToolFileInstructionsConfig.requiredFiles`, per-tool registry entries), `apps/frontend/src/features/tools/runtime/tool-page-selectors.test.ts` (dual-source requiredFiles assertion), `apps/frontend/src/features/tools/machines/briefing-upload.machine.ts` (tool-aware file guard). User requirement (session 2026-05-22) requires deterministic generalization to all multi-file tools with explicit per-tool setting from second file onward. | Frontend/UI, Generation |
| DDD-082 | 2026-05-22 | InputFilePayloadStatus | `InputFilePayloadStatus` is the canonical Frontend/UI Value Object representing the ordered collection of per-file load statuses for all tool input files that constitute the current `GenerationRequest` payload. Each entry carries: `key` (registry file identifier), `label` (UI display name), `requiredness` (`always-required \| required-by-tool-setting \| optional-by-tool-setting` per `ToolInputFileRequirementPolicy`, DDD-081), `status` (`todo \| active \| done \| error`), and `fileName` (`string \| null`). The collection is ordered by registry position, with `BriefingFile` always at index 0. Rendered as the **`InputFilePayloadStatus` section** inside the `Workflow Panel` (`ToolGenerationFlowVertical`); always visible across all flow phases (input, monitoring, completion). Congealed at generation start: during monitoring and completion the section renders as a frozen confirmation anchor, reassuring the user which files were included in the dispatched `GenerationRequest`. Replaces the phase-exclusive "Requisiti" checklist that was previously hidden during monitoring. Component prop name: `inputFilePayload: InputFilePayloadStatus[]`. Runtime derivation sources: `deriveToolInputFileCompletion` + `effectiveBriefingFileName` + `angleDetectorFileName` from `useToolPage`. UI copy for the section heading: "Payload caricato" (it-IT). | UX proposal 2026-05-22 (`docs/99-lifecycle/99-archive/superseded/ux/tool-page-sidebar-unified-flow.md`) identified that the existing "Requisiti" checklist disappears when monitoring starts, losing the user's confirmation of which files were in the payload during the long-running generation wait (avg 6–8 min). A persistent, phase-invariant display of input file statuses provides cognitive anchoring and reduces the user's impulse to reload the page. Registering the canonical term before implementation prevents drift between the prop name, section label, and domain concept across `ToolGenerationFlowVertical`, `ToolPageTemplate`, and specs. | Frontend/UI |
| DDD-083 | 2026-05-23 (archived note captured 2026-05-27) | WorkflowPanelFeedbackItem | `WorkflowPanelFeedbackItem` is the canonical Frontend/UI Value Object for a single aggregated feedback message rendered in the `WorkflowPanelFeedback` section of `ToolGenerationFlowVertical`. Each item carries: `id` (unique string key for React reconciliation), `severity` (`'error' \| 'info'`), `message` (display string), and optional `source` (origin tag for debugging). Visible in all canonical states. Aggregated in `ToolPageTemplate` from 7 priority-ordered sources: (1) `briefingError` → error, (2) missing required files → error, (3) `readinessReasonCodes` (`missing_project` → error, `missing_extraction_context` → error, `missing_primary_target_step` → info), (4) `artifactsReloadError` → error, (5) `briefingGuidance` → info, (6) optional-file advisory (when required complete) → info, (7) `canStartExtraction` hint → info. Passed as `workflowPanelFeedback: WorkflowPanelFeedbackItem[]` prop. Relates to `FeedbackChannel` `inline-action` channel (DDD-063). Does not carry `DispatchError` (DDD-061 ownership boundary). **Rendering mandate superseded by DDD-084 (2026-05-23): the `WorkflowPanelFeedbackItem[]` prop and its 7-source aggregation are removed from `ToolGenerationFlowVertical`. Use `errorMessage: string \| null` instead.** | Archival/timeline normalization: this row documents a legacy aggregation model and remains superseded by DDD-084 for execution-ready monitor contracts. The 2026-05-27 note reflects when archival wording was captured, not a later effective-date override of DDD-084. | Frontend/UI |
| DDD-084 | 2026-05-23 (revised 2026-05-23) | WorkflowPanel unified phase-monitor model | Canonical Workflow Panel composition for `ToolGenerationFlowVertical` is a **two-card monitor**: `Progress` card + `Informazioni di contesto` card. The `Progress` card uses one reusable preload-bar element across both phases (`Estrazione`, `Generazione`) with deterministic stop/play behavior by `CanonicalToolUiState`: `draft-empty` and `resume-needs-briefing` -> extraction idle stop (visible), `processing-briefing` -> extraction active play, `draft-ready` -> extraction completed stop, `running` -> generation active play, `paused-with-checkpoint` -> generation paused stop, `completed` -> generation completed stop. Progress metrics are phase-selective: extraction shows extraction-specific informational metrics; generation shows step-focused metrics (`Step corrente`, `N/N step completati`). The `Informazioni di contesto` card includes project block + payload file rows and keeps files gated by project selection. `DispatchError` (DDD-061) remains Setup Panel ownership and is not duplicated in Workflow Panel body. | Revision registered after implementation convergence of the monitoring dashboard behavior: the previous "single-bar minimal surface" wording no longer matched shipped UX and test expectations. Evidence anchors: `apps/frontend/src/features/tools/ui/ToolGenerationFlowVertical.tsx`, `apps/frontend/src/features/tools/ui/ToolGenerationFlowVertical.test.tsx`, `apps/frontend/src/styles.css`, `plan/refactor-tool-generation-flow-monitoring-dashboard-1.md`, `docs/99-lifecycle/99-archive/superseded/ux/tool-generation-flow-monitoring-dashboard-proposal.md`. | Frontend/UI |
| DDD-085 | 2026-05-23 | Tool Workspace status naming convergence (`ToolStepStatus.done` vs `CanonicalToolUiState.completed`) | Canonical naming convergence rule: `ToolStepStatus` keeps terminal value `done` (DDD-013), while `CanonicalToolUiState` keeps terminal value `completed`. UI projections must preserve this split explicitly: step badges/step status mappings may use `done`, but Workflow Panel preload-bar naming must use `completed` for both variant token and CSS class (`is-completed`). `done` is forbidden in preload-bar variant/class naming (`BarVariant`, `.workflow-preload-bar.*`) to prevent cross-layer drift. Enforcement gate: add a static guard test that validates component + CSS + test alignment and rejects `is-done` regressions in the preload bar surface. | Cross-check 2026-05-23 found the historical drift vector still possible at naming boundaries even after DDD-084 simplification: machine term `done` (step-level) and UI-state term `completed` (panel-level) coexist by design, but class/token renames can accidentally collapse one into the other. A dedicated convergence rule plus static guard test keeps machine status, UI class naming, and test expectations synchronized over time. Evidence anchors: `apps/frontend/src/features/tools/machines/tool-flow.machine.ts`, `apps/frontend/src/features/tools/ui/ToolGenerationFlowVertical.tsx`, `apps/frontend/src/styles.css`, `apps/frontend/src/features/tools/ui/ToolGenerationFlowVertical.test.tsx`, `apps/frontend/src/features/tools/ui/ToolGenerationFlowVertical.status-naming.guard.test.ts`. | Frontend/UI |
| DDD-086 | 2026-05-24 | ToolInputSource | `ToolInputSource` is the canonical cross-context concept for one authoritative origin of Tool input context. Current baseline families are: typed form fields, `ToolInputFile` uploads (`BriefingFile`, `AngleDetectorFile`), and API-backed retrieval from persisted `ApiService` definitions. This decision extends the current hybrid input model without replacing file-specific terms (`BriefingFile`, `AngleDetectorFile`) or `ExtractionContext` as the downstream structured payload concept. API-backed retrieval is expected to enter the workflow through `WorkflowStepType = 'acquisition'` or equivalent request assembly before downstream steps consume it. | Existing UL already documents hybrid file + form input semantics (`docs/02-design/domain-bounded-context-map.md:115-116`, `docs/02-design/specifications/tool-page-frontend-runtime-spec.md:268-279`) and a provisional `WorkflowStepType = acquisition` for external API retrieval (`docs/07-governance/domain-naming-decision-log.md:48`). The current request requires formalizing API fetch as a first-class input family before implementation so future tool specs do not introduce parallel synonyms such as `connector`, `integration`, or `data source`. | Generation, Frontend/UI |
| DDD-087 | 2026-05-24 | ApiService / ApiServiceAccessMode / ApiServiceCatalog | Canonical naming set for persisted API-backed tool inputs: `ApiService` is the administrator-managed persisted Entity representing one external API source usable by Tool acquisition flows; `ApiServiceAccessMode` is the Value Object that classifies outbound access as `public` or `token`; `ApiServiceCatalog` is the Generation-owned Domain Service exposing the authoritative set of persisted `ApiService` definitions. Governance rule: create/update/delete operations on `ApiServiceCatalog` require `AuthUserRole = 'admin'`; execution-time tool flows consume resolved service definitions read-only. `ApiServiceAccessMode = token` keeps token-bearing calls on the backend boundary and must not be promoted to Frontend-owned runtime configuration. | Implementation baseline is now in place with deterministic backend surfaces: persistence migration (`packages/infra-db/migrations/20260524_000011_api_service_catalog.sql`), catalog adapter (`apps/backend/src/lib/adapters/api-service.adapter.ts`), admin CRUD handlers/routes (`apps/backend/src/lib/runtime/auth-http/admin-api-service-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/auth-http-admin-routes.ts`), tool runtime resolver endpoint (`apps/backend/src/lib/runtime/auth-http/tools-api-service-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/auth-http-tools-routes.ts`), plus validation/redaction policy (`apps/backend/src/lib/runtime/integrations/api-service-validation.ts`) and outbound acquisition adapter scaffold (`apps/backend/src/lib/runtime/integrations/api-acquisition.adapter.ts`). | Generation, Frontend/UI, Auth |
| DDD-088 | 2026-05-24 | PrimaryActionPolicy `open-last-artifact` execution invariant | For `PrimaryActionPolicy = open-last-artifact`, the primary CTA action must execute independently from RHF/Zod form validation and must immediately navigate to `/sessionsummary/{sessionId}` through `handlePrimaryAction`. Validation-gated submit wrappers are allowed only for generation/extraction dispatch policies (`start-generation`, `resume-checkpoint`, `regenerate-current-step`) where request payload assembly depends on validated form state. This prevents no-op clicks in completed state when unrelated form fields are invalid. | Regression observed in Tool Workspace completion handoff: CTA label `Apri sessione` was visible but click was blocked because `ToolGenerationFlowVertical.primaryActionCta.onClick` was always wrapped in `handleSubmit(...)` inside `ToolPageTemplate`, causing RHF validation to intercept `open-last-artifact` branch. Runtime fix introduced policy-aware handler split and dedicated regression test. Evidence: `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx` (`handleUnifiedPrimaryActionClick`), `apps/frontend/src/features/tools/runtime/useToolPage.ts` (`handlePrimaryAction` navigate branch), `apps/frontend/src/features/tools/ui/ToolPageTemplate.open-session-cta.test.tsx` (guard test). | Frontend/UI |
| DDD-089 | 2026-05-24 | ContextGenerationPhase | `ContextGenerationPhase` is the canonical Frontend/UI umbrella term for the pre-step runtime phase that assembles the Tool input context before step-1 generation dispatch. It supersedes using `Extraction` as the page-phase label when the flow includes non-document sources. Scope includes one or more of: text extraction, API-backed acquisition, and direct input merge. `Extraction` remains a valid execution term for document-derived processing and may appear as one sub-activity inside `ContextGenerationPhase`, but must not name the whole phase when multiple input sources are involved. | Product evolution introduces mixed-source input tools (direct fields + file uploads + API fetch). Reusing `Extraction` as phase label for all cases creates semantic drift and user confusion because extraction is only one specific action. Registering `ContextGenerationPhase` creates deterministic FE naming while preserving backend/runtime extraction semantics. | Frontend/UI, Generation |
| DDD-090 | 2026-05-24 | ToolInputRequirementMatrix | `ToolInputRequirementMatrix` is the canonical policy for requiredness gating across all Tool input source families. The matrix classifies each input entry by `sourceFamily` (`direct-input`, `tool-input-file`, `api-acquisition`) and `requiredness` (`always-required`, `required-by-tool-setting`, `optional-by-tool-setting`). Start eligibility for context generation is deterministic: all entries marked `always-required` or `required-by-tool-setting` must be satisfied before the primary start action is enabled; `optional-by-tool-setting` entries must never block start. This policy extends `ToolInputFileRequirementPolicy` without replacing it. | Existing policy governance is detailed for files but under-specified for direct input and API-backed acquisition. A single requiredness matrix prevents per-tool ad-hoc gating logic and aligns FE readiness behavior with cross-source payload assembly. | Frontend/UI, Generation |
| DDD-091 | 2026-05-24 | StartContextGenerationAction (`Genera contesto` CTA contract) | `StartContextGenerationAction` is the canonical primary CTA contract for initiating `ContextGenerationPhase`. UI copy may remain `Genera contesto` for backward familiarity during transition, but runtime semantics are umbrella: one action starts the configured context assembly pipeline for the current tool (extraction, API fetch, and direct-input merge according to tool settings). The workflow panel progress shown during this phase must represent context-generation progress, not extraction-only semantics. On success, FE must pass the composed context payload to step-1 generation dispatch without introducing a second user-triggered pre-step action. | The current CTA label and progress model are extraction-centric, while planned tools require mixed-source payload composition. Registering a canonical action contract preserves deterministic UX flow and avoids dual-trigger behavior (separate extraction and API-fetch buttons) that would fragment readiness and progress semantics. | Frontend/UI |
| DDD-092 | 2026-05-24 | tokenHeaderName (`ApiService` request-profile field) | `tokenHeaderName` is the canonical `ApiService` request-profile field used when `ApiServiceAccessMode = token` and the outbound token must be injected into a non-default header key. Default runtime behavior remains `Authorization: Bearer <token>` when `tokenHeaderName` is null/empty. If `tokenHeaderName` is provided and valid, runtime injects token exactly once in that header key with deterministic precedence over same-name entries from `requestHeadersTemplateJson`; other template headers are preserved. Validation scope: strict HTTP header-name format, normalization by trim, no secret value exposure in FE payloads. | Implementation completed end-to-end with additive backward-compatible rollout: persistence migration (`packages/infra-db/migrations/20260524_000013_api_service_token_header_name.sql`), shared contract extension (`packages/contracts/src/api-service.ts`), backend persistence/runtime/handler propagation (`apps/backend/src/lib/types/api-service.ts`, `apps/backend/src/lib/adapters/api-service.adapter.ts`, `apps/backend/src/lib/runtime/integrations/api-service-validation.ts`, `apps/backend/src/lib/runtime/integrations/api-acquisition.adapter.ts`, `apps/backend/src/lib/runtime/auth-http/admin-api-service-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/tools-api-service-handlers.ts`), frontend admin create/edit support (`apps/frontend/src/features/admin/runtime/admin-client.ts`, `apps/frontend/src/features/admin/runtime/admin-api-service-form.ts`, `apps/frontend/src/features/admin/pages/AdminApiServicesPage.tsx`, `apps/frontend/src/features/admin/ui/AdminApiServiceCreateForm.tsx`, `apps/frontend/src/app/copy/system.ts`), and deterministic automated coverage (`apps/backend/src/lib/tests/runtime.api-acquisition.adapter.test.ts`, `apps/backend/src/lib/tests/runtime.api-service-validation.test.ts`, `apps/backend/src/lib/tests/runtime.api-service-auth-http.test.ts`, `apps/frontend/src/features/admin/pages/AdminApiServicesPage.test.tsx`, `apps/frontend/src/features/admin/pages/AdminRoutesA11ySmoke.test.tsx`, `apps/backend/src/lib/tests/generation-system.runtime.test.ts`). | Generation, Frontend/UI, Auth |
| DDD-093 | 2026-05-24 | ToolAvailabilityStatus tri-state role policy | `ToolAvailabilityStatus` is extended from binary to tri-state with canonical values: `enabled-for-all`, `disabled-for-all`, `enabled-for-admin-only`. `enabled-for-admin-only` means the Tool is visible and executable only for `AuthUserRole = admin`; `member` users must not see the Tool in Tool discovery surfaces and must receive deterministic `403 forbidden` on backend tool endpoints if they attempt direct calls. Canonical policy source is shared in `packages/contracts/src/tool-workflows.ts` and consumed by both FE and BE to avoid drift. | Binary enable/disable policy could not express admin-only tool rollout without ad-hoc flags. A tri-state canonical value object plus shared-contract policy source gives deterministic FE discovery behavior and backend enforcement consistency. | Frontend/UI, Generation, Auth |
| DDD-094 | 2026-05-25 | Meta Ads tool reactivation (supersedes DDD-030) | **Supersedes DDD-030.** Canonical reactivation identity for the new Tool is fixed as: `ToolKey = meta-ads` (kebab-case), `DisplayLabel = MetaAds Generator`, `ToolWorkflow = meta_ads_generator` (snake_case). `meta_ads` remains a deprecated alias and must not be reintroduced as primary `ToolWorkflow`. Cross-context rule: FE/URL/nav identity uses `meta-ads`; Generation routing/storage identity uses `meta_ads_generator`. | Reactivation is explicitly allowed only through a new canonical workflow identifier to avoid resurrecting deprecated legacy semantics. This preserves DDD traceability (DDD-030 history), keeps kebab/snake translation deterministic (DDD-C-005), and prevents ambiguity with previous obsolete value sets. | Generation, Frontend/UI |
| DDD-095 | 2026-05-26 | YouTube Description tool identity and direct-input policy | Canonical identity is implemented as: `ToolKey = youtube-description` (kebab-case), `DisplayLabel = YT Description Generator`, `ToolWorkflow = youtube_description` (snake_case). Canonical minimal flow is one generation step: `youtube-description-generation`. Input policy is direct-input-only (`ToolInputRequirementMatrix` with source family `direct-input` only), with no file upload gating for start eligibility. Current runtime guard baseline is presence-only on required direct-input fields plus markdown-only output contract; `socialLinks` and `hashtags` are optional-by-tool-setting and do not block stream start when omitted. Strict semantic checks (URL/timestamp format and hashtag-count cap) are intentionally deferred during initial tool testing and are not active in as-is guard enforcement. | Runtime implementation converged in FE/BE paths after DDD registration and contract rollout, with deterministic tests/build passing on 2026-05-26. Evidence: `packages/contracts/src/tool-workflows.ts`, `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts`, `apps/frontend/src/features/tools/runtime/tool-page-selectors.ts`, `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx`, `apps/backend/src/lib/runtime/generation-entry-guards.ts`, `apps/backend/src/lib/tests/runtime.generation-entry-guards.test.ts`. | Generation, Frontend/UI |
| DDD-096 | 2026-05-27 | ArtifactCoherenceDiagnostic / ArtifactCoherenceScore / ArtifactCoherenceMismatch / CTAProgressionTarget / CoherenceSeverityLevel | Register provisional cross-context coherence-diagnostic vocabulary and ownership boundaries. See Decision Evidence Appendix: DDD-096. | Deterministic three-dimension coherence model aligned with existing map-driven readiness governance. | Generation, Frontend/UI |
| DDD-097 | 2026-05-27 | ReadinessQualityGate / ReadinessQualityThresholdByTool | Register provisional quality-gated readiness vocabulary with explicit per-tool map policy requirement. See Decision Evidence Appendix: DDD-097. | Extends existing map-driven readiness governance without replacing context-presence semantics. | Generation, Frontend/UI |
| DDD-098 | 2026-05-27 | ArtifactOutcomeStatus / ArtifactOutcomeRecord / ArtifactComparisonSet / ArtifactLearningFeedbackLoop | Register provisional domain-wide Artifact outcome-learning vocabulary and FE/BE ownership split. See Decision Evidence Appendix: DDD-098. | Generalizes outcome-learning from tool-specific scope to all Artifact-producing workflows. | Generation, Frontend/UI |
| DDD-099 | 2026-05-27 | CoherenceDiagnosticReasonCode / ReadinessQualityReasonCode / CoherenceReasonCodePolicyByTool / ReadinessQualityReasonCodePolicyByTool (contract-first gate) | Register provisional reason-code and policy-map governance as mandatory pre-runtime contract. See Decision Evidence Appendix: DDD-099. | Prevents FE/BE drift by defining typed code sets and map policies before runtime branching. | Generation, Frontend/UI |

## Decision Evidence Appendix (Agent-Optimized)

### DDD-096
- Decision detail: `ArtifactCoherenceDiagnostic` is the deterministic pre-generation assessment envelope over `messageLevel`, `proofLevel`, and `ctaLevel`.
- Decision detail: `ArtifactCoherenceScore` is a derived `0..100` value and never user-entered.
- Decision detail: `ArtifactCoherenceMismatch` represents one typed misalignment finding; `CTAProgressionTarget` models expected CTA progression from message baseline; `CoherenceSeverityLevel` classifies mismatch gravity in machine-readable form.
- Ownership: Generation defines scoring semantics and severity rules; Frontend/UI computes and renders readiness-facing projections.
- Evidence rationale: deterministic three-dimension coherence model aligns with current map-driven readiness governance (`DDD-080`).

### DDD-097
- Decision detail: `ReadinessQualityGate` evaluates generation start eligibility from diagnostic quality outcomes, not only payload presence.
- Decision detail: `ReadinessQualityThresholdByTool` is an explicit per-tool policy map for score/severity acceptance.
- Governance constraint: threshold policy must be map-driven; fixed-count hardcoding and copy-text-dependent branching are forbidden.
- Ownership: Generation is authoritative for gate semantics; Frontend/UI projects gate status and reason codes in `ReadinessSnapshot`.
- Evidence rationale: extends existing readiness policy model (`ReadinessRequiredExtractionFieldKeysByTool`, DDD-080) with quality gating.

### DDD-098
- Decision detail: `ArtifactOutcomeStatus` canonical values are `not-tested`, `worked`, `failed`, `inconclusive`.
- Decision detail: `ArtifactOutcomeRecord` persists one outcome annotation over an Artifact with normalized tags and audit timestamps.
- Decision detail: `ArtifactComparisonSet` provides bounded comparison over two or three outcomes; `ArtifactLearningFeedbackLoop` feeds outcome evidence into readiness/advisory and regenerate strategy selection.
- Scope: domain-wide across all Artifact-producing workflows, including but not limited to `meta-ads`.
- Ownership: Generation owns persistence semantics and orchestration advisory consumption; Frontend/UI owns entry, visualization, and comparison projections.

### DDD-099
- Decision detail: `CoherenceDiagnosticReasonCode` and `ReadinessQualityReasonCode` are canonical typed code sets for diagnostic findings and quality-gate outcomes.
- Decision detail: `CoherenceReasonCodePolicyByTool` and `ReadinessQualityReasonCodePolicyByTool` are canonical per-tool map-driven policy sources.
- Contract-first rule: code sets and policy maps must be defined and approved in contracts/docs before FE/BE runtime control-flow branches are introduced or modified.
- Runtime constraint: runtime logic must consume existing map policies and reason-code sets; ad-hoc literals first/documentation later is forbidden.
- Evidence rationale: extends map-driven governance pattern already established by DDD-080, DDD-090, and DDD-097.

## Naming Conflicts Register

| Conflict ID | Candidate Terms | Impacted Areas | Proposed Resolution | Status |
| --- | --- | --- | --- | --- |
| DDD-C-001 | `ToolWorkflow` (backend `apps/backend/src/lib/types/artifact.ts`) vs `SupportedTool` (frontend `tool-flow.machine.ts`) | Generation ↔ Frontend/UI boundary | Both terms are kept as context-local: `ToolWorkflow` in Generation, `SupportedTool` in Frontend. The bounded context map documents the translation rule. No unification needed — they have different value sets and concerns. | resolved |
| DDD-C-002 | `WorkflowStep` (backend abstract) vs `ToolStep` (frontend concrete) | Generation ↔ Frontend/UI boundary | Kept distinct per DDD-003 and DDD-004. Backend `WorkflowStep` is an abstract descriptor; frontend `ToolStep` is a concrete named value. | resolved |
| DDD-C-003 | `extractionPayload` (field name in code) vs `ExtractionContext` (DDD canonical term) | Frontend codebase | `ExtractionContext` is the canonical DDD term (DDD-007). Field name `extractionPayload` is acceptable as an implementation detail in the code. Docs corrected to use `ExtractionContext` where referring to the domain concept (2026-05-03). | resolved |
| DDD-C-004 | `relaunchPrimary` / `relaunchSecondary` vs `ArtifactRelaunch` | Frontend/UI artifact detail and tool entry hydration | Resolved by converging to one domain concept (`ArtifactRelaunch`) and one hydrated-ready path with one effective generation-start action. Governance baseline now also fixes default runtime intent for artifact-driven relaunch entries to `regenerate` under DDD-020. | resolved |
| DDD-C-005 | `toolKey` values (kebab-case: `funnel-pages`) vs `ToolWorkflow` values (snake_case: `funnel_pages`) | Generation context internal (`GenerationSystemContext`, `GenerationRequest`) | Resolved with explicit cross-context translation rule. `ToolKey`/`SupportedTool` remain kebab-case identity values at FE/BE boundary; `ToolWorkflow` remains snake_case for Generation routing and DB compatibility. No unification is required because they represent orthogonal concerns and maintain separate canonical value sets. Authoritative mapping is centralized in `packages/contracts/src/tool-workflows.ts` (`toolKey` + `workflowType`) and consumed by backend request normalization in `apps/backend/src/lib/runtime/request-contract.ts` (`resolveToolWorkflowType`). | resolved-documented |
| DDD-C-006 | `extraction` in `ToolWorkflow` vs `extraction` in `WorkflowStepType` | Generation context | Two orthogonal concepts share the same token `extraction`. `ToolWorkflow = 'extraction'` is a direct routing path that bypasses `toolWorkflowMachine` entirely (`generation-system.machine.ts:532-554`); it is NOT a Tool in the DDD-026 sense. `WorkflowStepType = 'extraction'` is a step execution strategy handled by `ExtractionChain`. No renaming required — both usages are established in code and stable. Resolution: both glossary entries carry an explicit cross-reference note clarifying the distinction (see `ToolWorkflow` and `WorkflowStepType` entries in the glossary). | resolved-documented |
| DDD-C-007 | `getStepDependencies` (FE) vs `resolveStepDependencyIds` (BE) / `orchestrateToolStep` | Generation ↔ Frontend/UI | Runtime alignment is now documented: FE dispatch path invokes `orchestrateToolStep` in `apps/frontend/src/features/tools/runtime/useToolPageRunController.ts`, and BE serves `/api/tools/orchestrate` through route-table dispatch + dedicated orchestrate handlers (`apps/backend/src/lib/runtime/auth-http/route-table.ts`, `apps/backend/src/lib/runtime/auth-http/tools-orchestrate-handlers.ts`). `resolveStepDependencyIds` remains backend authority. Residual cleanup scope remains limited to retiring stale historical wording and keeping projection notes synchronized with DDD-031. | resolved-documented |
| DDD-C-008 | `feedbackMessage` (page-local success copy) vs global feedback naming | Frontend/UI | Canonical term for cross-page mutation outcomes is `GlobalFeedbackMessage` (provisional, DDD-063). Existing page-local state names such as `feedbackMessage` are accepted as implementation details during transition, but docs/specs must use canonical terms (`FeedbackChannel`, `PageStateMessage`, `GlobalFeedbackMessage`) and must not infer that success confirmations are `LoadingStateMessage`. | resolved-documented |
| DDD-C-009 | `bug` / `problem` / `issue` / `ticket` / `request` | Frontend/UI, Auth | Canonical persistence and API category term is `UserReportCategory` with fixed values `issue`, `feature-request`, `other` (DDD-065). `bug`, `problem`, `ticket`, and `request` are accepted only as UI-facing copy aliases that must be normalized to canonical values at input boundaries; they must not be stored as primary category values. | resolved-documented |
| DDD-C-010 | localized extraction labels (`Obiettivo del funnel`, `Prodotto o servizio`) vs raw keys (`knowledge_content`, `proprietary_methodology_disclosure`) | Frontend/UI, Generation | Canonical contract is split into `ExtractionFieldKey` (payload, English snake_case) and `ExtractionFieldLabel` (localized UI projection). Required-field guidance must render one layer at a time through deterministic key->label projection and must not mix both forms in the same list. See DDD-079. | resolved-documented |
| DDD-C-011 | `ApiService` vs `connector` vs `integration` vs `data source` | Generation, Frontend/UI | Use `ApiService` only for the persisted, admin-managed external API definition. Use `ToolInputSource` for the broader category that also includes typed form fields and `ToolInputFile` uploads. Avoid promoting `connector`, `integration`, or `data source` as parallel primary terms in docs/specs. | resolved-documented |
| DDD-C-012 | `Extraction` (page-phase naming) vs `ContextGenerationPhase` | Frontend/UI, Generation | Use `ContextGenerationPhase` as the canonical umbrella name for the pre-step FE phase when the flow may include extraction, API-backed acquisition, and direct-input merge. Keep `Extraction` only for the specific document-processing sub-activity or backend execution semantics. Transitional UX copy and historical docs may still mention extraction, but specs/governance must not use it as the full mixed-source phase name. See DDD-089. | resolved-documented |
| DDD-C-013 | `Genera contesto` (visible CTA copy) vs `StartContextGenerationAction` (runtime contract) | Frontend/UI | Keep `StartContextGenerationAction` as the canonical action contract and `Genera contesto` as transitional visible UI copy. Do not infer extraction-only runtime behavior from the label: the action starts the configured umbrella context pipeline (extraction, API fetch, direct-input merge) and must remain single-trigger. See DDD-091. | resolved-documented |
| DDD-C-014 | `ArtifactCoherenceDiagnostic` / `ReadinessQualityGate` / `ArtifactOutcomeRecord` / `CoherenceDiagnosticReasonCode` / `ReadinessQualityReasonCode` vs synonyms (`diagnostic scorecard`, `quality check`, `evaluation result`, `outcome entry`, `learning record`, `diagnostic reason`, `quality reason`, `reason map`) | Generation, Frontend/UI | Use canonical terms registered in DDD-096/DDD-097/DDD-098/DDD-099 only: `ArtifactCoherenceDiagnostic`, `ArtifactCoherenceScore`, `ArtifactCoherenceMismatch`, `CTAProgressionTarget`, `CoherenceSeverityLevel`, `ReadinessQualityGate`, `ReadinessQualityThresholdByTool`, `ArtifactOutcomeStatus`, `ArtifactOutcomeRecord`, `ArtifactComparisonSet`, `ArtifactLearningFeedbackLoop`, `CoherenceDiagnosticReasonCode`, `ReadinessQualityReasonCode`, `CoherenceReasonCodePolicyByTool`, `ReadinessQualityReasonCodePolicyByTool`. Avoid introducing parallel synonyms in docs/specs/notes; if a new nuance is required, register a new DDD decision first. | resolved-documented |
