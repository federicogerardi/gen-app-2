---
status: active
version: 2.16
last-reviewed: 2026-07-18
next-review-date: 2026-10-18
owner: Domain Architecture
---

# Domain Ubiquitous Language Glossary

## Scope
- Canonical domain vocabulary for the current workspace (`gen-app-2`).
- Source of truth for domain terms used across analysis, development, testing, operations, and user documentation.
- Bounded contexts covered: **Generation**, **Auth**, **Usage/Quota**, **Frontend/UI**, **Crawling & Extraction**, **Competitor Analysis**.

---

## Cross-Context Concepts

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| PG_POOL_MAX | Value Object | Environment variable controlling maximum PostgreSQL connection pool size. Default 20. Hotfix to prevent pool saturation during concurrent generation streams. | `.env.example`, `apps/backend/src/lib/adapters/postgres-redis.production.ts` | canonical |
| LlmModelId | Value Object | Cross-context canonical identifier for the LLM endpoint used in a generation. Corresponds to the `key` field of an `LlmModel` entity and must follow provider/model form (`${string}/${string}`), for example `openrouter/auto` and `openrouter/gpt-4.1-mini`. Carried as `GenerationRequest.model`; selected by Frontend from the enabled entries of `LlmModelCatalog`. Values must be validated against `LlmModelCatalog` enabled entries at dispatch time. The default fallback value is `openrouter/auto` (DDD-046). Supersedes the raw `string` type formerly used for `GenerationRequest.model`. See DDD-056. | `packages/contracts/src/index.ts:76` (`GenerationRequest.model`), `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx:85` | canonical |
| RequestTone | Value Object | Cross-context request-level tone type carried in `GenerationRequest.input.tone`. Union of user-facing `ToneProfile` values for generation steps and fixed extraction operational tone `analitico`. This value object is transport-level and does not replace `ExtractionContext.tone` (briefing-derived business context). See DDD-076. | `packages/contracts/src/index.ts:67` (`GenerationRequestInput.tone`) | canonical |
| Tool | Concept | A system instrument that encapsulates a complete user-facing capability: it accepts structured user input (for example typed form fields, `ToolInputFile` uploads such as `BriefingFile`, or API-backed input resolved from an `ApiService`), executes an ordered chain of `WorkflowStep`s that progressively enrich context at each pass, and produces one or more `Artifact`s as final output. Each Tool addresses one specific user problem. Steps within a Tool may be reused across different Tools. The system's long-term purpose is to compose a suite of Tools that collectively support team members' core work. Canonical active Tool identities: `funnel-pages`, `nextland`, `youtube-lf-script`, `angle-generator`, `youtube-description`, `geometric`, `blog-article-generator` (DDD-155), `brief-generator` (DDD-210). Canonically approved reactivation identity: `meta-ads` with `ToolWorkflow = meta_ads_generator` (DDD-094). Tool identity is carried by the cross-context canonical term `ToolKey` (DDD-029); its layer projections are `SupportedTool` (Frontend) and the `ToolKey` field in `GenerationRequest` (Generation). Not every `ToolWorkflow` value is a Tool: `extraction` is a direct routing path (no step chain). Deprecated workflow alias governance is documented under DDD-030 and DDD-094. YouTube Description identity/workflow policy is governed by DDD-095. | `apps/backend/src/lib/runtime/tool-workflow-registry.ts:14-18`, `docs/02-design/domain-bounded-context-map.md:115-116`, `docs/02-design/specifications/tool-page-frontend-runtime-spec.md:268-279`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ToolKey | Value Object | Cross-context canonical identifier for a Tool. Carries the identity of which Tool is in use across all bounded contexts. Canonical value set includes active identities `funnel-pages`, `nextland`, `youtube-lf-script`, `angle-generator`, `youtube-description`, `geometric`, `blog-article-generator` (DDD-155), `brief-generator` (DDD-210) and the approved reactivation identity `meta-ads` (DDD-094), all in kebab-case. Expressed as: `SupportedTool` in the Frontend layer (FE-local type alias, interaction scope); `ToolKey` field in `GenerationRequest` and `GenerationSystemContext` in the Generation layer (step orchestration + request routing); `SupportedToolWorkflow` BE type in `tool-workflow-registry.ts` (implementation enforcement, not a domain term — per DDD-018 pattern). The FE form registry implementation type (previously also called `ToolKey`) is renamed `ToolFormKey` (DDD-029). See DDD-025, DDD-029, DDD-C-005, DDD-040, DDD-077, DDD-094, DDD-095, DDD-117, DDD-155, DDD-210. | `apps/backend/src/lib/types/xstate.ts:54`, `apps/frontend/src/features/tools/machines/tool-flow.machine.ts:3`, `apps/backend/src/lib/runtime/tool-workflow-registry.ts:10`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ToneProfile | Value Object | User-selected tone parameter for ToolStep content artifact generation requests (for example `optin`, `quiz`, `vsl`, `landing`, `thank_you`). Scope is limited to `WorkflowStepType = 'generation'` steps rendered from the Tool Workspace Page Setup Panel. Governed by a dedicated ToolPage `<select>` with constrained values `Professional`, `Casual`, `Formal`, `Technical`; selected value is forwarded in `GenerationRequest.input.tone` for generation and regenerate dispatches. This Value Object does **not** govern extraction execution tone: `WorkflowStepType = 'extraction'` runs with fixed operational tone `analitico` for job consistency. It is also distinct from `ExtractionContext.tone`, which is briefing-derived business context. See DDD-039. | `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx:22`, `apps/frontend/src/features/tools/runtime/useToolPage.ts:440`, `apps/frontend/src/features/tools/runtime/tools-client.ts:202` | canonical |
| ToolInputSource | Concept | Cross-context concept for one authoritative origin of Tool input context. Current baseline families are enumerated by `ToolInputSourceFamily` (canonical, DDD-106): `direct-input` (user-typed form fields), `tool-input-file` (`BriefingFile`, `AngleDetectorFile`), `api-acquisition` (retrieval from persisted `ApiService` definitions). `ToolInputSource` extends the current hybrid input model without replacing file-specific concepts or `ExtractionContext`; API-derived input enters the Tool flow through `WorkflowStepType = 'acquisition'` and is assembled before downstream steps consume it. The broader cross-context concept remains provisional pending full BE-owned input assembly authority; the concrete `ToolInputSourceFamily` enumeration is canonical (DDD-106). See DDD-086. | `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts:64-67` (`ToolInputSourceFamily`), `apps/frontend/src/features/tools/runtime/tool-page-selectors.ts:515`, `docs/02-design/domain-bounded-context-map.md:115-116`, `docs/07-governance/domain-naming-decision-log.md` | provisional |
| ContextGenerationPhase | Process | **Provisional** Frontend/UI umbrella process for assembling the effective Tool input context before step-1 generation dispatch. It may include text extraction, API-backed acquisition, and direct input merge depending on tool configuration. `Extraction` remains a valid sub-activity when document processing is present, but does not name the full phase for mixed-source tools. See DDD-089. | `docs/02-design/specifications/tool-generation-flow-source-of-truth-spec.md`, `docs/02-design/specifications/frontend-ui-ubiquitous-language-spec.md`, `docs/07-governance/domain-naming-decision-log.md` | provisional |
| ToolInputRequirementMatrix | Policy | Requiredness policy matrix spanning all Tool input source families (`direct-input`, `tool-input-file`, `api-acquisition`). Start eligibility for context generation requires all entries marked `always-required` or `required-by-tool-setting`; entries marked `optional-by-tool-setting` are non-blocking. Extends `ToolInputFileRequirementPolicy` (DDD-081) without replacing file-specific rules. Fully implemented in `deriveToolInputRequirementMatrix` (`tool-page-selectors.ts:537`) with test coverage. See DDD-105. | `apps/frontend/src/features/tools/runtime/tool-page-selectors.ts:512-537` (`ToolInputRequirementMatrixEntry`, `ToolInputRequirementMatrix`, `deriveToolInputRequirementMatrix`), `apps/frontend/src/features/tools/runtime/tool-page-selectors.test.ts` | canonical |
| ArtifactCoherenceDiagnostic | Value Object | **Provisional** cross-context diagnostic envelope computed before generation start to evaluate alignment across `messageLevel`, `proofLevel`, and `ctaLevel`. Includes score and mismatch findings used by readiness quality gating. Generation owns scoring semantics; Frontend/UI owns projection in setup/readiness surfaces. See DDD-096. | `docs/07-governance/domain-naming-decision-log.md` (DDD-096), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ArtifactCoherenceScore | Value Object | **Provisional** deterministic coherence score (`0..100`) derived from `ArtifactCoherenceDiagnostic`. Never user-entered, never copy-derived at runtime, and always recomputable from diagnostic inputs and policy rules. See DDD-096. | `docs/07-governance/domain-naming-decision-log.md` (DDD-096), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ArtifactCoherenceMismatch | Value Object | **Provisional** typed mismatch record describing one coherence defect across message/proof/CTA dimensions and its severity classification. Used by gate evaluation and advisory rendering. See DDD-096. | `docs/07-governance/domain-naming-decision-log.md` (DDD-096), `docs/02-design/domain-bounded-context-map.md` | provisional |
| CTAProgressionTarget | Value Object | **Provisional** expected CTA progression target derived from message positioning. Used to detect CTA overshoot/undershoot conditions during coherence diagnostics. See DDD-096. | `docs/07-governance/domain-naming-decision-log.md` (DDD-096), `docs/02-design/domain-bounded-context-map.md` | provisional |
| CoherenceSeverityLevel | Value Object | **Provisional** machine-readable severity classification for coherence mismatches. Must be policy-driven and independent from localized UI wording. See DDD-096. | `docs/07-governance/domain-naming-decision-log.md` (DDD-096) | provisional |
| ReadinessQualityGate | Policy | **Provisional** cross-context readiness gate that evaluates generation start eligibility using diagnostic quality outcomes (score + severity), not only context presence. Generation owns evaluation semantics; Frontend/UI projects gate status and reason codes in `ReadinessSnapshot`. See DDD-097. | `docs/07-governance/domain-naming-decision-log.md` (DDD-097), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ReadinessQualityThresholdByTool | Policy | **Provisional** per-tool map of readiness quality thresholds consumed by `ReadinessQualityGate`. Must be explicit, map-driven, and extensible through DDD governance. See DDD-097. | `docs/07-governance/domain-naming-decision-log.md` (DDD-097), `docs/02-design/domain-bounded-context-map.md` | provisional |
| CoherenceDiagnosticReasonCode | Value Object | **Provisional** canonical typed reason-code set for coherence diagnostic findings. Codes must be machine-readable, stable, and mapped through policy (not inferred from UI text). See DDD-099. | `docs/07-governance/domain-naming-decision-log.md` (DDD-099), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ReadinessQualityReasonCode | Value Object | **Provisional** canonical typed reason-code set for readiness quality-gate outcomes. Used by `ReadinessSnapshot` projections and backend gate responses. See DDD-099. | `docs/07-governance/domain-naming-decision-log.md` (DDD-099), `docs/02-design/domain-bounded-context-map.md` | provisional |
| CoherenceReasonCodePolicyByTool | Policy | **Provisional** per-tool map-driven policy source that binds coherence diagnostic conditions to canonical `CoherenceDiagnosticReasonCode` values. Must be defined before runtime branches consume it. See DDD-099. | `docs/07-governance/domain-naming-decision-log.md` (DDD-099), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ReadinessQualityReasonCodePolicyByTool | Policy | **Provisional** per-tool map-driven policy source that binds quality-gate evaluation outcomes to canonical `ReadinessQualityReasonCode` values. Must be defined before runtime branches consume it. See DDD-099. | `docs/07-governance/domain-naming-decision-log.md` (DDD-099), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ArtifactOutcomeStatus | Value Object | **Provisional** domain-wide Artifact outcome classification. Canonical values: `not-tested`, `worked`, `failed`, `inconclusive`. Applies to all Artifact-producing tool workflows, including but not limited to `meta-ads`. See DDD-098. | `docs/07-governance/domain-naming-decision-log.md` (DDD-098), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ArtifactOutcomeRecord | Entity | **Provisional** persisted outcome annotation linked to one `Artifact`, including `ArtifactOutcomeStatus`, normalized tags, and audit timestamps. Owned by Generation for persistence semantics and consumed by Frontend/UI for display and filtering. See DDD-098. | `docs/07-governance/domain-naming-decision-log.md` (DDD-098), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ArtifactComparisonSet | Value Object | **Provisional** bounded comparison projection over two or three Artifact outcomes used for positional analysis and test-read interpretation. Cross-context: Generation provides source records; Frontend/UI renders comparative views. See DDD-098. | `docs/07-governance/domain-naming-decision-log.md` (DDD-098), `docs/02-design/domain-bounded-context-map.md` | provisional |
| ArtifactLearningFeedbackLoop | Process | **Provisional** cross-context learning process that feeds `ArtifactOutcomeRecord` evidence into advisory and regenerate strategy selection. Scope is all Artifact-producing workflows. See DDD-098. | `docs/07-governance/domain-naming-decision-log.md` (DDD-098), `docs/02-design/domain-bounded-context-map.md` | provisional |

---

## Canonical Terms

### Generation Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| Artifact | Entity | The persisted output of a single generation attempt. Carries a lifecycle (generating → completed \| failed), type, cost, and token metrics. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql`, `apps/backend/src/lib/types/artifact.ts` | canonical |
| ArtifactType | Value Object | Category of the artifact output, determines output handling, agent selection, and audit classification. Canonical values: `content`, `seo`, `code`, `extraction`. Provisional values: `analysis` (DDD-121 — GEOMETRIC analysis artifacts: scoring JSON, StrategicReport, UnifiedReport), `crawl` (DDD-122 — SERP crawling artifacts from `WorkflowStepType = 'crawling'`; distinct from `extraction` to preserve `StepHydration` path integrity). | `apps/backend/src/lib/types/artifact.ts:7-8` | canonical |
| ArtifactStatus | Value Object | Lifecycle state of an artifact. Values: `generating`, `completed`, `failed`. | `apps/backend/src/lib/types/artifact.ts:11-12` | canonical |
| ArtifactFailureReason | Value Object | Audit trail code explaining why an artifact failed. Values: `client_disconnect`, `timeout`, `error`, `stale`. | `apps/backend/src/lib/types/artifact.ts:15-22` | canonical |
| ArtifactRole | Value Object | Lifecycle role classification of an `Artifact` within a multi-step Tool execution chain. Values: `'step'` (intermediate artifact produced by a non-final `WorkflowStep`; feeds dependency resolution for subsequent steps) and `'final'` (artifact produced by the last `WorkflowStep`; represents the complete Tool output). Persisted in `ToolWorkflowPersistenceMetadata.artifactRole` within the artifact input JSON. Consumed by `StepHydration` for resume/regenerate classification and by Frontend display logic for CTA visibility. See DDD-033. | `apps/backend/src/lib/machines/generation-system.machine.ts` (field `artifactRole` in `buildToolWorkflowPersistenceMetadata`) | canonical |
| WorkflowSessionIdentifier | Value Object | Cross-request session correlation identifier for one multi-step Tool execution. Carried by `GenerationRequest.sessionId`; generated by Frontend at ToolPage initialization; reused by all step requests in the same execution session. See DDD-047. | `packages/contracts/src/index.ts`, `apps/frontend/src/features/tools/runtime/useToolPage.ts` | canonical |
| GenerationSession | Aggregate Root | Deterministic grouping aggregate for all artifacts generated in one multi-step Tool run. Includes `sessionId`, tool identity, run mode, ordered step progress, and artifact membership. See DDD-048. | `apps/backend/src/lib/machines/generation-system.machine.ts`, `apps/backend/src/lib/runtime/auth-http/tools-session-handlers.ts` | canonical |
| GenerationRequest | Command | The input command that initiates a generation. Carries `requestId`, `userId`, `projectId`, `artifactType`, `model`, `input`, `toolKey`, `workflowType`, `idempotencyKey`, `outputFormat`, optional registry selectors, and optional step-level fields `briefingId`, `extractionArtifactId`, `stepDependencyArtifactIds`. Tone handling is step-type specific: for ToolStep content artifact generation, `input.tone` follows canonical `ToneProfile` governance and comes from the user-selected Tool Workspace Page selector; for extraction requests, `input.tone` is an operational job parameter fixed to `analitico` and is not user-configurable. In multi-step flows, each subsequent request must preserve and extend prior step context so outputs remain coherent with the requested `ToolStep`. For `angle-generator`, extraction remains a single LLM job but the effective extraction context is assembled from two user-provided files (`BriefingFile` + `AngleDetectorFile`) before dispatch. | `apps/frontend/src/features/generation/contracts/backend-stream.ts:4-21`, `apps/frontend/src/features/tools/runtime/useToolPage.ts:431`, `apps/frontend/src/features/tools/runtime/tools-client.ts:137`, `packages/contracts/src/index.ts:104-116` | canonical |
| RequestId | Value Object | Unique identifier for a generation request. Scopes idempotency checks and stream session routing. | `apps/backend/src/lib/types/xstate.ts`, `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql` | canonical |
| ToolWorkflow | Value Object | Identifier that maps a generation request to a specific tool route and determines the artifact type. Values: `funnel_pages`, `nextland`, `youtube_lf_script`, `angle_generator`, `youtube_description`, `meta_ads_generator` (Tool-backed, multi-step chain), `blog_article_generator` (DDD-155, blog content generation), `geometric_analysis` (DDD-117, GEOMETRIC multi-query analysis cycle), `extraction` (direct routing path — maps to `RouteType = 'extraction'` internally, bypasses `toolWorkflowMachine`, not a Tool in the DDD-026 sense). snake_case, DB-compatible. **Deprecated alias**: `meta_ads` — legacy workflow token superseded by `meta_ads_generator` under DDD-094. **Naming note**: `ToolWorkflow = 'extraction'` and `WorkflowStepType = 'extraction'` use the same word for orthogonal concepts — the former is a routing path, the latter is a step execution strategy (DDD-C-006). | `apps/backend/src/lib/types/artifact.ts:24-26`, `apps/backend/src/lib/machines/generation-system.machine.ts:532-554`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| GenerationRequestAssembly | Process | **Provisional** — Application-Layer process that translates accumulated FE session state (`HydrationResult` + `ExtractionContext` + selected `ToolStep`) into a well-formed `GenerationRequest` command ready for BE dispatch. Runs in the Frontend runtime but produces a BE-owned domain command (scope: Generation — output ownership). Content step dispatch implemented in `apps/frontend/src/features/tools/runtime/useToolPage.ts:431-453`; extraction request assembly in `apps/frontend/src/features/tools/runtime/tools-client.ts:155-180`. Not a Domain Service (no domain logic) and not a Value Object (it is a transformation, not a data structure). Input: `HydrationResult` (prior artifact state), `ExtractionContext` (structured briefing payload), `ToolStep` (selected step). Output: `GenerationRequest`. Status: provisional — see DDD-032. | `apps/frontend/src/features/tools/runtime/useToolPage.ts:431-453`, `apps/frontend/src/features/tools/runtime/tools-client.ts:155-180` | provisional |
| ToolStepOrchestration | Domain Service | **Provisional** — Target canonical term for the backend operation that determines which prior `Artifact` IDs a given `ToolStep` depends on, given a `ToolKey` and the completed step history. Authoritative BE implementation: `resolveStepDependencyIds` in `apps/backend/src/lib/runtime/tool-workflow-registry.ts:27`, exposed via `/api/tools/orchestrate` endpoint. FE runtime consumes this via `orchestrateToolStep` in `apps/frontend/src/features/tools/runtime/useToolPageRunController.ts`. Status: implemented-runtime provisional; see DDD-031 and DDD-C-007. | `apps/backend/src/lib/runtime/tool-workflow-registry.ts:27`, `apps/backend/src/lib/runtime/auth-http/tools-orchestrate-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/route-table.ts`, `apps/frontend/src/features/tools/runtime/useToolPageRunController.ts:336` | provisional |
| OutputFormat | Value Object | Formatting contract for the streamed response. Values: `plain`, `json`, `markdown`. | `apps/backend/src/lib/types/artifact.ts:31-33` | canonical |
| ContentBuffer | Concept | Transient in-memory accumulator for streamed LLM output chunks before final persistence. | `apps/backend/src/lib/types/xstate.ts` (field `contentBuffer` on `GenerationSystemContext`) | canonical |
| WorkflowRunMode | Value Object | Intent of a generation invocation relative to prior runs. `new` = no prior artifact, first-time generation; `resume` = continue from a paused or incomplete prior run; `regenerate` = re-run a step against an already-completed or failed prior artifact. **Operational note**: Input representation in `requestInput.intent` is normalized to `WorkflowRunMode` by `resolveWorkflowRunMode` in `generation-system.machine.ts`. Resume and regenerate modes inject a `WorkflowStepBootstrap` into `ToolWorkflowInput` to specify the resume starting point (see DDD-037). **FE projection**: the identical value set `'new' \| 'resume' \| 'regenerate'` is used as an inline union type for the `intent` field in `PROGRESS_SYNCED` event and `BuildToolPageViewModelInput` in `tool-page.machine.ts` — same semantics, no import of the BE type (projection by structural equivalence). | `apps/backend/src/lib/types/xstate.ts:22`, `apps/backend/src/lib/machines/generation-system.machine.ts` (`resolveWorkflowRunMode`); FE projection: `apps/frontend/src/features/tools/machines/tool-page.machine.ts` (`intent` field, `PROGRESS_SYNCED` event) | canonical |
| WorkflowStep | Entity | A single named step within a multi-step tool generation flow. Exists in two forms: `WorkflowStepDescriptor` (static configuration: name, dependencies, optional `type`) and `WorkflowStepState` (runtime tracking: status, artifact id). Backend abstract descriptor — frontend concrete counterpart is `ToolStep`. Step dependencies enforce progressive context accumulation across steps. Each step belongs to a `WorkflowStepType` execution category. | `apps/backend/src/lib/types/xstate.ts` (`WorkflowStepDescriptor`, `WorkflowStepState`), `apps/backend/src/lib/machines/tool-workflow.machine.ts` | canonical |
| WorkflowStepType | Value Object | The execution strategy category of a `WorkflowStep`. Determines which runtime actor handles the step within the `GenerationSystem`. Values: `extraction` (LLM-driven structured extraction from input context — handled by `ExtractionChain`), `generation` (LLM-driven content production — handled by `StreamTransport` + `PersistenceBatch`), `acquisition` (retrieval of data from external APIs through persisted `ApiService` definitions via `acquisitionChainMachine`), `crawling` (asynchronous web scraping/SERP extraction with screenshot capture — handled by a new crawling chain actor, DDD-116), `scoring` (competitor domain grouping, source classification, weighted score computation, tier assignment — handled by a new scoring chain actor, DDD-116). Backend baseline is fully implemented and tested for `extraction`, `generation`, and `acquisition`: `WorkflowStepDescriptor` carries optional `type` metadata in `xstate.ts`; acquisition runtime is active through `acquisitionChainMachine`, context merge assembly, and orchestration-path tests. `crawling` and `scoring` are **provisional** — type definitions introduced; runtime actors pending implementation. Progressive rollout by tool configuration is in scope but does not block canonical status — the type system and execution paths are stable. See DDD-101, DDD-116. | `apps/backend/src/lib/types/xstate.ts`, `apps/backend/src/lib/machines/extraction-chain.machine.ts`, `apps/backend/src/lib/machines/stream-transport.machine.ts`, `apps/backend/src/lib/machines/generation/acquisition-chain.machine.ts`, `apps/backend/src/lib/machines/generation/context-generation-assembly.ts`, `apps/backend/src/lib/machines/tool-workflow.machine.ts`, `apps/backend/src/lib/tests/runtime.acquisition-workflow.machine.test.ts`, `apps/backend/src/lib/tests/runtime.tools-orchestrate.test.ts` | canonical |
| WorkflowStepStatus | Value Object | Status of a single WorkflowStep. Values: `idle`, `running`, `done`, `error`, `skipped`. | `apps/backend/src/lib/types/xstate.ts:23` | canonical |
| WorkflowStepBootstrap | Value Object | Initial state injected into a `ToolWorkflowMachine` when resuming or regenerating from a prior `Artifact`. Shape: `{ stepKey: string, output: string, artifactId: string }`. When `WorkflowRunMode = 'resume'` or `'regenerate'`, the `ToolWorkflowInput` receives a non-null `bootstrap` field to skip already-completed steps and continue from the resume point. Persisted in `ToolWorkflowPersistenceMetadata` as part of hydration. See DDD-037. | `apps/backend/src/lib/types/xstate.ts` (`ToolWorkflowInput.bootstrap`), `apps/backend/src/lib/machines/tool-workflow.machine.ts` (`createInitialStepStates`) | canonical |
| ApiService | Entity | Administrator-managed persisted definition of an external API source that a Tool can use as input context. `ApiService` carries service identity plus the outbound endpoint, retrieval policy, access mode (`ApiServiceAccessMode`), and token header configuration (`tokenHeaderName`). It belongs to the Generation context because external API invocation, request normalization, and credential-bearing outbound calls remain backend-owned. `ApiService` extends `ToolInputSource`; it does not replace typed form input or `ToolInputFile` uploads. Full implementation baseline: persistence (`api_services` + `api_service_tool_step_bindings` tables), adapter CRUD, admin HTTP handlers, tool runtime resolution, validation/redaction, outbound acquisition adapter, shared contracts, and frontend admin surfaces. See DDD-102. | `packages/infra-db/migrations/20260524_000011_api_service_catalog.sql`, `apps/backend/src/lib/types/api-service.ts`, `apps/backend/src/lib/adapters/api-service.adapter.ts`, `apps/backend/src/lib/runtime/auth-http/admin-api-service-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/tools-api-service-handlers.ts`, `apps/backend/src/lib/runtime/integrations/api-service-validation.ts`, `apps/backend/src/lib/runtime/integrations/api-acquisition.adapter.ts` | canonical |
| ApiServiceAccessMode | Value Object | Outbound access policy for an `ApiService`. Values: `public` (no authentication required), `token` (authentication via HTTP headers), `query-param` (authentication via URL query parameters, e.g., `?api_key=YOUR_KEY`). The `query-param` mode requires a `tokenParamName` field to specify the parameter name (defaults to `'api_key'`). This value object governs external service access only; it must not be conflated with end-user authentication concepts such as `AuthMethod` or `OAuthProvider`. Backend validation and redaction policy implemented. See DDD-103, DDD-130. | `apps/backend/src/lib/types/api-service.ts`, `packages/contracts/src/api-service.ts`, `apps/backend/src/lib/runtime/integrations/api-service-validation.ts`, `apps/backend/src/lib/runtime/integrations/api-acquisition.adapter.ts`, `apps/backend/src/lib/tests/runtime.api-service-validation.test.ts` | canonical |
| tokenHeaderName | Value Object | `ApiService` request-profile header key used only when `ApiServiceAccessMode = token` and outbound token placement must override the default bearer header. Default behavior remains `Authorization: Bearer <token>` when `tokenHeaderName` is null/empty. When set, token is injected exactly once into `headers[tokenHeaderName]` with deterministic override precedence over same-name entries in `requestHeadersTemplateJson`; other template headers are preserved. Validation requires strict HTTP header-name format and trim normalization. `tokenHeaderName` configures header key placement only and never carries token secret values. See DDD-104. | `packages/infra-db/migrations/20260524_000013_api_service_token_header_name.sql`, `apps/backend/src/lib/runtime/integrations/api-service-validation.ts`, `apps/backend/src/lib/runtime/integrations/api-acquisition.adapter.ts`, `packages/contracts/src/api-service.ts`, `apps/backend/src/lib/tests/runtime.api-acquisition.adapter.test.ts`, `apps/backend/src/lib/tests/runtime.api-service-validation.test.ts`, `apps/backend/src/lib/tests/runtime.api-service-auth-http.test.ts` | canonical |
| ApiServiceCatalog | Domain Service | **Provisional** admin-managed catalog of persisted `ApiService` definitions available to Tool acquisition flows. Baseline governance mirrors the existing `LlmModelCatalog` pattern: authenticated runtime reads resolve which external API source can be used during execution, while create/update/delete operations are restricted to `AuthUserRole = 'admin'`. The catalog is an execution-time authority; Tool flows consume resolved service definitions read-only. Backend admin/tool HTTP surfaces and route capability declarations are implemented; FE product rollout remains progressive. See DDD-087. | `apps/backend/src/lib/runtime/auth-http/admin-api-service-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/auth-http-admin-routes.ts`, `apps/backend/src/lib/runtime/auth-http/tools-api-service-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/route-table.ts`, `apps/backend/src/lib/tests/runtime.api-service-auth-http.test.ts` | provisional |
| WorkflowStepUnlocked | Domain Event | Event emitted by `toolWorkflowMachine` when the dependencies of a `WorkflowStep` are satisfied and the step transitions from `'idle'` to `'running'`. Internal event (does not cross process boundary). Semantically significant: represents the deterministic progression of a multi-step workflow step by step. See DDD-035. | `apps/backend/src/lib/machines/tool-workflow.machine.ts` (event `STEP_START`, guard `dependenciesSatisfied`) | canonical |
| WorkflowStepCompleted | Domain Event | Event emitted by `toolWorkflowMachine` when a `WorkflowStep` finishes execution and its `WorkflowStepState` reaches terminal status (`'done'`, `'error'`, `'skipped'`). Contains artifact ID (if successful) and error metadata (if failed). Internal event (does not cross process boundary). The completion event is consumed by downstream steps to unlock their dependencies. Represents the completion boundary of a step's execution contract. See DDD-036. | `apps/backend/src/lib/machines/tool-workflow.machine.ts` (events `STEP_SUCCESS`, `STEP_FAILURE`, `STEP_SKIP`) | canonical |
| ToolWorkflowPersistenceMetadata | Value Object | Structured metadata embedded under `input_json.toolWorkflow` when `routeType = 'tool'`. Orchestration authority remains JSON and includes `{ toolKey, workflowType, runMode, artifactRole, stepKey, dependsOnSteps, dependencyArtifactIds, dependencyArtifactIdsByStep }`; queryable denormalized cache is persisted in columns `{ session_id, step_key, artifact_role, run_mode }` for deterministic indexed reads. See DDD-034 and DDD-050. | `apps/backend/src/lib/machines/generation-system.machine.ts` (`buildToolWorkflowPersistenceMetadata`), `apps/backend/src/lib/adapters/postgres-redis.production.ts` | canonical |
| RegistryVersion | Value Object | Version identifier for a tool configuration snapshot in the tool registry. | `apps/backend/src/lib/types/xstate.ts` | canonical |
| RegistrySnapshotRef | Value Object | Content-addressable reference to a specific tool registry snapshot. | `apps/backend/src/lib/types/xstate.ts` | canonical |
| LlmModel | Entity | An administrator-managed LLM endpoint available for generation selection. Attributes: `id` (surrogate DB key), `key` (`LlmModelId`, e.g. `openrouter/auto`), `label` (display name), `status` (`LlmModelStatus`), `sortOrder` (optional UI ordering). Persisted in the `llm_models` table. Admin CRUD operations require `AuthUserRole = 'admin'`. Frontend-facing read is limited to `enabled` entries. See DDD-053. | `apps/frontend/src/features/admin/pages/AdminModelsPage.tsx:5-9` (hardcoded fallback — backend endpoint pending) | canonical |
| LlmModelStatus | Value Object | Operational lifecycle state of an `LlmModel`. Values: `enabled` (visible and selectable by users), `disabled` (hidden from user selection; preserved for audit continuity in artifact history records). See DDD-054. | `apps/frontend/src/features/admin/pages/AdminModelsPage.tsx:6-9` (status field) | canonical |
| LlmModelCatalog | Domain Service | The admin-managed, ordered collection of `LlmModel` entries that defines the authoritative set of models available for generation. Exposed as: `GET /api/models` (authenticated users, `enabled` entries only) and `GET /api/admin/models` / `POST` / `PUT /:id` / `DELETE /:id` (admin CRUD, requires `AuthUserRole = 'admin'`). Frontend consumes the catalog as option source for the `LlmModelSelector` component. Status: **canonical** — supersedes the deprecated `adminModels` / `listAdminModels` dead code (DDD-024, DDD-055). | `apps/frontend/src/features/admin/pages/AdminModelsPage.tsx` (page stub with `backendEndpointPending`), `apps/frontend/src/app/copy/system.ts:251` | canonical |
| LlmUsageMetrics | Value Object | Measured cost and token counters for a completed generation: `inputTokens`, `outputTokens`, `costUsd`. | `apps/backend/src/lib/types/xstate.ts` | canonical |
| StepLlmModelOverrideConfig | Value Object | Static configuration of LLM model override for a specific tool step. Structure: `{ toolKey: ToolKey, stepKey: string, overrideModelId: LlmModelId, reason?: string }`. Configured statically in repository and validated against canonical registries at startup. When configured, takes precedence over user selection during generation. See DDD-150. | `apps/backend/src/lib/runtime/step-llm-model-overrides.config.ts` | canonical |
| StepLlmModelResolver | Domain Service | Service for determining the effective LLM model for a given tool step based on override configuration and user selection. Implements precedence rules: static override → user selection → system default (`openrouter/auto`). Operates synchronously using in-memory configuration for optimal performance. See DDD-151. | `apps/backend/src/lib/runtime/step-llm-model-resolver.ts` | canonical |
| EffectiveModelResolution | Value Object | Result of model resolution with metadata about decision source. Structure: `{ effectiveModel: LlmModelId, source: 'user-selection' \| 'step-override', overrideReason?: string, originalUserModel?: LlmModelId }`. Used for transparency indicators and audit requirements. See DDD-152. | `apps/backend/src/lib/types/xstate.ts` | canonical |
| GenerationSystem | Aggregate Root | The XState actor tree orchestrating the end-to-end lifecycle of a single generation: gateway → idempotency → usage → tool-workflow → stream → persistence. | `apps/backend/src/lib/machines/generation-system.machine.ts` | canonical |
| StreamTransport | Domain Service | Actor managing the SSE streaming session from the LLM to the backend. Internal machine events: `chunk` (content token), `heartbeat` (keepalive), `completed` (stream closed). These are distinct from `BackendStreamEvent` types (`start`, `chunk`, `terminal`) emitted on the SSE wire to the Frontend. | `apps/backend/src/lib/machines/stream-transport.machine.ts` | canonical |
| PersistenceBatch | Domain Service | Actor responsible for flushing incremental chunks and finalizing the artifact in the database. | `apps/backend/src/lib/machines/persistence-batch.machine.ts` | canonical |
| ExtractionChain | Domain Service | Actor running the extraction workflow: attempts structured extraction from LLM output, falls back to text. | `apps/backend/src/lib/machines/extraction-chain.machine.ts` | canonical |
| IdempotencyCoordinator | Domain Service | Actor that checks and atomically claims an idempotency slot to prevent duplicate artifact generation. | `apps/backend/src/lib/machines/idempotency-coordinator.machine.ts` | canonical |
| IdempotencyKey | Value Object | Client-supplied deduplication token scoped to `(userId, projectId, endpoint)`. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql` | canonical |
| IdempotencyDecision | Value Object | Result of an idempotency check. `claimed` = new slot reserved, generation proceeds; `replay` = duplicate request detected, prior artifact is returned without re-running; `conflict` = slot exists in an irrecoverable inconsistent state, generation is rejected. | `apps/backend/src/lib/adapters/generation.adapters.ts:37-41` | canonical |
| RequestGateway | Domain Service | Actor performing pre-generation validation: auth, input validation, model availability, project ownership, and quota pre-authorization gate. Does not decrement quota — actual quota decrement is performed via `ClaimUsage` by `UsageMachine` after idempotency is resolved. | `apps/backend/src/lib/machines/request-gateway.machine.ts` | canonical |
| BackendStreamEvent | Domain Event | Server-Sent Event emitted during a generation. Types: `start` (artifact created), `chunk` (incremental content), `terminal` (final status). | `apps/frontend/src/features/generation/contracts/backend-stream.ts:20-30` | canonical |
| OrchestrateArtifactCache | Domain Service | Redis Hash cache storing completed artifact step→ID mappings per user/project/workflow. Used by `/api/tools/orchestrate` for cache-first lookup to avoid DB queries during active sessions. TTL 4 hours. Populated by `finalizeSuccess` after each artifact completion. | `apps/backend/src/lib/runtime/auth-http/tools-orchestrate-handlers.ts`, `apps/backend/src/lib/adapters/postgres-redis.production.ts` | canonical |
| CrawlArtifact | Value Object | **Provisional** — The structured data artifact produced by the `serp-crawling` step (`WorkflowStepType = 'crawling'`). `ArtifactType = 'crawl'`, `ArtifactRole = 'step'`. Content is serialized JSON carrying per-query extraction data: `SerpAIOverviewSnippet` texts, parsed `SerpSource` list, `SerpScreenshot` storage URL, and `PAAQuery` entries. Consumed as `stepDependencyArtifactIds` by both `competitor-scoring` and `strategic-reporting` steps. The `crawl` type is intentionally distinct from `ArtifactType = 'extraction'` to preserve `StepHydration` hydration-path integrity (DDD-122). | `docs/07-governance/domain-naming-decision-log.md` (DDD-122) | provisional |
| ScoringArtifact | Value Object | **Provisional** — The structured analysis artifact produced by the `competitor-scoring` step (`WorkflowStepType = 'scoring'`). `ArtifactType = 'analysis'`, `ArtifactRole = 'step'`. Content is serialized `CompetitorRanking` JSON (competitor domains, entity names, raw scores, tier assignments, query coverage). The `GeoScore` and `CompetitorTier` **types** remain internal to `CompetitorAnalysisContext` (DDD-119, DDD-124); the **data** crosses the context boundary as artifact content consumed by `strategic-reporting` and `unified-report` steps via `stepDependencyArtifactIds`. | `docs/07-governance/domain-naming-decision-log.md` (DDD-124) | provisional |
| AnalysisSessionIdentifier | Value Object | **Provisional** — Canonical session identity Value Object for GEOMETRIC `AnalysisSession`. Distinct from `WorkflowSessionIdentifier` (DDD-047): generated at `AnalysisSession` creation time (when user submits `BaseQuery + language + country`), stable through the dynamic `QueryCluster` expansion phase (as `PAAQuery` entries are discovered during `serp-crawling`). Scope uniqueness: `(userId, projectId, toolKey, baseQuery, language, country, createdAt)`. Frontend generates it immediately before the first `serp-crawling` dispatch and reuses it for all subsequent steps in the same analysis cycle. Required because GEOMETRIC's QueryCluster is mutable during execution — unlike standard tools where all step dependencies are known at session start. See DDD-127. | `docs/07-governance/domain-naming-decision-log.md` (DDD-127) | provisional |
| AnalysisSession | Aggregate Root | **Provisional** — Deterministic grouping aggregate for all artifacts produced during a multi-query analysis cycle within the GEOMETRIC tool. Unlike `GenerationSession` (which groups artifacts from a single multi-step Tool run), `AnalysisSession` groups artifacts from a `QueryCluster` (one `BaseQuery` + its derived `PAAQuery` entries). Carries `sessionId`, `projectId`, `userId`, `toolKey`, `baseQuery`, `language`, `country`, and artifact membership. Status: **provisional** — new aggregate root introduced for GEOMETRIC tool (DDD-113). | `docs/07-governance/domain-naming-decision-log.md` (DDD-113) | provisional |
| QueryCluster | Value Object | **Provisional** — The complete set of queries associated with one `AnalysisSession`: one `BaseQuery` (user-entered keyword/question) plus up to four `PAAQuery` entries (Google "People Also Ask" correlated queries). `QueryCluster` is internal to the GEOMETRIC tool flow and is not shared via contracts. See DDD-118. | `docs/07-governance/domain-naming-decision-log.md` (DDD-118) | provisional |
| BaseQuery | Value Object | **Provisional** — The initial user-entered keyword or question that initiates an `AnalysisSession`. Carries the raw query text and optional metadata (language, country). See DDD-118. | `docs/07-governance/domain-naming-decision-log.md` (DDD-118) | provisional |
| PAAQuery | Value Object | **Provisional** — A Google "People Also Ask" correlated query derived during the crawling phase of an `AnalysisSession`. Each `AnalysisSession` can have up to four `PAAQuery` entries associated with its `BaseQuery`. See DDD-118. | `docs/07-governance/domain-naming-decision-log.md` (DDD-118) | provisional |
| StrategicReport | Entity | **Provisional** — The qualitative LLM-generated analysis artifact produced by the `strategic-reporting` step (`WorkflowStepType = 'generation'`) of a GEOMETRIC `AnalysisSession`. `ArtifactType = 'analysis'`, `ArtifactRole = 'step'`. LLM input: `SerpAIOverviewSnippet` texts from the `serp-crawling` `CrawlArtifact` + `CompetitorRanking` JSON from the `competitor-scoring` `ScoringArtifact` (consumed as `stepDependencyArtifactIds`). Output: qualitative Markdown content (insight patterns, trend observations, operational recommendations for the brand). `SerpScreenshot` data is never forwarded to the LLM (token efficiency rule, DDD-120). Its content feeds the subsequent `unified-report` step as dependency payload. See DDD-120, DDD-123. | `docs/07-governance/domain-naming-decision-log.md` (DDD-120) | provisional |
| UnifiedReport | Entity | **Provisional** — The terminal artifact of the GEOMETRIC tool flow, produced by the `unified-report` step (`WorkflowStepType = 'generation'`). `ArtifactType = 'analysis'`, `ArtifactRole = 'final'`. LLM input: `StrategicReport` content from the prior step + `CompetitorRanking` data as structured table context (consumed as `stepDependencyArtifactIds`). Output: final combined Markdown document integrating qualitative analysis and quantitative competitor ranking tables. The `UnifiedReport` content is the source for the FE `GeometricExport` download operation. See DDD-120, DDD-123. | `docs/07-governance/domain-naming-decision-log.md` (DDD-120) | provisional |
| GeometricExport | Application Service | **Provisional** — Frontend-only download operation that aggregates the `UnifiedReport` artifact content from an `AnalysisSession` and produces a downloadable file (`.docx` or `.pdf`). Not a `WorkflowStep` and does not produce a backend `Artifact`. Analogous to the existing SessionSummary download pattern. Owned exclusively by Frontend/UI. See DDD-123. | `docs/07-governance/domain-naming-decision-log.md` (DDD-123) | provisional |

---

### Crawling & Extraction Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| CrawlingAndExtractionContext | Bounded Context | **Provisional** — New bounded context responsible for asynchronous web crawling, SERP scraping, anti-bot bypass, and structured data extraction from search engine results. Produces `CrawlingJob` (Aggregate Root) and `QueryExtraction` (Entity) artifacts. This context is upstream of Generation (provides raw extraction data) and downstream of Auth/Usage (gated by user identity and quota). The crawling step is exposed as a reusable `WorkflowStepType = 'crawling'` within Generation (DDD-116). See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| CrawlingJob | Aggregate Root | **Provisional** — The asynchronous task in queue representing a complete crawling/extraction cycle for one `QueryCluster`. Carries job identity, session reference, status (`PENDING`, `RUNNING`, `COMPLETED`, `FAILED`), and artifact references. Renamed from `ExtractionJob` (DDD-C-015). See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| QueryExtraction | Entity | **Provisional** — A single query extraction within a `QueryCluster`. Carries the query text (Base or PAA), raw extracted text, and parsed `SerpSource` list. Belongs to a `CrawlingJob`. Produces a `CrawlArtifact` upon completion. See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| SerpScreenshot | Value Object | **DEPRECATED** — Former PNG image documenting the visual aspect of the Google SERP during a `QueryExtraction`. Obsoleted by SerpApi-only architecture: SerpApi provides structured data without visual capture. `screenshotPath` is always `null` in SerpApi normalizer. Removal completed in `remove-geometric-screenshot-archival.md`. See DDD-145. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114, DDD-145) | deprecated |
| CrawlingResult | Value Object | **Provisional** — Temporary in-memory data structure containing extracted `SerpSource` list and `SerpAIOverviewSnippet` text for a single query. Intermediate output before persistence into a `CrawlArtifact`. Renamed from `ExtractionResult` (DDD-C-018). See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| SerpSource | Value Object | **Provisional** — A single bibliographic reference cited within a `SerpAIOverview`. Composed of title, URL, and text snippet. Belongs to `QueryExtraction`. Classified by `SerpSourceType`. Renamed from `Source` (DDD-C-016). See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| SerpSourceType | Value Object | **Provisional** — Canonical classification of a `SerpSource` detected within a `SerpAIOverview`. Values: `ORGANIC_WEBSITE`, `SPONSORED_ADS`, `YOUTUBE_VIDEO`, `SOCIAL_MEDIA`, `UGC_COMMUNITY`. Used by `CompetitorAnalysisContext` to apply differentiated scoring weights. Renamed from `SourceType` (DDD-C-016). See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| SerpAIOverview | Value Object | **Provisional** — The Google AI Overview block displayed at the top of a SERP for a specific query. Contains the full overview text body and the list of cited `SerpSource` entries. Belongs to `QueryExtraction`. Renamed from `AIOverview` (DDD-C-017). See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| SerpAIOverviewSnippet | Value Object | **Provisional** — The raw text body of the `SerpAIOverview` associated with a specific query. Serialized into the `CrawlArtifact` JSON content. Consumed by the `strategic-reporting` step as primary LLM input context. Renamed from `AIOverviewSnippet` (DDD-C-017). See DDD-114. | `docs/07-governance/domain-naming-decision-log.md` (DDD-114) | provisional |
| screenshotArchival | Adapter/System | **DEPRECATED** — Former adapter system for persisting SERP screenshot metadata and files during Puppeteer-based crawling. Included storage path management, expiration handling, and admin query interfaces. Obsoleted by SerpApi-only architectural shift (no screenshot production). Removal completed in `remove-geometric-screenshot-archival.md`. See DDD-146. | `apps/backend/src/lib/adapters/generation.adapters.ts` (historical) | deprecated |
| aiOverviewConfidence | Value Object | **DEPRECATED** — Numeric confidence score (0.0-1.0) for AI Overview extraction accuracy in Puppeteer-based crawling. Computed via CSS selector reliability mapping (`[data-snf]`: 0.95, `.AIHVYe`: 0.90, etc.). Hardcoded to 0.95 in SerpApi normalizer without semantic meaning. No downstream consumers use for operational logic. Removal completed in `remove-geometric-screenshot-archival.md`. See DDD-145, DDD-146. | `apps/backend/src/lib/runtime/integrations/crawling.adapter.ts` (historical) | deprecated |
| selectorUsed | Value Object | **DEPRECATED** — CSS selector string indicating which DOM element was used to extract AI Overview content during Puppeteer-based crawling. Values included `[data-snf]`, `.AIHVYe`, `serpapi-ai-overview` (hardcoded). Obsoleted by SerpApi structured data which provides direct AI Overview text without DOM parsing. Removal completed in `remove-geometric-screenshot-archival.md`. See DDD-145, DDD-146. | `apps/backend/src/lib/runtime/integrations/crawling.adapter.ts` (historical) | deprecated |
| screenshotPath | Value Object | **DEPRECATED** — File system path to stored PNG screenshot of SERP during Puppeteer-based crawling. Always `null` in SerpApi implementation since API returns structured data without visual capture. Used for admin verification and archival storage. Removal completed in `remove-geometric-screenshot-archival.md`. See DDD-145, DDD-146. | `apps/backend/src/lib/runtime/integrations/crawling.adapter.ts` (historical) | deprecated |

---

### Competitor Analysis Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| CompetitorAnalysisContext | Bounded Context | **Provisional** — New bounded context responsible for grouping raw extraction data, identifying unique competitor domains, classifying source types, computing weighted `GeoScore` values, and assigning `CompetitorTier` classifications. Produces `CompetitorRanking` (Aggregate Root) per `AnalysisSession`. This context is downstream of `CrawlingAndExtractionContext` (consumes raw extraction JSON) and upstream of Generation (provides scoring data for LLM reporting). `GeoScore` and `CompetitorTier` are internal to this context — not shared via contracts. See DDD-115. | `docs/07-governance/domain-naming-decision-log.md` (DDD-115) | provisional |
| CompetitorRanking | Aggregate Root | **Provisional** — The quantitative ranking calculated for competitors within an `AnalysisSession`. Groups all `Competitor` entities associated with one session and provides tier-based classification. Persisted as a `ScoringArtifact` (`ArtifactType = 'analysis'`) whose JSON content crosses the boundary to Generation via `stepDependencyArtifactIds`. See DDD-115, DDD-124. | `docs/07-governance/domain-naming-decision-log.md` (DDD-115) | provisional |
| Competitor | Entity | **Provisional** — A brand or web domain detected among the `SerpSource` entries of a `SerpAIOverview`. Carries domain, entity name, raw score, tier classification, and query coverage. Belongs to a `CompetitorRanking`. `GeoScore` and `CompetitorTier` values are computed internally and serialized into the `ScoringArtifact` content. See DDD-115. | `docs/07-governance/domain-naming-decision-log.md` (DDD-115) | provisional |
| GeoScore | Value Object | **Provisional** — The numeric visibility score (normalized 1-10) computed for a `Competitor` within `CompetitorAnalysisContext`. Internal TypeScript type — not exposed via `packages/contracts`. The computed value is serialized as a numeric field in the `ScoringArtifact` JSON content, which crosses the boundary to Generation. See DDD-119, DDD-124. | `docs/07-governance/domain-naming-decision-log.md` (DDD-119) | provisional |
| CompetitorTier | Value Object | **Provisional** — The positioning segmentation of a competitor based on its `GeoScore`. Values: `TIER_1` (Dominators), `TIER_2` (Constant Presence), `TIER_3` (Informative/Niche Sources). Internal TypeScript type — not exposed via `packages/contracts`. The computed value is serialized as a string enum field in the `ScoringArtifact` JSON content. See DDD-119, DDD-124. | `docs/07-governance/domain-naming-decision-log.md` (DDD-119) | provisional |

---

### Auth Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| User | Entity | A registered account in the system. Carries identity, role, status, quota, and optional password credentials. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql`, `apps/backend/src/lib/types/auth.ts` | canonical |
| AuthUserRole | Value Object | Access control level for a User. Values: `admin`, `member`. | `apps/backend/src/lib/types/auth.ts:3-4` | canonical |
| AuthUserStatus | Value Object | Operational state of a User account. Values: `active`, `disabled`, `pending_password_reset`. | `apps/backend/src/lib/types/auth.ts:7-8` | canonical |
| AuthMethod | Value Object | Authentication mechanism used for a session. Values: `native` (email+password), `google` (OAuth). | `apps/backend/src/lib/types/auth.ts:11-12` | canonical |
| AuthSession | Entity | A live authenticated session binding a User to a session token. Tracks IP, user-agent, expiry, and revocation. | `apps/backend/src/lib/types/auth.ts` (`AuthSessionRecord`) | canonical |
| AuthSessionPrincipal | Value Object | Composite read model combining an AuthSession with the minimal User identity (`id`, `email`, `role`, `status`). | `apps/backend/src/lib/types/auth.ts:46-49` | canonical |
| OAuthProvider | Value Object | External identity provider used for OAuth authentication. Values: `google`. | `apps/backend/src/lib/types/auth.ts:15-16` | canonical |
| OAuthAccount | Entity | The link between a User and an external OAuth identity, storing the provider subject and email at provider. | `apps/backend/src/lib/types/auth.ts` (`OAuthAccountRecord`) | canonical |
| OAuthStateToken | Value Object | Short-lived PKCE/CSRF token generated at the start of an OAuth flow; consumed once upon callback. | `apps/backend/src/lib/types/auth.ts` (`OAuthStateTokenRecord`) | canonical |

---

### Usage / Quota Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| CreditQuota | Value Object | Alias canonico per `MonthlyQuota` ridefinito come "massimo numero di crediti mensili disponibili per un utente" invece di "massimo numero di richieste mensili". Il valore numerico rimane sulla colonna `monthly_quota` della tabella `users`. `CreditQuota` è il termine canonico per comunicazione e documentazione; `MonthlyQuota` rimane come backward-compat alias. See DDD-137. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql` (column `monthly_quota`) | canonical |
| MonthlyQuota | Value Object | Backward-compat alias per `CreditQuota`. Ridefinito come massimo crediti mensili (non più request count). See DDD-137. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql` (column `monthly_quota`) | canonical (alias) |
| MonthlyCreditsUsed | Value Object | The counter of credits consumed by a User in the current billing period. Replaces `MonthlyUsed`. Persisted as `monthly_credits_used` (DB column, renamed from `monthly_used`). See DDD-138. | `packages/infra-db/migrations/` (column `monthly_credits_used`) | canonical |
| MonthlyUsed | Value Object | **Deprecated** — backward-compat alias per `MonthlyCreditsUsed`. See DDD-138. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql` (column `monthly_used`) | deprecated |
| CreditCost | Value Object | The cost in credits of a Session Summary for a given tool. Default value: 1. Configurable per tool in `packages/contracts/src/tool-workflows.ts` as `creditCost` field on `ToolWorkflowDefinition`. See DDD-139. | `packages/contracts/src/tool-workflows.ts` | canonical |
| ArtifactGateLimit | Value Object | The maximum number of artifacts generable per month for a User (default 1000). Invisible to the user — API responses do not expose this value. Persisted as `monthly_artifact_limit` on `users` table. See DDD-140. | `packages/infra-db/migrations/` (column `monthly_artifact_limit`) | canonical |
| ArtifactGateUsed | Value Object | The counter of artifacts generated with SUCCESS in the current billing month. Invisible to the user. Persisted as `monthly_artifacts_used` on `users` table. See DDD-140. | `packages/infra-db/migrations/` (column `monthly_artifacts_used`) | canonical |
| QuotaWindowPeriod | Value Object | The calendar-anchored time boundary for a User's quota period. Determines when `MonthlyCreditsUsed` is reset to zero before the next generation attempt. Persisted as `quota_window_started_at` (DB column). Reset is evaluated at `ClaimUsage` time by comparing the stored start date against the current date at month granularity; if the stored period is in a prior month the counter is reset and the new `quota_window_started_at` is written atomically. Not a user-facing concept. See DDD-059. | `packages/infra-db/migrations/20260518_000010_quota_window_started_at.sql`, `apps/backend/src/lib/adapters/postgres-redis.production.ts:325` | canonical |
| QuotaHistory | Entity | An immutable audit record of a single generation attempt, capturing outcome, token counts, cost, and metadata. Extended with `session_id`, `cost_type` (`session_summary` | `artifact`), `credit_cost`. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql` (table `quota_history`) | canonical |
| QuotaEventStatus | Value Object | Outcome classification for a QuotaHistory entry. Values: `success`, `error`, `rate_limited`. | `apps/backend/src/lib/types/artifact.ts:29-30` | canonical |
| UsageDecision | Value Object | Result of a quota claim attempt: `{ granted: boolean, reason?: string, creditCost?: number }`. The `creditCost` field carries the tool's credit cost when granted. Optionally carries `resetDate?: Date` when the quota window was reset during the claim (audit tracing only, not a readiness gate). See DDD-060, DDD-143. | `apps/backend/src/lib/adapters/generation.adapters.ts:33-36` | canonical |
| ClaimUsage | Command | The atomic operation that verifies a User's remaining quota (credits + artifact gate) without consuming credits. Checks two conditions: (1) `ArtifactGateUsed < ArtifactGateLimit` (invisible gate), (2) `MonthlyCreditsUsed + CreditCost <= CreditQuota` (available credits). Returns `{ granted: true, creditCost }` or `{ granted: false, reason: 'quota_exhausted' }`. When the `QuotaWindowPeriod` has expired, resets `MonthlyCreditsUsed` to zero and updates `quota_window_started_at` before checking. Credits are consumed post-SUCCESS via `ConsumeCredits`, not here. See DDD-143. | `apps/backend/src/lib/machines/usage.machine.ts` (`claimUsage` actor) | canonical |
| ConsumeCredits | Command | Scales user credits upon SUCCESS of a Session Summary. Invoked after the last artifact of the session is finalized with success. Increments `MonthlyCreditsUsed` by the tool's `CreditCost` and writes a `quota_history` row with `cost_type = 'session_summary'`. See DDD-141. | `apps/backend/src/lib/adapters/` | canonical |
| RecordArtifactSuccess | Command | Increments `ArtifactGateUsed` by 1 upon SUCCESS of each artifact. Invoked after each artifact finalization. Writes a `quota_history` row with `cost_type = 'artifact'`. If the gate is exceeded, the command fails and the artifact is not finalized. See DDD-142. | `apps/backend/src/lib/adapters/` | canonical |
| Project | Entity | A named workspace owned by a User that groups related Artifacts. | `packages/infra-db/migrations/20260424_000001_generation_adapters_minimal.sql` (table `projects`), `apps/backend/src/lib/types/projects.ts` | canonical |

---

### Frontend / UI Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| ToolPage | Aggregate Root | The frontend page that manages the full lifecycle of a tool generation session, from project selection to multi-step generation completion. | `apps/frontend/src/features/tools/machines/tool-page.machine.ts` | canonical |
| ToolPageViewModel | Value Object | The derived read model exposed to React components, combining readiness, canonical UI state, primary action policy, and secondary action flags. | `apps/frontend/src/features/tools/machines/tool-page.machine.ts:57-61` | canonical |
| ReadinessSnapshot | Value Object | A computed snapshot indicating whether a ToolPage can start generation, with typed reason codes for any blocking condition. `hasExtractionContext` is satisfied only when extraction context passes deterministic tool-aware validity checks. For `BriefingUpload`-driven context this includes non-empty `normalizedText` and actionable extraction payload; for `HydrationResult` legacy relaunch context, non-empty `normalizedText` remains mandatory while empty payload can still be accepted when hydration source data is sparse. Readiness required extraction fields are governed by the per-tool canonical map `ReadinessRequiredExtractionFieldKeysByTool` (map-driven policy, DDD-080). Current `youtube-lf-script` baseline keys remain `knowledge_content`, `avatar`, `pain_point`, `offer`, `proof` (DDD-043). Invalid extraction attempts must keep readiness blocked until a new valid upload succeeds (DDD-064). | `apps/frontend/src/features/tools/machines/tool-page.machine.ts:43-50`, `apps/frontend/src/features/tools/machines/tool-page.machine.ts:365-391`, `apps/frontend/src/features/tools/machines/extraction-context-validity.ts:1-57`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ReadinessReasonCode | Value Object | A typed code explaining why a ToolPage is not ready to start. Values: `missing_project`, `missing_extraction_context`, `missing_primary_target_step`. | `apps/frontend/src/features/tools/machines/tool-page.machine.ts:37-41` | canonical |
| CanonicalToolUiState | Value Object | The canonical enumeration of UI states for a ToolPage, used to drive conditional rendering without ad-hoc flags. Values: `draft-empty`, `processing-briefing`, `draft-ready`, `prefilled-regenerate`, `paused-with-checkpoint`, `resume-needs-briefing`, `running`, `completed`. | `apps/frontend/src/features/generation/ui/tool-ux-state.ts` | canonical |
| PrimaryActionPolicy | Value Object | Policy object controlling the label, enabled state, and action of the primary CTA on the tool page. Values: `disabled`, `start-generation`, `resume-checkpoint`, `open-last-artifact`, `regenerate-current-step`. | `apps/frontend/src/features/generation/ui/tool-ux-state.ts` | canonical |
| SecondaryActionFlags | Value Object | Boolean flags determining which secondary CTAs are visible on the tool page. Fields: `canRetry`, `canSkipStep`, `canCancelGeneration`, `canOpenPreviousArtifact`. | `apps/frontend/src/features/generation/ui/tool-ux-state.ts` | canonical |
| SupportedTool | Value Object | The Frontend-layer projection of `ToolKey` (DDD-029, cross-context canonical). Represents the subset of `ToolKey` values that have a live Frontend implementation. Values: `funnel-pages`, `nextland`, `youtube-lf-script`, `angle-generator`, `youtube-description`, `geometric` (DDD-117, kebab-case). Canonical within the Frontend/UI context; the authoritative cross-context term remains `ToolKey`. | `apps/frontend/src/features/tools/machines/tool-flow.machine.ts:3`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ToolAvailabilityStatus | Value Object | Frontend tool exposure state used by the registry and UI shell. Values: `enabled-for-all` (tool appears for `member` and `admin`), `disabled-for-all` (tool is hidden for all roles), `enabled-for-admin-only` (tool is visible and executable only for `admin`). This status does not change `SupportedTool` identity or `ToolKey` values; it governs discovery plus access policy and must be enforced by backend tool endpoints with deterministic `403` for unauthorized roles. See DDD-062 and DDD-093. | `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts`, `apps/frontend/src/app/routing/app-router.tsx`, `apps/backend/src/lib/runtime/auth-http/tools-brief-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/tools-orchestrate-handlers.ts` | canonical |
| ToolFormKey | Value Object | Frontend implementation type for the tool form registry lookup key. `keyof typeof toolFormRegistry` in `tool-form-architecture.ts`. Supersedes the old FE `ToolKey` registry type (DDD-011). Implementation detail only — not a domain term. May include provisional/example entries beyond the `SupportedTool` canonical set. Docs must use `SupportedTool` or `ToolKey` when referring to domain concepts; `ToolFormKey` is acceptable only in code-level spec sections describing the form registry pattern. See DDD-029. | `docs/02-design/specifications/frontend-tool-pages-architecture-spec.md`, `apps/frontend/src/features/tools/` form registry | canonical |
| ToolStep | Value Object | A named step within a SupportedTool's generation flow. Values (by tool): `optin`, `quiz`, `vsl` (funnel-pages); `landing`, `thank_you` (nextland); `pre-script-analysis`, `packaging`, `intro-structure`, `body-structure`, `native-cta-embeds`, `outro-structure` (youtube-lf-script, prompt-chat-faithful canonical sequence); `context-and-angle-matrix`, `angle-prioritization`, `creative-activation` (angle-generator, minimal 3-step sequence derived from the approved Angle Detector system prompt workflow); `youtube-description-generation` (youtube-description, single-step canonical flow per DDD-095); `serp-crawling`, `competitor-scoring`, `strategic-reporting`, `unified-report` (geometric, 4-step analysis cycle per DDD-123). | `apps/frontend/src/features/tools/machines/tool-flow.machine.ts:4`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ToolStepStatus | Value Object | Runtime status of a ToolStep. Values: `idle`, `running`, `done`, `error`. | `apps/frontend/src/features/tools/machines/tool-flow.machine.ts:5` | canonical |
| BriefingUpload | Domain Service | The frontend actor managing the lifecycle of a briefing file: upload, extraction, normalization, and recovery from prior extraction artifacts. | `apps/frontend/src/features/tools/machines/briefing-upload.machine.ts` | canonical |
| LlmModelSelector | Value Object | The Frontend/UI term for the `<select>` control that presents the `enabled` entries of `LlmModelCatalog` as selectable options and emits the chosen `LlmModelId` to `ToolPage` form state. Replaces the previous free-text `<input>` for `model`. Pre-populated by consuming `GET /api/models`. Default selected value: `openrouter/auto` (DDD-046 fallback when catalog is empty or endpoint unavailable). See DDD-057. | `apps/frontend/src/features/tools/ui/ToolFormComponents.tsx:96` (current free-text input — to be replaced), `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx:85` | canonical |
| BriefingFile | Value Object | The uploaded primary source file (txt, md, docx) from which extraction context is derived. Under `ToolInputFileRequirementPolicy`, this is the first input file and is always required for tool execution. | `apps/frontend/src/features/tools/machines/briefing-upload.machine.ts` (field `file`), `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts` (`requiredFiles`) | canonical |
| AngleDetectorFile | Value Object | The uploaded market-analysis source file used by `angle-generator` extraction. It is a secondary input file paired with `BriefingFile` for dual-source extraction context assembly, and its requiredness is governed by `ToolInputFileRequirementPolicy` (explicit per-tool setting from second file onward). The extraction LLM job remains a single invocation over the merged context, not two independent extraction jobs. | `apps/frontend/src/features/tools/machines/briefing-upload.machine.ts:15`, `apps/frontend/src/features/tools/runtime/tools-client.ts:52-59`, `packages/contracts/src/index.ts:104-116`, `docs/07-governance/domain-naming-decision-log.md` (DDD-078, DDD-081) | canonical |
| ToolInputFileRequirementPolicy | Policy | Canonical policy for tool input-file requiredness in Frontend Tool Workspace setup. Rule set: if a tool has one file, that file is always required; if a tool has two or more files, only the first file is always required, and each file from the second onward is explicitly classified by tool setting as required or optional. This policy governs file-requiredness semantics only and is independent from extraction-field policies. See DDD-081. | `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts` (`ToolFileInstructionsConfig.requiredFiles`, `toolFileInstructionsRegistry`), `apps/frontend/src/features/tools/runtime/tool-page-selectors.test.ts`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| InputFilePayloadStatus | Value Object | The ordered collection of per-file load statuses for all tool input files that constitute the current `GenerationRequest` payload, rendered as a persistent section in `ToolGenerationFlowVertical` (Workflow Panel) across all flow phases. Each entry: `key` (registry file identifier), `label` (display name), `requiredness` (`always-required \| required-by-tool-setting \| optional-by-tool-setting` per DDD-081), `status` (`todo \| active \| done \| error`), `fileName` (`string \| null`). During monitoring and completion phases the section freezes as a confirmation anchor showing which files were included in the dispatched `GenerationRequest`. Replaces the phase-exclusive "Requisiti" checklist. Prop name: `inputFilePayload: InputFilePayloadStatus[]`. See DDD-082. | `apps/frontend/src/features/tools/ui/ToolGenerationFlowVertical.tsx`, `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx`, `docs/07-governance/domain-naming-decision-log.md` (DDD-082) | canonical |
| WorkflowPanelFeedbackItem | Value Object | A single aggregated feedback message rendered in the `WorkflowPanelFeedback` section of `ToolGenerationFlowVertical`. Shape: `{ id: string, severity: 'error' \| 'info', message: string, source?: string }`. Aggregated in `ToolPageTemplate` from 7 priority-ordered sources in the `workflowPanelFeedback` prop. Belongs to the `inline-action` feedback channel (DDD-063). Does not carry `DispatchError` (DDD-061 ownership boundary). See DDD-083. | `apps/frontend/src/features/tools/ui/ToolGenerationFlowVertical.tsx`, `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx`, `docs/07-governance/domain-naming-decision-log.md` (DDD-083) | canonical |
| ExtractionContext | Value Object | The structured payload extracted by the backend `ExtractionChain` and consumed by Frontend as input context for subsequent generation steps. The FE type mirrors the BE-produced shape — Frontend does not own extraction logic. Source of authority: BE `ExtractionChain`; FE type definition in `frontend-stream.machine.ts` is a projection/consumer type. Baseline extraction derives from `BriefingFile`; for `angle-generator`, extraction derives from dual-source context assembly (`BriefingFile` + `AngleDetectorFile`) executed as a single extraction LLM job. For `youtube-lf-script`, canonical fields are: `knowledge_content`, `avatar`, `pain_point`, `purchase_process_type`, `offer`, `proof`, `tone`, `target_duration_minutes`, `proprietary_methodology_disclosure`; missing fields must be extracted as `null` (DDD-042). Field naming governance is deterministic: extraction payload properties use canonical `ExtractionFieldKey` (English snake_case) while UI-facing text uses `ExtractionFieldLabel`; mixed label/key lists are non-canonical (DDD-079). The `tone` field inside `ExtractionContext` is briefing-derived business context and must not be conflated with the operational extraction request tone or with the user-selected `ToneProfile` used by generation steps. | `apps/backend/src/lib/machines/extraction-chain.machine.ts` (BE producer), `apps/frontend/src/features/generation/machines/frontend-stream.machine.ts:21` (FE consumer type), `apps/frontend/src/features/tools/runtime/tools-client.ts:226`, `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts:128-176`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ExtractionFieldKey | Value Object | Canonical identifier for one extraction payload field inside `ExtractionContext`, or for a direct-input form field for direct-input-only tools. Must be English snake_case and stable across tools for the same concept. This key is contract-facing and must be used in payloads, validation logic, and readiness rules. Canonical runtime catalog (DDD-079): `avatar`, `base_query` (GEOMETRIC direct-input, DDD-125), `brand_or_company`, `country` (GEOMETRIC direct-input, DDD-125), `creative_constraints`, `funnel_goal`, `goal`, `knowledge_content`, `language` (GEOMETRIC direct-input, DDD-125), `market`, `offer`, `offer_or_service`, `pain_point`, `primary_cta`, `product_or_service`, `proof`, `proprietary_methodology_disclosure`, `purchase_process_type`, `required_sections`, `target_audience`, `target_duration_minutes`, `tone`, `website_goal`. Per-tool required key sets are governed through two distinct canonical maps: `InstructionRequiredExtractionFieldKeysByTool` (UI guidance) and `ReadinessRequiredExtractionFieldKeysByTool` (generation gating), as defined by DDD-080. | `packages/contracts/src/extraction-fields.ts`, `apps/frontend/src/features/tools/runtime/extraction-field-matrix.ts`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ExtractionFieldLabel | Value Object | Localized UI projection of an `ExtractionFieldKey` for display in Tool Workspace Page guidance. Labels are presentation-only and must not replace `ExtractionFieldKey` in payload contracts. Example: `primary_cta` -> `CTA principale` (it-IT UI copy). | `apps/frontend/src/features/tools/runtime/extraction-field-matrix.ts`, `apps/frontend/src/features/tools/ui/ToolFileInstructionsSection.tsx` | canonical |
| InstructionRequiredExtractionFieldKeysByTool | Policy | Canonical per-tool map of extraction field keys required for instruction guidance rendering in Tool Workspace Page setup UX. This policy controls UI guidance semantics only and must not directly define generation gating behavior. | `packages/contracts/src/extraction-fields.ts`, `docs/07-governance/domain-naming-decision-log.md` (DDD-080) | canonical |
| ReadinessRequiredExtractionFieldKeysByTool | Policy | Canonical per-tool map of extraction field keys required by readiness gating (`ReadinessSnapshot`). This policy is map-driven and extensible; updating required keys for a tool must be done through DDD governance without introducing fixed-count hardcoded machine logic. Current baseline: `youtube-lf-script` requires `knowledge_content`, `avatar`, `pain_point`, `offer`, `proof`; `meta-ads` keeps context-not-empty gating with empty required-key set; `geometric` requires `base_query`, `language`, `country` (direct-input fields per DDD-125); other supported tools keep actionable-payload gating with empty required-key lists. | `packages/contracts/src/extraction-fields.ts`, `docs/07-governance/domain-naming-decision-log.md` (DDD-043, DDD-080) | canonical |
| HydrationResult | Value Object | The complete state snapshot produced when entering a ToolPage from an existing Artifact. Resolution is deterministic by source `ArtifactType`: `extraction` artifacts are hydrated directly, while `content` artifacts hydrate by resolving the referenced extraction context (`briefingId` and/or `extractionArtifactId`). Completeness is mandatory: a valid `HydrationResult` for artifact-driven relaunch must recover `extractionArtifactId`, `briefingId`, non-empty normalized briefing text sourced from the artifact's `briefingText`, and a structured `extractionPayload` resolved through the canonical extraction fallbacks. Local FE hydration and `/api/tools/hydrate` must follow the same semantic contract. **Ranking governance** (DDD-075): when multiple extraction artifacts exist for a project and the request specifies `resolvedBriefingId`, the `/api/tools/hydrate` endpoint filters candidates by briefing coherence **before** applying sourceExtractionArtifactId exact match and recency ranking. This enforces the semantic pairing of ExtractionContext ↔ Briefing (DDD-007, DDD-038, DDD-075). See DDD-038, DDD-075. | `apps/frontend/src/features/tools/machines/tool-page.machine.ts:22-30`, `apps/frontend/src/features/tools/machines/tool-page.machine.ts:523-606`, `apps/frontend/src/features/generation/runtime/step-hydration.ts:154-199`, `apps/backend/src/lib/runtime/auth-http/tools-hydrate-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/tools-hydration-parser.ts`, `docs/07-governance/domain-naming-decision-log.md` (DDD-075) | canonical |
| StepHydration | Client-Side Projection | The process of projecting prior `WorkflowStep` completion state — owned and persisted by the backend — into local FE context to enable resume/regenerate flows. Implemented as a set of pure read functions over `GenerationArtifact` history (`collectCompletedStepsByTool`, `buildLatestArtifactByStep`, `readExtractionPayloadFromArtifact`). Does not compute or decide domain logic: all step ordering and dependency resolution authority lives on the backend (`resolveStepDependencyIds`, `/api/tools/orchestrate`). Classified as Client-Side Projection, not Domain Service, to reflect that FE reads and reconstructs BE-owned state without owning it. See DDD-028. | `apps/frontend/src/features/generation/runtime/step-hydration.ts` | canonical |
| SessionArtifactGroup | Value Object | Frontend/UI session display projection over a `GenerationSession`, with ordered step artifacts (`stepKey`, `artifactRole`, status, content, timestamps, failure reason). Used for deterministic multi-artifact session rendering and step navigation. See DDD-049. | `apps/backend/src/lib/adapters/session-query.adapter.ts`, `apps/frontend/src/features/tools/runtime/session-client.ts` | canonical |
| SessionSummary | Value Object | Frontend/UI aggregate-listing projection for `GenerationSession` access (standard multi-step tools). Shape: `{ sessionId, projectId, toolKey, status, artifactCount, updatedAt }`. Canonical listing endpoint contract is `GET /api/tools/sessions` returning `SessionSummary[]`. Canonical Frontend navigation namespace for this projection is `sessionsummary`: listing route `/sessionsummary`, aggregate-detail route `/sessionsummary/{sessionId}`. In Project detail navigation, contextual history must be served as `SessionSummary[]` filtered by `projectId` (not as non-aggregated artifact list). During transition, Frontend may derive `SessionSummary` from artifact listing where backend endpoint rollout is pending. Listing primacy is a UX implementation policy only, not a domain invariant. Not interchangeable with `AnalysisSessionSummary` (GEOMETRIC). See DDD-051 and DDD-052. |
| AnalysisSessionSummary | Value Object | **Provisional** — Frontend/UI aggregate-listing projection for `AnalysisSession` access (GEOMETRIC tool). Shape: `{ sessionId, projectId, toolKey, status, artifactCount, updatedAt, baseQuery, language, country }`. Extends `SessionSummary` with GEOMETRIC-specific fields (`baseQuery`, `language`, `country`) essential for user identification of analysis sessions — without these fields a GEOMETRIC session row is indistinguishable by the user. Canonical listing endpoint: `GET /api/tools/sessions?toolKey=geometric` returns `AnalysisSessionSummary[]`. Frontend navigation namespace follows the same `sessionsummary` pattern (DDD-052): listing `/sessionsummary`, detail `/sessionsummary/{sessionId}`. Not interchangeable with `SessionSummary` (standard tools). The listing endpoint must discriminate by `toolKey` when both session types coexist. See DDD-128. | `docs/07-governance/domain-naming-decision-log.md` (DDD-128) | provisional | `apps/frontend/src/features/tools/runtime/session-client.ts:37-44`, `apps/frontend/src/features/tools/runtime/session-client.ts:77-117`, `apps/frontend/src/app/runtime/queries/useSessionsQuery.ts:52-56`, `apps/frontend/src/features/artifacts/ui/SessionsListingSection.tsx:14-40`, `apps/frontend/src/features/projects/pages/ProjectDetailPage.tsx:45-52`, `apps/frontend/src/app/routing/app-router.tsx:101-118` | canonical |
| GenerationArtifact | Value Object | The frontend read model of an artifact as displayed in artifact history, carrying step association and content. Must remain step-recognizable (for example `optin`, `quiz`, `vsl`) so users can relaunch or continue flows with unambiguous target-step intent. Artifact History is non-aggregated and remains under the `artifacts` navigation namespace: listing `/artifacts`, detail `/artifacts/{artifactId}`. | `apps/frontend/src/features/generation/ui/artifact-history.ts`, `apps/frontend/src/app/routing/app-router.tsx:101-118` | canonical |
| ArtifactRelaunch | Concept | The user action that starts a new generation cycle from an existing Artifact by opening the ToolPage in a hydrated ready state (project and extraction context preloaded), regardless of source `ArtifactType`. Domain UX requires one relaunch concept, one effective primary CTA (`regenerate-current-step`), and default runtime intent `regenerate` for artifact-driven relaunch entries. **Lifecycle**: (1) Initial navigation — `intent='regenerate'`, `runRequestPrefix=null` → machine transitions to `prefilled-regenerate` (`CanonicalToolUiState`), CTA resolves to `regenerate-current-step` (`PrimaryActionPolicy`) — not `start-generation`, which is reserved for `intent=new` first-time generation. (2) Run completion — once the user clicks the relaunch CTA and the run finishes all steps, `runRequestPrefix` is non-null and `hasCompletedAllSteps=true` (`isCurrentRunComplete=true`); the `intent='regenerate'` branch is skipped and the machine transitions to `completed` (`CanonicalToolUiState`), CTA resolves to `open-last-artifact`. The `runRequestPrefix` field in `BuildToolPageViewModelInput` is the discriminant that distinguishes initial navigation (historical completion, prefix null) from current-run completion (prefix set by `handlePrimaryAction` before dispatch). Normative authority cross-reference: DDD-020. | `apps/frontend/src/features/generation/ui/artifact-history.ts:28-34`, `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx:225-237`, `apps/frontend/src/features/tools/machines/tool-page.machine.ts` (`buildToolPageViewModel`, `intent === 'regenerate' && !isCurrentRunComplete` branch; `isCurrentRunComplete` computation), `docs/07-governance/domain-naming-decision-log.md` (`DDD-020`) | canonical |
| GenerationWorkspace | Application Service | The React context maintained by `GenerationWorkspaceProvider` that holds per-session generation state shared across all hooks and components on a tool page. Exposes: `extractionByProject: Record<string, ExtractionContext>` (per-project extraction context store), `artifacts: GenerationArtifact[]` (live artifact list), `focusedProjectId`, `isStreamActive`, `streamStatus`, `terminalCompletedStep`, `terminalFailedStep`, plus imperatives `getExtractionContext(projectId)`, `upsertExtractionContext(ctx)` (writes to `extractionByProject` via `EXTRACTION_UPSERTED` on `frontendStreamMachine`), `start(request)`, `cancel()`. Used by `useToolPage` as the single shared state bus between `BriefingUpload` extraction, generation dispatch, and workspace display. See DDD-069. | `apps/frontend/src/features/generation/runtime/GenerationWorkspaceProvider.tsx` | canonical |
| ExtractionContextBridge | Concept | The React effect in `useToolPage` (effect #2b, lines ~140–185) that syncs a ready `BriefingUpload` actor's extraction context into `GenerationWorkspace` via `upsertExtractionContext`. Runs whenever `briefingSnapshot` transitions to `ready` and all four required fields (`briefingId`, `extractionArtifactId`, `normalizedText`, `parsedFormat`) are non-empty. Implements a deterministic idempotency guard: compares all six fields of the candidate context against the current `workspaceExtractionContext` (including JSON-serialized `extractionPayload`) before calling `upsertExtractionContext`, preventing repeated writes on re-renders. Guarantees that `workspaceExtractionContext` is current before effect #7 (generation dispatch) fires. Without this bridge, `startGenerationStep` reads stale or null context even though `ReadinessSnapshot.hasExtractionContext = true`. See DDD-070. | `apps/frontend/src/features/tools/runtime/useToolPage.ts` (lines ~140–185) | canonical |
| FeedbackChannel | Value Object | Canonical channel classification for Frontend/UI user feedback. Values: `inline-action` (action-adjacent, contextual feedback), `page-state` (Data Table View state feedback rendered in page body), `global` (cross-page ephemeral feedback for mutation outcomes). Channel selection is deterministic by UX intent: action-scoped failures remain local, query/list lifecycle states remain in-page, and post-mutation confirmations/errors can be surfaced globally without replacing local context. See DDD-063. | `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx:255-257`, `apps/frontend/src/app/ui/ListingTableSection.tsx:51-57`, `apps/frontend/src/features/admin/pages/AdminUsersPage.tsx:332-333` | canonical |
| PageStateMessage | Value Object | Canonical Frontend/UI feedback primitive family for Data Table View and index/detail page states. Implemented through `LoadingStateMessage`, `ErrorStateMessage`, and `EmptyStateMessage`; rendered in deterministic page-body positions (before or around table body), not as global notifications. | `apps/frontend/src/app/ui/primitives.tsx:103-113`, `apps/frontend/src/app/ui/ListingTableSection.tsx:51-57`, `apps/frontend/src/features/projects/pages/ProjectsListPage.tsx:32-35` | canonical |
| GlobalFeedbackMessage | Value Object | **Provisional** ephemeral Frontend/UI feedback message for cross-page mutation outcomes (for example create/update/delete success or mutation-level failure not tied to a specific form field). Current behavior is fragmented across local page states (`feedbackMessage`, `mutationError`); target governance is a single app-level provider + viewport that preserves `DispatchError` and `PageStateMessage` ownership boundaries. Status remains provisional until app-level runtime standardization is shipped. See DDD-063. | `apps/frontend/src/features/admin/pages/AdminUsersPage.tsx:98-99`, `apps/frontend/src/features/admin/pages/AdminUsersPage.tsx:332-333`, `apps/frontend/src/features/dashboard/pages/DashboardPage.tsx:119` | provisional |
| DispatchError | Value Object | A local React state (`string \| null`) in `useToolPage` holding the human-readable error message displayed to the user when generation cannot proceed or must be force-closed back to `configuring`. This includes `startGenerationStep` returning `false` (generation dispatch failed) and stream terminal failures that do not expose a recoverable `failedStep`. Set in effect #7 before sending `CANCEL_GENERATION` to the machine, and also set by the terminal-stream bridge when a failed run must be unblocked; cleared to `null` by `handlePrimaryAction` before each new dispatch attempt. Surfaces to `ToolPageTemplate` via the `useToolPage` return value and is rendered adjacent to the primary CTA. Decoupled from `ReadinessReasonCode` (which gates readiness before dispatch) and `generationError` (which is machine context). **Translation layer**: `DispatchError` messages are driven by `DispatchErrorReasonCode` via `mapInlineDispatchError` (DDD-149). See DDD-061, DDD-147. | `apps/frontend/src/features/tools/runtime/useToolPage.ts` (`dispatchError` state, line ~72), `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx` (line ~252), `apps/frontend/src/features/tools/runtime/tool-page-runtime-utils.ts` (`mapInlineDispatchError`) | canonical |
| DispatchErrorReasonCode | Value Object | Canonical typed reason code for `DispatchError` message translation. Replaces string-based matching in `mapInlineDispatchError`. Values: `idempotency_conflict` (duplicate request detected by `IdempotencyCoordinator`), `extraction_context_insufficient` (briefing data insufficient for generation, backend `extraction_context_insufficient`), `stream_empty_output` (LLM returned empty output), `terminal_failed` (backend generation terminal failure, prefix match on `terminal_failed:*`), `timeout` (ECONNRESET or long-running timeout), `connection_lost` (proxy connection failure), `unknown` (fallback). **Translation contract**: backend reason strings → `DispatchErrorReasonCode` → localized `DispatchError` message from `appCopy.ui.toolPage.runtimeErrors`. New backend error codes require a new `DispatchErrorReasonCode` entry + glossary update. See DDD-148. | `apps/frontend/src/features/tools/runtime/tool-page-runtime-utils.ts` (`mapInlineDispatchError`), `docs/07-governance/domain-naming-decision-log.md` (DDD-148) | canonical |
| ProductChangelog | Entity | Administrator-authored release communication intended for end users. Represents one changelog entry with title, body, publication metadata, and lifecycle status. Publication capability is restricted to admin principals and consumed read-only by authenticated users. See DDD-065. | `apps/frontend/src/features/admin/routing/admin-guard.tsx:7-9`, `apps/backend/src/lib/runtime/auth-http/admin-feedback-center-handlers.ts`, `apps/backend/src/lib/runtime/auth-http/public-handlers.ts`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| ProductChangelogStatus | Value Object | Lifecycle state of a `ProductChangelog`. Values: `draft`, `published`. `published` requires publication metadata (`publishedAt`, `publishedBy`) and is role-gated to admin principals. See DDD-066. | `docs/02-design/specifications/admin-changelog-and-user-reporting-spec.md`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| UserReport | Entity | User-submitted report/request record used to capture product issues, feature requests, or other feedback. Carries author identity, category, message body, lifecycle status, and optional external issue linkage metadata. See DDD-065. | `apps/frontend/src/app/runtime/feedback-channel-map.ts:1-25`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| UserReportCategory | Value Object | Canonical category classification for `UserReport`. Values: `issue`, `feature-request`, `other`. `issue` and `feature-request` are eligible for optional GitHub publication policy. See DDD-065. | `apps/frontend/src/app/runtime/feedback-channel-map.ts:1-25`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| UserReportStatus | Value Object | Lifecycle state of a `UserReport`. Values: `submitted`, `triaged`, `github-published`, `closed`. `github-published` is reachable only when `UserReportCategory` is eligible for GitHub publication and publication succeeds. See DDD-065. | `apps/frontend/src/app/runtime/feedback-channel-map.ts:1-25`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| GitHubIssueLink | Value Object | External reference attached to a `UserReport` when an eligible category (`issue` or `feature-request`) is published to GitHub. Shape includes repository identifier, issue number, and issue URL. It is an integration projection and not a replacement for local `UserReport` identity. See DDD-065. | `docs/07-governance/domain-naming-decision-log.md` | canonical |
| IssuePublicationPolicy | Policy | Deterministic policy that allows GitHub issue publication for `UserReportCategory = issue` or `feature-request`; `other` remains local to the application backlog. Policy must preserve local report persistence even if external publication fails. See DDD-065. | `apps/frontend/src/features/admin/routing/admin-guard.tsx:7-9`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| FeedbackCenter | Concept | The backend/API delivery boundary that groups `ProductChangelog` and `UserReport` policy plus public/admin HTTP handlers. In this system it names the `auth-http` slice that validates category/status inputs, decides issue-publication eligibility, and exposes changelog/reporting endpoints. It is not a bounded context and does not replace the underlying entity names in contracts. `FeedbackCenterMachine` remains the separate Frontend/UI application-service term. See DDD-072. | `apps/backend/src/lib/runtime/feedback-center-policy.ts:1-60`, `apps/backend/src/lib/runtime/auth-http/public-handlers.ts:1-153`, `apps/backend/src/lib/runtime/auth-http/admin-feedback-center-handlers.ts`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| FeedbackCenterMachine | Application Service | Frontend XState orchestration concept for the changelog/reporting feature boundary. Coordinates publish/read/report/escalate intents, role-gated actions, and channel-consistent feedback states without replacing existing `FeedbackChannel` governance. See DDD-065. | `apps/frontend/src/app/runtime/feedback-channel-map.ts:1-25`, `docs/02-design/specifications/frontend-ui-ubiquitous-language-spec.md`, `docs/07-governance/domain-naming-decision-log.md` | canonical |
| AdminDashboard | Concept | The unified administrative control plane accessible to `AuthUserRole = 'admin'` users. It is a contained feature group within the Frontend/UI context organizing all admin-facing functions: user CRUD (`AdminUsersPage`), LLM model management (`AdminModelsPage`), ApiService catalog and binding management (`AdminApiServicesPage`), changelog publishing (`AdminChangelogPage`), user report triage (`AdminUserReportsPage`), and activity log review (`AdminActivityPage`). Single entry point: route `/admin`; access controlled by `AdminGuard` component. Each admin page follows the canonical `Data Table View` pattern with atomic construction: query hook + toolbar + table + mutations + feedback channels. `AdminDashboard` is not a separable bounded context — it is a Frontend/UI feature group organization. `AdminDashboard` owns no domain logic; it is purely a presentation and navigation concern. See DDD-067. | `plan/refactor-admin-dashboard-frontend-1.md` (implementation plan, completed 2026-05-17), `apps/frontend/src/app/routing/app-router.tsx:101-118` (admin route config), `apps/frontend/src/features/admin/ui/AdminPageContainer.tsx` (shell pattern), `apps/frontend/src/features/admin/routing/admin-guard.tsx` (access control), `apps/frontend/src/features/admin/pages/AdminApiServicesPage.tsx` | canonical |
| DataTableView | Pattern | Canonical Frontend/UI design pattern for list-oriented domain entity display. Required composition: (1) Query hook retrieves ordered entity collection; (2) Toolbar component exposes filters/actions/controls; (3) Presentational table component (`ListingTableSection` primitive) renders rows with deterministic pagination/sorting; (4) Mutation hooks and feedback channels (`FeedbackChannel`) handle CRUD outcomes atomically. Pattern alignment rules: use shared primitives (`Surface`, `TopBar`, `ListingTableSection`); row-level actions emit `inline-action` feedback for immediate outcomes; page-state messages route to `page-state` channel for query/list lifecycle feedback (`LoadingStateMessage`, `ErrorStateMessage`, `EmptyStateMessage`); cross-page mutation confirmations or critical errors route to `global` channel. All admin pages (`AdminUsersPage`, `AdminModelsPage`, `AdminApiServicesPage`, `AdminChangelogPage`, `AdminUserReportsPage`) conform to this pattern. The pattern is established to encourage consistency and prevent divergent list/table implementations. See DDD-068. | `apps/frontend/src/features/admin/pages/AdminUsersPage.tsx` (exemplar: orchestrator with hooks + toolbar + table + mutations), `apps/frontend/src/features/admin/pages/AdminApiServicesPage.tsx`, `apps/frontend/src/features/admin/ui/AdminPageContainer.tsx` (shell primitive), `apps/frontend/src/app/ui/ListingTableSection.tsx` (table primitive), `apps/frontend/src/features/artifacts/ui/SessionsListingSection.tsx` (non-admin exemplar), `docs/02-design/specifications/frontend-ui-ubiquitous-language-spec.md` (page archetype references), `docs/07-governance/domain-naming-decision-log.md` | canonical |
| isFormLocked | Value Object | UI state flag on ToolPageTemplate indicating the form is non-interactive during generation or processing. Named for versatility across multiple non-submission states. | `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx` | canonical |

Operational note for `ExtractionContext`: at artifact-driven relaunch hydration time and at ToolStep dispatch time, the effective context must carry both non-empty `briefingText` and a structured `extractionPayload`, resolved deterministically from extraction artifact content (raw JSON, fenced JSON, envelope fields) with fallback to `sourceRequest.input.extractionPayload`. Readiness must remain blocked while this completeness gate is unsatisfied (`apps/frontend/src/features/tools/runtime/useToolPage.ts:358-453`, `apps/frontend/src/features/generation/runtime/step-hydration.ts:54-138`, `apps/frontend/src/features/tools/runtime/tools-client.test.ts:134-187`, `apps/frontend/src/features/tools/machines/tool-page.machine.ts:98-134`).

---

## Aliases And Deprecated Terms

| Alias/Deprecated Term | Canonical Term | Notes |
| --- | --- | --- |
| `ToolKey` (FE form registry, DDD-011) | `ToolFormKey` | FE form registry lookup type superseded by `ToolFormKey` (DDD-029). `ToolKey` as a standalone FE type is deprecated; `ToolKey` as cross-context canonical term remains valid (DDD-029). |
| `SupportedToolWorkflow` (BE type) | `ToolKey` | BE implementation type in `tool-workflow-registry.ts:10`; treated as impl detail of `ToolKey` (DDD-018 pattern). Not a domain term. |
| `meta_ads` (ToolWorkflow value) | `meta_ads_generator` | Deprecated legacy alias. Reactivation must use canonical `ToolWorkflow = meta_ads_generator` for `ToolKey = meta-ads` (DDD-094, superseding DDD-030). |
| `workflow_type` (DB column) | ToolWorkflow | Snake-case DB column name; canonical code term is `ToolWorkflow`. |
| `artifact_type` (DB column) | ArtifactType | Snake-case DB column name. |
| `monthly_quota` / `monthly_used` (DB columns) | MonthlyQuota / MonthlyCreditsUsed | Mapped to camelCase in TypeScript types. `MonthlyUsed` deprecated in favor of `MonthlyCreditsUsed` (DDD-138). |
| `MonthlyUsed` | `MonthlyCreditsUsed` | Deprecated term (DDD-138). Contatore crediti mensili consumati. Alias backward-compat mantenuto per transizione. |
| `requestIdempotency` (DB table) | IdempotencyCoordinator / IdempotencyKey | Table represents the persistence layer for idempotency logic. |
| `quota_history` (DB table) | QuotaHistory | DB table name; canonical term is `QuotaHistory`. |
| `ToolExtractionContext` | ExtractionContext | Former name in `frontend-stream.machine.ts`. Deprecated DDD-012; backward-compat alias in `tool-form-architecture.ts`. |
| `BriefingContext` | ExtractionContext | Former definition in `tool-form-architecture.ts`. Deprecated DDD-012; replaced with `export type BriefingContext = ExtractionContext`. |
| `ToolPageReadinessSnapshot` | ReadinessSnapshot | Former name in `tool-page.machine.ts`. Deprecated DDD-014; alias removed in 2026-05-03 quality audit. |
| `ToolPageReadinessReasonCode` | ReadinessReasonCode | Former name in `tool-page.machine.ts`. Deprecated DDD-014; alias removed in 2026-05-03 quality audit. |
| `ToolRegistryVersion` | RegistryVersion | Former name in `apps/backend/src/lib/types/xstate.ts`. Deprecated DDD-015; backward-compat alias `ToolRegistryVersion = RegistryVersion` maintained. |
| `ToolRegistrySnapshotRef` | RegistrySnapshotRef | Former name in `apps/backend/src/lib/types/xstate.ts`. Deprecated DDD-015; backward-compat alias maintained. |
| `StreamUsageMetrics` | LlmUsageMetrics | Former interface in `xstate.ts`. Deprecated DDD-016; backward-compat alias `StreamUsageMetrics = LlmUsageMetrics` maintained. |
| `PersistedArtifactStatus` | ArtifactStatus | Former type in `generation.adapters.ts`. Deprecated DDD-017; backward-compat alias `PersistedArtifactStatus = ArtifactStatus` maintained. |
| `hl_funnel` (ToolWorkflow value) | `funnel-pages` | Legacy Frontend naming (`hl` = HotLeadFunnel); normalized to canonical `funnel-pages` (kebab-case, cross-context ToolKey). Silently mapped by `normalizeToolWorkflowKey` in `generation-system.machine.ts`. Documented as deprecated in DDD-030 family (no Frontend `SupportedTool` counterpart). Do not use in new code. |
| `youtube-long-form` (ToolKey alias) | `youtube-lf-script` | Deprecated alias kept only for backward-compat migration references. Canonical ToolKey is `youtube-lf-script` (DDD-040). |
| `youtube_long_form` (ToolWorkflow alias) | `youtube_lf_script` | Deprecated alias. Canonical workflow type is `youtube_lf_script` (DDD-040). |
| `relaunchPrimary` / `relaunchSecondary` (UI copy keys) | ArtifactRelaunch | Deprecated UI copy split for a single domain concept. Canonical UL keeps one `ArtifactRelaunch` concept, effective CTA: `regenerate-current-step` (in stato `prefilled-regenerate`), and default runtime intent `regenerate` for artifact-driven relaunch entries. |
| `/artifacts/{sessionId}` (session-detail overload) | `/sessionsummary/{sessionId}` | Deprecated transitional route behavior. Session aggregate detail belongs to the `sessionsummary` navigation namespace (DDD-052); `artifacts` remains reserved for non-aggregated artifact history and single generation detail. |
| `ToolUiDerivationInput` / `ToolUiDerivationOutput` | — (removed) | Deprecated types in `apps/frontend/src/features/tools/runtime/tool-ux-state.ts`. Removed in Sprint 4 / DDD-024; no runtime consumers existed. Canonical state is `toolPageMachine.context.viewModel`. |
| `deriveCanonicalToolUiState` (tools/runtime shim) | `deriveCanonicalToolUiState` (generation/ui) | Deprecated overload in `tools/runtime/tool-ux-state.ts`. Removed in Sprint 4 / DDD-024. Use canonical source in `apps/frontend/src/features/generation/ui/tool-ux-state.ts`. |
| `useStepSelection` | — (removed) | Unused hook in `useToolForm.ts`. Removed in Sprint 4 / DDD-024; had no runtime consumers. |
| `useToolUiState` | — (removed) | Test-only hook in `useToolForm.ts`. Removed in Sprint 4 / DDD-024; was mocked in tests only, not imported by any runtime component. |
| `PROGRESS_SYNCED.hasExtractionContext?` / `PROGRESS_SYNCED.hasPrimaryTargetStep?` | — (removed) | Deprecated optional event payload fields in `tool-page.machine.ts`. Removed in Sprint 4 / DDD-024; machine derives them internally via `deriveHasExtractionContext`/`deriveHasPrimaryTargetStep`. |
| `adminModels` (BackendCapabilities) | — (removed) | Frontend capability flag for `GET /api/admin/models`. Removed in Sprint 3 / DDD-024; endpoint not implemented in backend runtime. |
| `listAdminModels` / `listAdminActivity` | — (removed) | Dead admin client functions. Removed in Sprint 3 / DDD-024; no runtime consumers. |
| `feedbackMessage` (local page state) | GlobalFeedbackMessage | Transitional local variable name used in `AdminUsersPage`; canonical UL term for cross-page mutation feedback is `GlobalFeedbackMessage` (provisional, DDD-063). |

---

## Evidence Anchors

Key source files referenced by this glossary:

- `apps/backend/src/lib/types/xstate.ts`
- `apps/backend/src/lib/types/artifact.ts`
- `apps/backend/src/lib/types/auth.ts`
- `apps/backend/src/lib/types/api-service.ts`
- `apps/backend/src/lib/adapters/generation.adapters.ts`
- `apps/backend/src/lib/adapters/api-service.adapter.ts`
- `apps/backend/src/lib/machines/generation-system.machine.ts`
- `apps/backend/src/lib/machines/tool-workflow.machine.ts`
- `apps/backend/src/lib/machines/extraction-chain.machine.ts`
- `apps/backend/src/lib/machines/idempotency-coordinator.machine.ts`
- `apps/backend/src/lib/machines/stream-transport.machine.ts`
- `apps/backend/src/lib/machines/persistence-batch.machine.ts`
- `apps/backend/src/lib/machines/request-gateway.machine.ts`
- `apps/backend/src/lib/machines/usage.machine.ts`
- `apps/backend/src/lib/runtime/tool-workflow-registry.ts`
- `apps/backend/src/lib/runtime/auth-http/route-table.ts`
- `apps/backend/src/lib/runtime/auth-http/tools-orchestrate-handlers.ts`
- `apps/backend/src/lib/runtime/auth-contract.ts`
- `apps/backend/src/lib/runtime/request-contract.ts`
- `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts`
- `apps/frontend/src/features/tools/runtime/useToolPage.ts`
- `apps/frontend/src/features/tools/runtime/tool-page-selectors.ts`
- `apps/frontend/src/features/tools/machines/tool-page.machine.ts`
- `apps/frontend/src/features/tools/machines/tool-flow.machine.ts`
- `apps/frontend/src/features/tools/ui/ToolPageTemplate.tsx`
- `apps/frontend/src/features/generation/contracts/backend-stream.ts`
- `apps/frontend/src/features/generation/runtime/step-hydration.ts`
- `packages/contracts/src/tool-workflows.ts`
- `packages/contracts/src/index.ts`

## Blog Article Generator Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| blog-article-generator | ToolKey | Cross-context canonical identifier for the Blog Article Generator tool. Follows kebab-case convention per DDD-029. Tool implements 3-step workflow: SEO structure generation → research → article composition using specialized LLM models for optimal output quality. | `docs/07-governance/domain-naming-decision-log.md` (DDD-155) | canonical |
| blog_article_generator | ToolWorkflow | Generation context routing identifier for blog article generation workflow. Follows snake_case convention per DDD-C-005. Maps to 3-step orchestration: `blog_seo_structure` → `blog_research` → `blog_article`. | `docs/07-governance/domain-naming-decision-log.md` (DDD-155) | canonical |
| BlogArticleStep | Value Object | Step identification Value Object for blog generation workflow. Union type: `'blog_seo_structure' \| 'blog_research' \| 'blog_article'`. Step sequence is deterministic and ordered. Each step represents distinct generation phase with specific LLM model, temperature, and prompt configuration. Snake_case convention matches backend `WorkflowStep` identifier requirements. | `docs/07-governance/domain-naming-decision-log.md` (DDD-156) | canonical |
| blog_seo_structure | ToolStep | First step of blog article generation workflow. Generates SEO-optimized article structure with H1, H2 headers and source citations using `gpt-4o-mini-search-preview` model. Output serves as structural foundation for subsequent research and writing steps. Step type: `generation`. | `docs/07-governance/domain-naming-decision-log.md` (DDD-156) | canonical |
| blog_research | ToolStep | Second step of blog article generation workflow. Conducts comprehensive topical research with detailed, structured information using `gpt-5-search-api` model. Consumes SEO structure from step 1 and produces research data for article composition. Step type: `generation`. | `docs/07-governance/domain-naming-decision-log.md` (DDD-156) | canonical |
| blog_article | ToolStep | Final step of blog article generation workflow. Composes complete 800-word professional article using `gpt-5.2` model. Consumes both SEO structure and research data from previous steps. Produces final downloadable artifact with `ArtifactRole = 'final'`. Step type: `generation`. | `docs/07-governance/domain-naming-decision-log.md` (DDD-156) | canonical |

---

## Brief Generator Context

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| brief-generator | ToolKey | Cross-context canonical identifier for the Brief Generator tool. Follows kebab-case convention per DDD-029. Primitive single-step tool: user uploads a raw file (`.txt`, `.md`, or `.docx`), the system extracts structured context, performs LLM-driven generation, and promotes the output as a `'brief'` Asset with `AssetSource = 'generated'`. Produces structured Markdown output (Italian) covering five extraction fields: `product_or_service`, `target_audience`, `campaign_objective`, `primary_offer`, `tone`. CreditCost: 1. Availability: `enabled-for-all`. See DDD-210. | `docs/07-governance/domain-naming-decision-log.md` (DDD-210) | canonical |
| brief_generator | ToolWorkflow | Generation context routing identifier for brief generator workflow. Follows snake_case convention per DDD-C-005. Single-step orchestration: `brief-generation`. | `docs/07-governance/domain-naming-decision-log.md` (DDD-210) | canonical |
| Brief Generator | DisplayLabel | User-facing display label for the `brief-generator` tool. Follows Title Case convention for UI copy (navbar, tool hub, tool page header). Distinct from `ToolKey = 'brief-generator'` and `ToolWorkflow = 'brief_generator'` — the DisplayLabel is a presentation-layer concern only and must not replace canonical identifiers in code, contracts, or routes. See DDD-210. | `docs/07-governance/domain-naming-decision-log.md` (DDD-210) | canonical |
| brief-generation | ToolStep | Single step of the Brief Generator workflow. Extracts structured brief content from an uploaded file via `BriefingUpload` extraction, then generates structured Markdown output covering five extraction fields. Step type: `generation`. `feedbackEnabled: true` — final output eligible for user feedback. | `docs/07-governance/domain-naming-decision-log.md` (DDD-210) | canonical |

---

## Asset Domain — Project Workspace

The following terms define the **Asset** domain model, evolving the system from monofunctional tools producing session-scoped `Artifact`s to a networked project workspace where `Asset`s are first-class, cross-tool reusable project property.

Core semantic distinction: **`Artifact` = content produced in the Project** (generation output record, session-scoped, DDD-001). **`Asset` = property of the Project** (persistent resource, project-scoped, DDD-188).

### Base Asset Model

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| Asset | Entity | A persistent project resource that can be used as input across multiple Tools. Originates from a completed `Artifact` promoted by the user (`AssetSource = 'generated'`), an external file upload (`'uploaded'`), or manual user creation (`'manual'`). Core fields: `assetId`, `projectId` (FK to `Project`), `assetType: AssetType`, `source: AssetSource`, `sourceArtifactId` (nullable, set only for `'generated'`), `status: AssetStatus`, `content`, `label`, `createdAt`, `updatedAt`. Lifecycle is independent from `GenerationSession` — persists across sessions and survives project archival. See DDD-188. | `docs/07-governance/domain-naming-decision-log.md` (DDD-188) | canonical |
| AssetReference | Value Object | A reference to an Asset used as input in `GenerationRequest`. Shape: `{ assetId?, assetGroupId?, sourceToolKey: ToolKey, usageIntent: 'input' \| 'injection' }`. `assetId` and `assetGroupId` are xor (mutually exclusive). `sourceToolKey` records the Tool that originally produced the Asset. `usageIntent = 'input'` means primary input context; `'injection'` means injected into a specific step via `AssetInjectionDirective`. Carried in `GenerationRequest.input.assetReferences`. See DDD-189. | `docs/07-governance/domain-naming-decision-log.md` (DDD-189) | canonical |
| AssetSource | Value Object | Origin classification of an Asset. Values: `'generated'` (promoted from a completed `Artifact` — carries `sourceArtifactId`), `'uploaded'` (file uploaded directly as project resource), `'manual'` (user-created inline content). Immutable after creation. See DDD-190. | `docs/07-governance/domain-naming-decision-log.md` (DDD-190) | canonical |
| AssetStatus | Value Object | Lifecycle state of an Asset. Values: `'active'` (selectable as input in Tools, visible in project asset browser), `'archived'` (preserved for audit and existing references but hidden from input selectors). Archival is reversible. Mirrors `ArtifactStatus` pattern (DDD-017) with distinct semantics. See DDD-191. | `docs/07-governance/domain-naming-decision-log.md` (DDD-191) | canonical |
| AssetInjectionDirective | Value Object | Specification for injecting Asset content into a specific `WorkflowStep` during prompt assembly. Shape: `{ assetId, stepKey, injectionMode: 'prepend' \| 'append' \| 'replace', fieldMappingKey? }`. `fieldMappingKey` references an `AssetFieldMapping` (DDD-207) entry for structured extraction. Multiple directives can target the same step, processed in order. See DDD-193. | `docs/07-governance/domain-naming-decision-log.md` (DDD-193) | canonical |
| AssetGroup | Entity | A named collection of Assets within a Project. Fields: `groupId`, `projectId` (FK), `label`, `assetIds: string[]` (ordered), `groupUsage: AssetGroupUsage`. Referenced via `AssetReference.assetGroupId`. An Asset can belong to multiple groups; deleting a group does not delete its members. See DDD-194. | `docs/07-governance/domain-naming-decision-log.md` (DDD-194) | canonical |
| AssetGroupUsage | Value Object | Consumption mode for an `AssetGroup`. Values: `'individual'` (Tool iterates over each Asset independently — N outputs) and `'bundled'` (all Assets injected together — 1 combined output). Declared at group creation, mutable. See DDD-195. | `docs/07-governance/domain-naming-decision-log.md` (DDD-195) | canonical |

### Versioning and Derivation

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| AssetVersion | Value Object | Incremental version of an Asset. Each update creates a new version: `{ versionNumber, assetId, content, sourceArtifactId?, createdAt }`. Current version = highest `versionNumber`. Past versions retained immutably. Consumer `AssetReference` resolves to current version at dispatch time (snapshot semantics). See DDD-196. | `docs/07-governance/domain-naming-decision-log.md` (DDD-196) | canonical |
| AssetDerivationChain | Entity | DAG tracking genealogical relationships between Assets. Fields: `upstreamAssetId`, `upstreamVersion`, `downstreamAssetId`, `toolKey` (Tool that performed the derivation), `sessionId`, `createdAt`. Populated automatically when a `GenerationRequest` carries `AssetReference`s and completes successfully. See DDD-197. | `docs/07-governance/domain-naming-decision-log.md` (DDD-197) | canonical |
| AssetStalenessPolicy | Policy | Rules for how downstream Assets react to upstream version changes: (1) upstream versioning marks downstreams with `staleUpstream` flag; (2) stale Assets show warning but remain functional; (3) user can dismiss warning or regenerate; (4) staleness never auto-resolves; (5) archived/deleted upstream → permanent staleness. See DDD-198. | `docs/07-governance/domain-naming-decision-log.md` (DDD-198) | canonical |

### Typing and Compatibility

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| AssetType | Value Object | Semantic content type classification of an Asset. Canonical values: `angle`, `persona`, `brand-voice`, `hook`, `competitor-analysis`, `creative-brief`, `brief`, `ad-copy`, `landing-page`, `article-outline`, `article`, `script`, `description`. Authority: `packages/contracts`. Bridges producing and consuming Tools via `ToolAssetContract` (DDD-200). Extensible through DDD governance. See DDD-199, DDD-208, DDD-209. | `docs/07-governance/domain-naming-decision-log.md` (DDD-199) | canonical |
| `brief` (AssetType) | AssetType value | A general-purpose structured creative brief document (Markdown) serving as authoritative input for generation Tools. Covers five core domains: product or service description (`product_or_service`), target audience profile (`target_audience`), campaign objective (`campaign_objective`), primary offer (`primary_offer`), and communication tone (`tone`). Source paths: `'uploaded'` (user-provided raw content via file upload) or `'generated'` (produced by `brief-generator` tool, DDD-210). Consumed by five Tools: `funnel-pages`, `nextland`, `youtube-lf-script`, `angle-generator`, `meta-ads` (all declared in `TOOL_ASSET_CONTRACTS`). Distinct from `'creative-brief'` (DDD-209): `'brief'` targets campaign/production tools, while `'creative-brief'` targets editorial/content-creation tools (`blog-article-generator`). See DDD-208, DDD-209. | `packages/contracts/src/asset.ts` (`ASSET_TYPES`, `TOOL_ASSET_CONTRACTS`), `docs/07-governance/domain-naming-decision-log.md` (DDD-208) | canonical |
| ToolAssetContract | Policy | Per-`ToolKey` declaration of `produces: AssetType[]` and `consumes: AssetType[]`. Authority: `packages/contracts/src/tool-workflows.ts`. `consumes` entries may be `required` or `optional`. Enables `AssetCompatibilityMatrix` (DDD-201) and `AssetGapDetection` (DDD-203). See DDD-200. | `docs/07-governance/domain-naming-decision-log.md` (DDD-200) | canonical |
| AssetCompatibilityMatrix | Domain Service | Derives cross-tool compatibility from `ToolAssetContract` declarations. Queries: `getCompatibleConsumerTools(assetType)`, `getCompatibleAssetTypes(toolKey)`, `getCompatibleAssets(projectId, toolKey)`, `getToolProductionChain(fromTool, toTool)`. Pure function over static contracts — no DB access. See DDD-201. | `docs/07-governance/domain-naming-decision-log.md` (DDD-201) | canonical |

### Discovery and Quality

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| AssetDiscovery | Domain Service | Finds compatible Assets for a Project+Tool. Queries `AssetCompatibilityMatrix` + project Assets, returns results ordered by quality score descending, then recency. Exposed via `GET /api/projects/{id}/assets/compatible?toolKey={key}`. See DDD-202. | `docs/07-governance/domain-naming-decision-log.md` (DDD-202) | canonical |
| AssetGapDetection | Domain Service | Identifies missing Assets that would improve Tool output. Returns `{ missingAssetTypes: { assetType, canBeProducedBy: ToolKey[] }[] }`. Example: "No Persona asset. Generate one with angle-generator." See DDD-203. | `docs/07-governance/domain-naming-decision-log.md` (DDD-203) | canonical |
| AssetSimilarityScore | Value Object | Semantic similarity measure between two Assets of the same `AssetType`. Range `0.0..1.0`. Used for redundancy detection (score > 0.85 suggests merge/differentiate). Advisory only. See DDD-204. | `docs/07-governance/domain-naming-decision-log.md` (DDD-204) | canonical |
| AssetQualityScore | Value Object | Composite quality measure (`0..100`). Weighted factors: `feedbackScore` (40%, from `GenerationFeedback`), `usageScore` (25%, log-scaled usage count), `freshnessScore` (20%, recency decay), `completenessScore` (15%, structural completeness). Recomputed on read, not persisted. See DDD-205. | `docs/07-governance/domain-naming-decision-log.md` (DDD-205) | canonical |
| AssetQualityGate | Policy | Advisory gate based on `AssetQualityScore`: ≥70 silent, 40–69 mild warning, <40 strong warning. Never blocks generation — user always decides. Applied at Asset selection and dispatch time. See DDD-206. | `docs/07-governance/domain-naming-decision-log.md` (DDD-206) | canonical |
| AssetFieldMapping | Value Object | Structured extraction rules for injecting Asset fields into Tool prompts. Per `(assetType, toolKey)` pair: `{ fieldKey → { sourcePath, injectionTemplate, required } }`. Referenced by `AssetInjectionDirective.fieldMappingKey`. Uses Markdown templates with `{{fieldKey}}` placeholders. Authority: `packages/contracts`. See DDD-207. | `docs/07-governance/domain-naming-decision-log.md` (DDD-207) | canonical |

### Extended Concepts

| Term | Type | Definition | Source | Status |
| --- | --- | --- | --- | --- |
| ToolInputSourceFamily | Value Object | **Extended DDD-106.** Adds `'project-asset'` to the existing set (`direct-input`, `tool-input-file`, `api-acquisition`). `project-asset` entries default to `requiredness: optional-by-tool-setting` in `ToolInputRequirementMatrix`. See DDD-192. | `apps/frontend/src/features/tools/runtime/tool-form-architecture.ts`, `docs/07-governance/domain-naming-decision-log.md` (DDD-192) | canonical |

---
