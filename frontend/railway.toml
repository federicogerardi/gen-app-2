[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPaths = ["frontend/**", "package*.json"]

[deploy]
startCommand = "npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10