# apps/backend

Backend application: Node.js + XState generation system.

**Workspace package name**: `@gen-app-2/backend`

## Layout (after Phase 3)
- `src/` — Runtime source code (migrated from `/src`)
- `package.json` — Backend workspace scripts
- `tsconfig.json` — TypeScript config

## Entrypoint
- `src/server.ts` — Main backend server

## Phase 1 Status
- Workspace manifest: created 2026-05-06
- Runtime move: scheduled Phase 3 (TASK-011)
