# frontend/.env.example
# Variabili d'ambiente del server frontend Railway (frontend/server.mjs).
# NON sono esposte nel bundle Vite — solo lette a runtime dal processo Node.
# Copiare in .env.local per sviluppo locale.

# URL interno del backend Railway.
# Locale: http://localhost:3000
# Produzione Railway: http://<backend-service>.railway.internal:<port>
BACKEND_INTERNAL_URL=http://localhost:3000

# Feature capabilities (Vite).
# Projects e Artifacts sono core e in runtime hanno fallback enabled,
# ma qui li rendiamo espliciti per chiarezza del template locale.
VITE_CAP_PROJECTS=true
VITE_CAP_MODELS=true
VITE_CAP_ARTIFACTS=true
VITE_CAP_TOOLS_UPLOAD=true

# Porta del server frontend (impostata automaticamente da Railway in produzione).
# PORT=3000
