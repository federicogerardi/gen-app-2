---
status: active
version: 2.1
last-reviewed: 2026-05-07
next-review-date: 2026-08-03
owner: Domain Architecture
---

# Domain Naming Decision Log

## Decision Rules
- Every new canonical term must be logged before broad reuse.
- Decisions must include rationale and propagation scope.
- Conflicting synonyms must be resolved here first.

---

## Approved Naming Decisions

| ID | Date | Canonical Term | Decision | Rationale | Scope |
| --- | --- | --- | --- | --- | --- |
| DDD-001 | 2026-05-03 | Artifact | `Artifact` is the canonical term for a persisted generation output. Not `Output`, `Result`, `Generation`, or `Document`. | `artifact` is used consistently in DB schema (`artifacts` table), TypeScript types (`ArtifactType`, `ArtifactStatus`), and frontend contracts. Renaming would require broad migration. | all contexts |
| DDD-002 | 2026-05-03 | GenerationRequest | `GenerationRequest` is the canonical term for the command that initiates generation. Not `GenerationInput`, `GenerationPayload`, or `CreateGenerationDto`. | Consistent with `requestId` as the primary correlation identifier throughout the system. | Generation, Frontend |
| DDD-003 | 2026-05-03 | WorkflowStep | `WorkflowStep` is the canonical backend term for a step in a multi-step generation flow. Not `Task`, `Stage`, or `Phase`. | Backend uses `WorkflowStepDescriptor` and `WorkflowStepState` consistently. | Generation |
| DDD-004 | 2026-05-03 | ToolStep | `ToolStep` is the canonical frontend term for a named step in a specific tool's flow (e.g., `optin`, `vsl`, `landing`). Coexists with `WorkflowStep` as the frontend concrete counterpart. | Frontend tool machines use `ToolStep` as a union type of actual step names; backend `WorkflowStep` is the abstract descriptor. Kept distinct to avoid confusion. See DDD-C-001. | Frontend |
| DDD-005 | 2026-05-03 | ClaimUsage | `ClaimUsage` is the canonical command name for the atomic quota reservation operation. Not `DecrementQuota`, `ConsumeQuota`, or `CheckAndReserve`. | `claimUsage` is used in `UsageAdapter` interface and `usageMachine` actor consistently. | Usage/Quota |
| DDD-006 | 2026-05-03 | ReadinessSnapshot | `ReadinessSnapshot` (not `ReadinessState` or `CanStartFlags`) is the canonical term for the computed start-eligibility object on the tool page. | Used in `tool-page.machine.ts` and documented in `tool-generation-flow-source-of-truth-spec.md`. Deprecated aliases `ToolPageReadinessSnapshot`, `ToolPageReadinessReasonCode` (see DDD-014) have been removed. | Frontend |
| DDD-007 | 2026-05-03 | ExtractionContext | `ExtractionContext` is the canonical term for the structured payload extracted from a briefing file and used as generation input. Not `BriefingContext`, `ExtractedData`, or `ParsedBriefing`. | Defined in `apps/frontend/src/features/generation/machines/frontend-stream.machine.ts:21`; deprecated aliases `ToolExtractionContext` and `BriefingContext` remain as backward-compat only (see DDD-012). | Frontend, Generation |
| DDD-008 | 2026-05-03 | AuthSessionPrincipal | `AuthSessionPrincipal` is the canonical shared read model passed across Auth → other contexts. Not `CurrentUser`, `LoggedInUser`, or `SessionUser`. | Defined explicitly as `AuthSessionPrincipal` in `src/lib/types/auth.ts:46`. | Auth |
| DDD-009 | 2026-05-03 | BackendStreamEvent | `BackendStreamEvent` is the canonical term for SSE events emitted during a generation. Types: `start`, `chunk`, `terminal`. | Defined in `packages/contracts/src/index.ts` (shared contract). Consistent with SSE protocol naming. | Generation, Frontend |
| DDD-010 | 2026-05-03 | HydrationResult | `HydrationResult` is the canonical term for the state snapshot produced when loading prior artifact history into a ToolPage. Not `ResumeState`, `CheckpointData`, or `SessionSnapshot`. | `HydrationResult` is the explicit type in `tool-page.machine.ts` and aligns with `StepHydration` service naming. | Frontend |
| DDD-011 | 2026-05-03 (revised 2026-05-04) | ToolKey (FE form registry → ToolFormKey) | ~~`ToolKey` is the registry implementation type (`keyof typeof toolFormRegistry`) in `tool-form-architecture.ts`.~~ **Revised (DDD-029)**: The FE form registry implementation type is renamed `ToolFormKey`. `ToolKey` is no longer a standalone FE domain term — it is promoted to cross-context canonical identifier for Tool identity. Docs must use `SupportedTool` or `ToolKey` (cross-context) when referring to domain concepts; `ToolFormKey` is acceptable only in code-level spec sections describing the form registry. | `ToolKey` as both a FE-specific registry type and a Generation step-orchestration identifier created a naming collision (DDD-011 vs DDD-025). Resolution: `ToolKey` becomes the single cross-context canonical term; the FE form registry implementation type takes the distinct name `ToolFormKey`. Superseded by DDD-029. | Frontend (superseded by DDD-029) |
| DDD-018 | 2026-05-03 | RegistryBackedWorkflowType / RegistryBackedArtifactType / RegistryBackedToolKey | **Declassified as internal implementation type** — not for domain documentation or canonical glossary entries. These three open-union types in `src/lib/types/xstate.ts` (lines 5–7) extend canonical value objects (`ToolWorkflow`, `ArtifactType`) with `string & {}` to accommodate registry-driven entries not present in the canonical enumerations at compile time. They must not appear in domain documentation or glossary entries as primary terms. The canonical terms (`ToolWorkflow`, `ArtifactType`, `ToolKey`) remain the authoritative names for domain communication. | These types surface in `GenerationSystemContext` and `generation-system.machine.ts` where the registry may inject values unknown at compile time. Declassification confirmed 2026-05-03: permanent infrastructure-layer widening types, not candidates for promotion to first-class domain concepts. | Generation |
| DDD-019 | 2026-05-03 | toolStepOrder | `toolStepOrder` is the single canonical constant mapping each `SupportedTool` to its ordered `ToolStep[]`. Defined once in `apps/frontend/src/features/tools/machines/tool-flow.machine.ts` and re-exported by `tool-generation-engine.ts`. Prior to the 2026-05-03 quality audit the constant existed as an independent copy in both files with drift risk. All consumers import from `tool-generation-engine.ts` (re-export) or `tool-flow.machine.ts` directly. | A duplicated constant with identical values is a drift vector. Single source of truth eliminates silent divergence. | Frontend |
| DDD-012 | 2026-05-03 | ExtractionContext | `ExtractionContext` is the canonical term for the structured briefing extraction payload. `ToolExtractionContext` (former name in `frontend-stream.machine.ts`) and `BriefingContext` (former definition in `tool-form-architecture.ts`) were aliases for the same concept; both removed as live definitions and replaced with backward-compat aliases pointing to `ExtractionContext`. | Three names for the same Value Object caused confusion at import sites. `ExtractionContext` aligns with DDD-007 and the canonical glossary entry. | Frontend |
| DDD-013 | 2026-05-03 | ToolStepStatus / `'done'` | `ToolStepStatus` with value `'done'` is the canonical representation of a completed step. The value `'completed'` was a drift in `tool-page.machine.ts`, `tool-ux-state.ts`, and UI components relative to the canonical definition in `tool-flow.machine.ts`. All occurrences updated. `CanonicalToolUiState = 'completed'` and `ArtifactStatus = 'completed'` are unrelated concepts and were not modified. | Canonical source is `tool-flow.machine.ts:5`. All derived types and runtime comparisons must use `'done'`. | Frontend |
| DDD-014 | 2026-05-03 | ReadinessSnapshot / ReadinessReasonCode | `ReadinessSnapshot` and `ReadinessReasonCode` are the canonical terms for the start-eligibility snapshot and its reason codes (see DDD-006). The `ToolPage` prefix was redundant within the `tool-page.machine.ts` namespace. Backward-compat aliases `ToolPageReadinessSnapshot = ReadinessSnapshot` and `ToolPageReadinessReasonCode = ReadinessReasonCode` were introduced for 1 deprecation cycle and **removed in the 2026-05-03 quality audit** — these aliases no longer exist in the codebase. | Prefix removal reduces verbosity without losing precision. | Frontend |
| DDD-015 | 2026-05-03 | RegistryVersion / RegistrySnapshotRef | `RegistryVersion` and `RegistrySnapshotRef` are the canonical terms for the tool registry branding types in `src/lib/types/xstate.ts`. The former `ToolRegistry` prefix was redundant in the `types/` namespace. Backward-compat aliases `ToolRegistryVersion = RegistryVersion` and `ToolRegistrySnapshotRef = RegistrySnapshotRef` maintained in `xstate.ts`. | Consistent with DDD-001 principle of removing context-redundant prefixes. | Generation |
| DDD-016 | 2026-05-03 | LlmUsageMetrics | `LlmUsageMetrics` is the canonical term for the token and cost metrics of a completed generation. The former `StreamUsageMetrics` interface in `xstate.ts` was a structural duplicate; it is now a backward-compat alias `export type StreamUsageMetrics = LlmUsageMetrics`. The definition authority is `src/lib/types/xstate.ts`; `generation.adapters.ts` imports and re-exports it. Backward-compat alias `StreamUsageMetrics` **removal-target: 2026-Q3**; shim: `src/lib/types/xstate.ts:97`. | Single authoritative definition eliminates structural drift. Source field in glossary updated. | Generation |
| DDD-017 | 2026-05-03 | ArtifactStatus | `ArtifactStatus` (from `src/lib/types/artifact.ts`) is the canonical term for the lifecycle state of an artifact in persistence. The former `PersistedArtifactStatus = 'generating' \| 'completed' \| 'failed'` in `generation.adapters.ts` was a structural duplicate with identical values. It is now `export type PersistedArtifactStatus = ArtifactStatus` (backward-compat alias). All consumer files that import `PersistedArtifactStatus` continue to compile without modification. Backward-compat alias `PersistedArtifactStatus` **removal-target: 2026-Q3**; shim files: `src/lib/adapters/generation.adapters.ts`, `src/lib/adapters/index.ts`, `src/lib/adapters/postgres-redis.stub.ts`. | Aligns with DDD-001 (canonical `ArtifactStatus` as source of truth). | Generation |
| DDD-020 | 2026-05-03 | ArtifactRelaunch | `ArtifactRelaunch` is the canonical concept for starting a new generation cycle from an existing `Artifact`. The relaunch entry must be defined once at domain level (not split into primary/secondary concepts). `HydrationResult` resolution is deterministic by source `ArtifactType`: direct context recovery for `extraction`, linked extraction recovery for `content` using `briefingId` and/or `extractionArtifactId`. The effective primary action after hydration remains `start-generation`; default runtime intent for artifact-driven relaunch entries is `regenerate`. **Scope note**: `ArtifactRelaunch` is a **governance-only concept** — it is not materialized as a TypeScript type in the codebase. Implementation uses `runtimeIntent: WorkflowRunMode` and `relaunchFromArtifactId` as concrete identifiers; these are valid implementation variants of the same domain concept and must not be promoted to separate domain terms. | Current frontend terms `ToolRelaunchIntent` and copy keys `relaunchPrimary`/`relaunchSecondary` indicate implementation/UI variants for one domain concept. Canonical UL requires one relaunch concept, one effective post-hydration generation-start CTA, and one default runtime intent for artifact-driven entries. | Frontend, Generation |
| DDD-021 | 2026-05-03 | ExtractionContext | `ExtractionContext` completeness is deterministic and mandatory at ToolStep dispatch time. The GenerationRequest for step 1 must include a non-empty `briefingText` and a structured `extractionPayload` resolved through ordered fallbacks: parsed extraction artifact content (raw JSON, fenced JSON, or payload envelope), then `sourceRequest.input.extractionPayload` when content is non-JSON. | Session evidence showed real smoke requests with large `briefingText` but empty `extractionPayload`, causing semantic context loss. Resolution is now enforced in frontend runtime and verified by tests: `frontend/src/features/tools/runtime/tools-client.ts:113-329`, `frontend/src/features/tools/ui/ToolPageTemplate.tsx:47-589`, `frontend/src/features/tools/runtime/tools-client.test.ts:134-187`. | Frontend |
| DDD-022 | 2026-05-03 | RouteType | `RouteType` (`'generic' \| 'tool' \| 'extraction' \| null`) is an **internal implementation type** local to `src/lib/machines/generation-system.machine.ts`. It is not a bounded-context domain term and must not appear in domain documentation or glossary entries. The canonical routing concepts remain `ToolWorkflow` (Generation context) and `SupportedTool` (Frontend context). | `RouteType` controls internal dispatch branching within `GenerationSystem`; its values map to `ToolWorkflow` values for routing logic. Registered 2026-05-03 to prevent future confusion with canonical terms and to block promotion attempts. | Generation |
| DDD-023 | 2026-05-04 | BackendStreamEvent / GenerationRequest (contract authority) | `frontend/src/features/generation/contracts/backend-stream.ts` is the **single authoritative FE source** for `BackendStreamEvent`, `GenerationRequest`, `ArtifactType`, and `OutputFormat`. No other FE file may re-define these types. A compile-time parity guard (`backend-stream.parity.guard.ts`) enforces structural identity with the pinned BE shapes in `src/lib/runtime/stream-contract.ts` and `src/lib/runtime/request-contract.ts`. Both BE source files carry doc-comments declaring their authority. | Eliminates silent drift across the FE↔BE contract boundary. Parity guard fails the typecheck if FE types drift from the pinned BE shapes. | Generation, Frontend |
| DDD-024 | 2026-05-04 | Legacy shim / deprecated export removal | Deprecated types and functions removed from `frontend/src/features/tools/runtime/tool-ux-state.ts` in Sprint 4: `ToolUiDerivationInput`, `ToolUiDerivationOutput`, `deriveCanonicalToolUiState` (old-style overload). File is now a pure re-export shim for `CanonicalToolUiState`, `PrimaryActionPolicy`, `SecondaryActionFlags`, `derivePrimaryActionLabel`. Removed from `frontend/src/features/tools/runtime/useToolForm.ts`: `useStepSelection` (unused, no runtime consumers), `useToolUiState` (test-only, mocked in tests but not imported at runtime). Deprecated optional event payload fields `hasExtractionContext?` and `hasPrimaryTargetStep?` removed from `PROGRESS_SYNCED` event type in `tool-page.machine.ts` — machine derives these internally via `deriveHasExtractionContext` and `deriveHasPrimaryTargetStep`. Admin dead code removed in Sprint 3: `listAdminModels`, `listAdminActivity`, associated types, `admin.models`/`admin.activity` from `ApiPaths` and `BackendCapabilities`. | Surface-area reduction aligned with PAT-002 (reachability matrix) and PAT-003 (thin ToolPage orchestration). All removals verified: typecheck clean, test baseline 208/211 (same 3 pre-existing fails). | Frontend |
| DDD-025 | 2026-05-04 | ToolKey (Generation context) | `ToolKey` is the canonical term in the Generation bounded context for the **step-orchestration identifier** carried in a `GenerationRequest` and stored in `GenerationSystemContext.toolKey`. It identifies which `toolWorkflowStepOrder` entry to use when resolving `WorkflowStep` dependency artifact IDs. Valid values are the step-capable subset of tools: `funnel-pages`, `nextland` (kebab-case). It is distinct from `ToolWorkflow` (artifact routing + type determination, snake_case values: `funnel_pages`, `nextland`, `meta_ads`, `extraction`) — both fields coexist in `GenerationSystemContext` and serve orthogonal concerns. The implementation type `SupportedToolWorkflow` in `src/lib/runtime/tool-workflow-registry.ts` is the closed-set enforcement for `ToolKey` values; it is a valid implementation detail, not a domain term. ACL translation at the Generation ↔ Frontend boundary: `ToolKey` (Generation) ← `SupportedTool` (Frontend). See also DDD-011 (`ToolKey` as Frontend registry type), DDD-C-001 (resolved: `ToolWorkflow` vs `SupportedTool`), DDD-C-005 (open: naming-convention divergence). | `GenerationSystemContext` in `src/lib/types/xstate.ts` carries two fields that reference "the same tool" from orthogonal angles: `toolKey` (step orchestration) and `workflowType` (artifact routing). Neither was documented as a distinct domain concept, causing ambiguity at code-review and onboarding: reviewers conflate the two or treat `toolKey` as a redundant alias for `workflowType`. Additionally, `toolKey` values use kebab-case (`funnel-pages`) while `ToolWorkflow` values use snake_case (`funnel_pages`), a silent naming-convention divergence for the same logical tool. Registering `ToolKey` as canonical in Generation context closes the documentation gap, confirms the two fields are intentionally distinct, and grounds the divergence in DDD-C-005. | Generation |
| DDD-026 | 2026-05-04 | Tool | `Tool` is the canonical cross-context **Concept** for a system instrument that encapsulates a complete user-facing capability: structured input intake, an ordered `WorkflowStep` chain, progressive context enrichment across steps, and final `Artifact` production. Each Tool addresses one specific user problem; steps within a Tool may be reused across Tools. `Tool` is the organizing concept that gives the application its core purpose. It is expressed as `SupportedTool` (Frontend identifier), routed via `ToolWorkflow` (Generation routing), and step-ordered via `ToolKey` (Generation orchestration). The term `Tool` must not be used as a synonym for `SupportedTool`, `ToolWorkflow`, or `ToolKey` — those are context-local representations of the same underlying concept. Currently implemented Tools: `funnel-pages`, `nextland`. | The codebase uses `toolKey`, `SupportedTool`, and `ToolWorkflow` extensively but never defines what a Tool *is* at the domain level. Without a canonical Concept entry, documentation and onboarding conflate the identifier (ToolKey/SupportedTool) with the concept itself, and new contributors cannot understand the organizing principle of the application from the UL alone. | all contexts |
| DDD-027 | 2026-05-04 | WorkflowStepType | `WorkflowStepType` is the canonical term for the execution strategy category of a `WorkflowStep`. Values: `extraction` (LLM-driven structured extraction, handled by `ExtractionChain`), `generation` (LLM-driven content production, handled by `StreamTransport` + `PersistenceBatch`), `acquisition` (external API data retrieval — **provisional**, not yet implemented). Status: **provisional** — `WorkflowStepDescriptor` does not yet carry a `type` field in code; type classification is currently implicit in the actor wiring within `toolWorkflowMachine`. When runtime support for `acquisition` steps is added, `WorkflowStepDescriptor` must be extended with a `type: WorkflowStepType` field and this entry promoted to `canonical`. | The introduction of the `Tool` concept (DDD-026) requires naming the classes of steps that compose a Tool's chain. `extraction` and `generation` are already implemented as distinct actors; `acquisition` is a planned capability. Establishing `WorkflowStepType` now prevents ad-hoc naming when acquisition is implemented and grounds the distinction between step execution strategies in the UL. | Generation |
| DDD-028 | 2026-05-04 | StepHydration (reclassification) | `StepHydration` is reclassified from "Domain Service" to **"Client-Side Projection"** in the UL. It projects backend-owned `WorkflowStep` state (persisted as `Artifact` history) into FE-local context for resume/regenerate flows. It does not own or compute domain logic: step ordering authority is `toolWorkflowStepOrder` (BE, `tool-workflow-registry.ts`); dependency resolution authority is `resolveStepDependencyIds` (BE) and `/api/tools/orchestrate` (BE endpoint). FE functions `getStepDependencies` and `collectCompletedStepsByTool` are FE-side read projections, not domain logic. **Architecture note**: at time of writing, FE `ToolPageTemplate.tsx:591` calls `getStepDependencies` (local FE compute) instead of `orchestrateToolStep` (BE endpoint), which has zero runtime callers. `orchestrateToolStep` (`tools-client.ts:339`) is dead weight from the prior FE-heavy architecture. This is a code-level drift that the UL now flags explicitly; resolution requires a future refactor to route step dependency resolution through the BE endpoint. See DDD-C-007. | "Domain Service" implies ownership of domain logic. In the intended BE-first architecture, Frontend owns only interaction and display. Classifying a FE artifact reconstruction function as a Domain Service was a terminology residue from the prior architecture in which FE shared domain logic with BE. Reclassification aligns the UL with the target architecture and makes the code-level drift visible. | Frontend |
| DDD-019 | 2026-05-03 (revised 2026-05-04) | toolStepOrder | ~~`toolStepOrder` is the single canonical constant mapping each `SupportedTool` to its ordered `ToolStep[]`. Defined once in `frontend/src/features/tools/machines/tool-flow.machine.ts`.~~ **Revised**: `toolStepOrder` (FE) and `toolWorkflowStepOrder` (BE, `src/lib/runtime/tool-workflow-registry.ts`) are currently in a mirroring relationship where FE is the source. This is a residue of the prior FE-heavy architecture. In the target BE-first architecture, `toolWorkflowStepOrder` (BE) should be the single canonical source; `toolStepOrder` (FE) should be a consumed projection. **Current status**: both constants coexist with identical values; the BE file comment says "mirrors the frontend". Promotion of BE as source is a future code refactor target, tracked in DDD-C-007. For now, `toolStepOrder` (FE) remains the effective single source in code (DDD-019 original intent preserved) but is flagged as architecture drift. | Per original DDD-019: a duplicated constant with identical values is a drift vector. Revised 2026-05-04 audit: the DDD-019 decision implicitly positioned FE as source-of-truth for step ordering, contrary to the BE-first architecture principle. The revision flags this without requiring an immediate code change. | Frontend (current), Generation (target) |
| DDD-029 | 2026-05-04 | ToolKey (cross-context), ToolFormKey | Two decisions consolidated: (1) `ToolKey` is promoted to the **cross-context canonical identifier** for Tool identity across all bounded contexts. Values: `funnel-pages`, `nextland` (kebab-case). `SupportedTool` (FE) is now the Frontend-layer projection of `ToolKey`; `SupportedToolWorkflow` (BE type, `tool-workflow-registry.ts:10`) is a closed-set enforcement type — implementation detail per DDD-018 pattern, not a domain term. (2) The old FE form registry implementation type previously called `ToolKey` (DDD-011) is renamed **`ToolFormKey`** (`keyof typeof toolFormRegistry`). `ToolFormKey` is an implementation type only; docs must use `SupportedTool` or `ToolKey` for domain concepts. Supersedes DDD-011 (FE scope) and DDD-025 (Generation scope) for cross-context authority. | Three names for the same concept (`SupportedTool`, `SupportedToolWorkflow`, `ToolKey`) coexisted with no single canonical. Additionally, the FE form registry reused `ToolKey` as an implementation type, creating a collision with the Generation-context `ToolKey`. Consolidation resolves both issues: one canonical cross-context term for tool identity, one distinct name for the form registry impl type. | all contexts |
| DDD-030 | 2026-05-04 | meta_ads (deprecated ToolWorkflow value) | `meta_ads` is deprecated and removed as a `ToolWorkflow` value. It was documented as a "planned generic workflow" with no Frontend `SupportedTool` counterpart. It is now confirmed as an obsolete reference with no planned implementation. Must be removed from all code references to `ToolWorkflow` value sets and from any route/switch logic that branches on it. | User confirmed: `meta_ads` is an obsolete reference that can disappear. Retaining it as a ToolWorkflow value would create a dead entry in a canonical enumeration, which is a drift vector. Removal enforces the principle that `ToolWorkflow` values represent active or intentionally-staged routing paths only. | Generation |
| DDD-032 | 2026-05-04 | GenerationRequestAssembly | **Provisional** — `GenerationRequestAssembly` is the canonical term for the Application-Layer process that translates accumulated FE session state — `HydrationResult`, `ExtractionContext`, and selected `ToolStep` — into a well-formed `GenerationRequest` command ready for BE dispatch. The process runs entirely in the Frontend runtime but produces a BE-owned domain command; scope is therefore Generation (output ownership). Implemented in `frontend/src/features/tools/runtime/tools-client.ts:113-329` (~216 lines). The process is not a Domain Service (it contains no domain logic) and not a Value Object (it is a transformation, not a data structure); type classification: **Process** (Application Layer, boundary-crossing translation). Status: provisional — term introduced to establish canonical vocabulary before any potential refactor of the assembly logic into a dedicated module. | Three FE concepts (`HydrationResult`, `ExtractionContext`, `ToolStep`) compose into one BE command (`GenerationRequest`) through ~216 lines of code with no domain name. Without a canonical term, the assembly block is invisible in planning, ADRs, and code review. Naming it makes the boundary-crossing intent explicit and grounds the concept in the UL before architectural decisions about ownership are finalized. | Generation (output ownership) |
| DDD-031 | 2026-05-04 | ToolStepOrchestration | **Provisional** — `ToolStepOrchestration` is the target canonical term for the backend operation that determines which prior `Artifact` IDs a given `ToolStep` depends on, given a `ToolKey` and the current step completion history. In the target BE-first architecture: the operation is owned by `resolveStepDependencyIds` (BE, `tool-workflow-registry.ts:27`), exposed via `/api/tools/orchestrate` (BE endpoint), and consumed by the FE via `orchestrateToolStep` adapter (`tools-client.ts:339`). FE-local `getStepDependencies` is the current production implementation but is a code-level drift target (DDD-C-007). Status: provisional — promoted to canonical when DDD-C-007 refactor is executed. | Registering the target canonical term before the refactor ensures that planning, ADRs, and PR descriptions use consistent vocabulary and that the term is available in the UL for cross-reference. Without a canonical term, the refactor risks introducing a third synonym. | Generation |
| DDD-033 | 2026-05-04 | ArtifactRole | `ArtifactRole` is the canonical Value Object classifying the role of an `Artifact` within a multi-step Tool execution chain. Values: `'step'` (intermediate artifact produced by a non-final `WorkflowStep`; its output feeds dependency resolution for subsequent steps) and `'final'` (artifact produced by the last `WorkflowStep` in the chain; represents the complete Tool output). Persisted in the artifact input JSON under `toolWorkflow.artifactRole` (see `ToolWorkflowPersistenceMetadata`, DDD-034 and DDD-050). Consumed by `StepHydration` for resume/regenerate classification and by Frontend display logic to determine CTA visibility. Status: **canonical** — value set is stable and persisted in DB. | The distinction between step and final artifacts is operationally fundamental: `StepHydration` must know whether a prior artifact is a dependency input or a terminal output. Without a canonical name the distinction is invisible in UL, ADRs, and code review. Gap surfaced by Generation context coverage audit 2026-05-04 (G-003). | Generation |
| DDD-034 | 2026-05-04 | ToolWorkflowPersistenceMetadata | `ToolWorkflowPersistenceMetadata` is the canonical Value Object embedded under the `toolWorkflow` key in an `Artifact`'s persisted input JSON when `routeType = 'tool'`. Base orchestration shape: `{ toolKey: ToolKey, workflowType: ToolWorkflow, runMode: WorkflowRunMode, artifactRole: ArtifactRole, stepKey: string, dependsOnSteps: string[], dependencyArtifactIds: string[], dependencyArtifactIdsByStep: Record<string, string> }`. Written at artifact creation time by `buildToolWorkflowPersistenceMetadata` in `generation-system.machine.ts`; read by FE `StepHydration` to reconstruct step context for resume/regenerate flows, and by BE `extractStepFromArtifactInput` (`tool-workflow-registry.ts`) to identify step key from artifact history. This JSON envelope remains the authoritative source for orchestration semantics. **Revision note**: queryable denormalized columns (`session_id`, `step_key`, `artifact_role`, `run_mode`) are governed by DDD-050 and do not replace the JSON authority defined here. Status: **canonical** — base orchestration contract remains stable; queryability revision is additive via DDD-050. | The `toolWorkflow` JSON blob in the artifact input is the foundational persistence contract for the multi-step Tool flow, but it had no UL name. Without a canonical term it cannot be referenced in integration constraints, ADRs, or the BCM translation table. Gap surfaced by Generation context coverage audit 2026-05-04 (G-004). | Generation |
| DDD-035 | 2026-05-04 | WorkflowStepUnlocked | **Canonical** — `WorkflowStepUnlocked` is the domain event emitted by `toolWorkflowMachine` when the dependencies of a `WorkflowStep` are satisfied and the step is ready to execute. Internal event: does not cross the process boundary (not on SSE wire). Triggers the transition from `'idle'` to `'running'` for the step's `WorkflowStepState`. Semantically significant for domain: represents the progression of a multi-step workflow. Emitted by `toolWorkflowMachine` within `GenerationSystem`; consumed by the same actor context. | Multi-step Tool execution relies on dependency-driven step progression. The unlock event is the boundary condition that makes the progression deterministic. Coverage audit G-005 surfaced this as a domain event missing from UL. | Generation |
| DDD-036 | 2026-05-04 | WorkflowStepCompleted | **Canonical** — `WorkflowStepCompleted` is the domain event emitted by `toolWorkflowMachine` when a `WorkflowStep` finishes execution (successfully or with failure) and its `WorkflowStepState` transitions to terminal status (`'done'`, `'error'`, `'skipped'`). Internal event: does not cross the process boundary. Contains the artifact ID produced by the step (if successful) and error metadata (if failed). The completion event serves the dependency graph: downstream steps may only unlock when their dependencies have completed. | Step completion state is fundamental to the multi-step execution contract. Without a canonical event name, internal dependency logic appears ad-hoc. Explicit naming makes the progression semantics visible in domain language. Coverage audit G-005 surfaced this as a domain event missing from UL. | Generation |
| DDD-037 | 2026-05-04 | WorkflowStepBootstrap | **Canonical** — `WorkflowStepBootstrap` is the Value Object that represents the initial state injected into a `ToolWorkflowMachine` when resuming or regenerating from a prior `Artifact`. Shape: `{ stepKey: string, output: string, artifactId: string }`. When `WorkflowRunMode = 'resume'` or `'regenerate'`, the `ToolWorkflowInput` receives a non-null `bootstrap` field containing the prior step state. This enables the machine to skip already-completed steps and continue from the resume point. Persisted in `ToolWorkflowPersistenceMetadata` as part of hydration. See DDD-034, DDD-050, DDD-031, DDD-020 (`ArtifactRelaunch`). | Resume/regenerate flows require a checkpointing mechanism: which step to start from, and what artifacts are already complete. Bootstrap encapsulates this contract explicitly. Without a canonical name, the resume logic is invisible in UL vocabulary. Coverage audit G-009 surfaced this as a gap. | Generation |
| DDD-038 | 2026-05-05 | HydrationResult completeness / ReadinessSnapshot | Artifact-driven relaunch readiness must be gated by **complete** recovered extraction context, not by the mere presence of a non-null `HydrationResult`. For `ArtifactType = extraction`, both local FE hydration and `/api/tools/hydrate` must recover the same semantic minimum: `extractionArtifactId`, `briefingId`, and non-empty normalized briefing text. `ReadinessSnapshot.hasExtractionContext` must remain `false` until that minimum is satisfied. **`extractionPayload` is not part of the readiness gate**: production evidence (2026-05-05) confirmed that historical extraction artifacts do not store a structured payload, yet their `normalizedText` alone is sufficient for `GenerationRequest` assembly. The FE `hasCompleteHydrationResult` predicate therefore requires only `extractionArtifactId + briefingId + normalizedText`; `extractionPayload` is passed through as-is (may be `{}`) and continues to be forwarded in `GenerationRequest.input.extractionPayload` (DDD-021 scope). **Local hydration fallthrough rule**: if the local in-memory extraction artifact has no recoverable text or payload (both empty), the FE must skip local resolution and fall through to `/api/tools/hydrate`. **BE Step 1 fallthrough rule**: if the directly-referenced extraction artifact has no recoverable text or payload (both empty), the BE must fall through to Step 2 (recency-ranked listing) to find the most complete artifact for the project. | Production smoke test on artifact `artifact-rqto8szw` (2026-05-05): local artifact found in 110-item cache but `normalizedTextLength: 0` and `extractionPayloadKeys: 0`. After local fallthrough, BE Step 2 returned `normalizedTextLength: 149679` and `extractionPayloadKeys: 0` — relaunch succeeded with text-only context. Original DDD-038 incorrectly included `extractionPayload` in the readiness gate, conflating the hydration completeness contract with the dispatch completeness contract (DDD-021). The two contracts are now separated: hydration gate = text-only; dispatch payload = text + optional structured payload. | Frontend, Generation |
| DDD-039 | 2026-05-05 (revised 2026-05-06) | ToneProfile | **Provisional** — `ToneProfile` is the target Value Object for user-selected tone in ToolStep content artifact generation (`optin`, `quiz`, `vsl`, and equivalent outputs). Current implementation behavior: `GenerationRequest.input.tone` is forwarded as an optional passthrough string parsed from entry params (for example `?tone=`), with no constrained enum validation and no dedicated ToolPage tone selector. Target governance (to be promoted to canonical after implementation) remains: constrained values `Professional` (default), `Casual`, `Formal`, `Technical`, explicit UI selection, and deterministic defaulting. | PR review and code evidence (2026-05-06) show a mismatch between prior wording and current runtime: parsing is free-form (`tool-entry-params.ts`) and dispatch is passthrough (`useToolPage.ts`), while ToolPage currently lacks a dedicated tone selector. Revising DDD-039 to provisional removes the contradiction and preserves the target model for later implementation. | Frontend, Generation |
| DDD-040 | 2026-05-07 | Youtube LF Script tool identity | Canonical identity for the new Tool is fixed as follows: `ToolKey = youtube-lf-script` (kebab-case), `DisplayName = Youtube LF Script`, `ToolWorkflow = youtube_lf_script` (snake_case). Deprecated aliases: `youtube-long-form`, `youtube_long_form`. | Fixes naming drift across plan, runtime registry, URL routes, and workflow persistence metadata; preserves kebab/snake cross-layer convention (DDD-C-005). | all contexts |
| DDD-041 | 2026-05-07 | ToolStep (youtube-lf-script sequence) | Canonical `ToolStep` sequence for `youtube-lf-script` is prompt-chat-faithful and ordered as: `pre-script-analysis` → `packaging` → `intro-structure` → `body-structure` → `native-cta-embeds` → `outro-structure`. Final step is `outro-structure`. | Eliminates ambiguity between legacy 5-step chain and approved prompt-chat sequence; keeps deterministic orchestration and artifact role assignment. | Frontend, Generation |
| DDD-042 | 2026-05-07 | ExtractionContext (youtube-lf-script schema) | Canonical `ExtractionContext` fields for `youtube-lf-script`: `knowledge_content`, `avatar`, `pain_point`, `purchase_process_type`, `offer`, `proof`, `tone`, `target_duration_minutes`, `proprietary_methodology_disclosure`. Missing fields must be normalized to `null` (no inferred defaults). | Aligns extraction output with the approved prompt-chat input contract and prevents silent fallback semantics. | Frontend, Generation |
| DDD-043 | 2026-05-07 | ReadinessSnapshot (youtube-lf-script minimum gate) | For `youtube-lf-script`, readiness minimum required extraction fields are: `knowledge_content`, `avatar`, `pain_point`, `offer`, `proof`. Other canonical fields may be `null` without blocking start. | Makes readiness deterministic and compatible with nullability policy (DDD-042). | Frontend |
| DDD-044 | 2026-05-07 | `/api/tools/briefs` toolKey contract | Endpoint contract accepts `toolKey` in both query and body; precedence rule is `body > query`; if missing in both, request is rejected with HTTP `400` and explicit validation error. | Maximizes backward compatibility while making source-of-truth precedence deterministic and testable. | Generation, Frontend |
| DDD-045 | 2026-05-07 | Regenerate stale policy | On regenerate of a non-terminal step, downstream artifacts are preserved, marked `stale`, readable for traceability, and not finalizable until recomputed in sequence. | Preserves audit continuity while preventing invalid finalization over stale dependency chains. | Frontend, Generation |
| DDD-046 | 2026-05-07 | Generation language and model default (youtube-lf-script) | Generation output language for `youtube-lf-script` is always Italian. Model selection remains user-controlled in Frontend; default input value is `openrouter/auto`. | Aligns output policy with business requirement while preserving existing frontend model-selection behavior. | Frontend, Generation |
| DDD-047 | 2026-05-07 | WorkflowSessionIdentifier | `WorkflowSessionIdentifier` is the canonical Value Object for cross-request correlation in multi-step Tool workflows. It is generated by Frontend when ToolPage initializes and propagated in each `GenerationRequest.sessionId`. Scope uniqueness: per `(userId, projectId, toolKey, session runtime window)`. | Replaces heuristic grouping by `(projectId + toolKey + time proximity)` with deterministic session identity. | Frontend, Generation |
| DDD-048 | 2026-05-07 | GenerationSession | `GenerationSession` is the canonical Aggregate Root (Generation context) that groups all `Artifact` entities produced in one multi-step Tool execution. Attributes include: `sessionId`, `toolKey`, `projectId`, `userId`, `runMode`, ordered step-state progression, and artifact membership. | Makes multi-artifact execution queryable and deterministic without changing `Artifact` entity identity (composition, not inheritance). | Generation |
| DDD-049 | 2026-05-07 | SessionArtifactGroup | `SessionArtifactGroup` is the canonical Frontend/UI read model Value Object for session display. It projects session state as ordered step artifacts with `stepKey`, `artifactRole`, status, and content, suitable for tabs/step navigation UX. | Separates backend orchestration aggregate (`GenerationSession`) from frontend display projection while preserving deterministic ordering. | Frontend |
| DDD-050 | 2026-05-07 | ToolWorkflowPersistenceMetadata (revision) | Revision of DDD-034 after session aggregation implementation. `ToolWorkflowPersistenceMetadata` keeps JSON as orchestration authority and adds denormalized query cache columns in `artifacts`: `session_id`, `step_key`, `artifact_role`, `run_mode`. Source-of-truth hierarchy: `input_json.toolWorkflow` for orchestration semantics; DB columns for indexed queries/display. This decision refines queryability and persistence-read concerns without superseding the base orchestration contract defined in DDD-034. | Maintains compact orchestration metadata while enabling deterministic low-latency session queries and role-based filtering. | Generation |

## Open Naming Conflicts

| Conflict ID | Candidate Terms | Impacted Areas | Proposed Resolution | Status |
| --- | --- | --- | --- | --- |
| DDD-C-001 | `ToolWorkflow` (backend `src/lib/types/artifact.ts`) vs `SupportedTool` (frontend `tool-flow.machine.ts`) | Generation ↔ Frontend/UI boundary | Both terms are kept as context-local: `ToolWorkflow` in Generation, `SupportedTool` in Frontend. The bounded context map documents the translation rule. No unification needed — they have different value sets and concerns. | resolved |
| DDD-C-002 | `WorkflowStep` (backend abstract) vs `ToolStep` (frontend concrete) | Generation ↔ Frontend/UI boundary | Kept distinct per DDD-003 and DDD-004. Backend `WorkflowStep` is an abstract descriptor; frontend `ToolStep` is a concrete named value. | resolved |
| DDD-C-003 | `extractionPayload` (field name in code) vs `ExtractionContext` (DDD canonical term) | Frontend codebase | `ExtractionContext` is the canonical DDD term (DDD-007). Field name `extractionPayload` is acceptable as an implementation detail in the code. Docs corrected to use `ExtractionContext` where referring to the domain concept (2026-05-03). | resolved |
| DDD-C-004 | `relaunchPrimary` / `relaunchSecondary` vs `ArtifactRelaunch` | Frontend/UI artifact detail and tool entry hydration | Resolved by converging to one domain concept (`ArtifactRelaunch`) and one hydrated-ready path with one effective generation-start action. Governance baseline now also fixes default runtime intent for artifact-driven relaunch entries to `regenerate` under DDD-020. | resolved |
| DDD-C-005 | `toolKey` values (kebab-case: `funnel-pages`) vs `ToolWorkflow` values (snake_case: `funnel_pages`) | Generation context internal (`GenerationSystemContext`, `GenerationRequest`) | Both identifiers reference the same logical tool within `GenerationSystemContext` but use different naming conventions. `ToolKey` (step orchestration, DDD-025) uses kebab-case, mirroring frontend `SupportedTool`; `ToolWorkflow` (artifact routing) uses snake_case, matching the DB `workflow_type` column. No unification required — they serve orthogonal concerns and different value sets. The convention divergence must be acknowledged in the `domain-bounded-context-map.md` ACL translation table (note: `SupportedTool`/`ToolKey` use kebab-case; `ToolWorkflow` uses snake_case for DB compatibility). | open |
| DDD-C-006 | `extraction` in `ToolWorkflow` vs `extraction` in `WorkflowStepType` | Generation context | Two orthogonal concepts share the same token `extraction`. `ToolWorkflow = 'extraction'` is a direct routing path that bypasses `toolWorkflowMachine` entirely (`generation-system.machine.ts:532-554`); it is NOT a Tool in the DDD-026 sense. `WorkflowStepType = 'extraction'` is a step execution strategy handled by `ExtractionChain`. No renaming required — both usages are established in code and stable. Resolution: both glossary entries carry an explicit cross-reference note clarifying the distinction (see `ToolWorkflow` and `WorkflowStepType` entries in the glossary). | resolved-documented |
| DDD-C-007 | `getStepDependencies` (FE) vs `resolveStepDependencyIds` (BE) / `orchestrateToolStep` (FE dead) | Generation ↔ Frontend/UI | Step dependency resolution logic is duplicated: FE `getStepDependencies` (`tool-generation-engine.ts:39`) computes step deps locally and is called at runtime (`ToolPageTemplate.tsx:591`). BE `resolveStepDependencyIds` (`tool-workflow-registry.ts:27`) is the authoritative implementation and is exposed via `/api/tools/orchestrate`. FE `orchestrateToolStep` (`tools-client.ts:339`) is the intended BE-calling adapter but has **zero runtime callers** — dead weight from prior FE-heavy architecture. Provisional canonical target term: `ToolStepOrchestration` (DDD-031). Resolution: future refactor should replace `getStepDependencies` call in `ToolPageTemplate.tsx` with `orchestrateToolStep` (BE call), remove `getStepDependencies` from FE runtime, and make `toolStepOrder` (FE) a downstream projection of `toolWorkflowStepOrder` (BE). Until the refactor, the FE compute is the effective implementation. | open — provisional term registered (DDD-031); code refactor required |
